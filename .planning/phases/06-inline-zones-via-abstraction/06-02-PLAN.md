---
phase: 06-inline-zones-via-abstraction
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - packages/editor/src/StrudelEditor.tsx
autonomous: true
requirements: ["ZONE-04"]
gap_closure: true

must_haves:
  truths:
    - "StrudelEditor calls resume() on play after zones were paused"
  artifacts:
    - path: "packages/editor/src/StrudelEditor.tsx"
      provides: "resume() call in handlePlay"
      contains: "resume()"
  key_links:
    - from: "StrudelEditor.tsx handlePlay"
      to: "InlineZoneHandle.resume()"
      via: "viewZoneCleanupRef.current?.resume()"
      pattern: "viewZoneCleanupRef\\.current\\?\\.resume\\(\\)"
---

<objective>
Close ZONE-04 gap: add the missing `viewZoneCleanupRef.current?.resume()` call in StrudelEditor's `handlePlay` function.

Purpose: The `InlineZoneHandle.resume()` method exists and is unit-tested, and `pause()` is called on stop, but `resume()` is never called on play. This means if zones are paused and play is re-triggered without re-evaluation, renderers stay frozen. Adding `resume()` completes the pause/resume lifecycle contract specified by ZONE-04.

Output: StrudelEditor.tsx with `resume()` wired into handlePlay. All existing tests still pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inline-zones-via-abstraction/06-01-SUMMARY.md
@.planning/phases/06-inline-zones-via-abstraction/06-VERIFICATION.md

<interfaces>
From packages/editor/src/visualizers/viewZones.ts:
```typescript
export interface InlineZoneHandle {
  cleanup(): void
  pause(): void
  resume(): void
}
```

From packages/editor/src/StrudelEditor.tsx (current state):
```typescript
const viewZoneCleanupRef = useRef<InlineZoneHandle | null>(null)

// handlePlay line 176-185: creates zones but never calls resume()
// handleStop line 201-206: correctly calls pause()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resume() call in handlePlay</name>
  <files>packages/editor/src/StrudelEditor.tsx</files>
  <read_first>
    - packages/editor/src/StrudelEditor.tsx (lines 139-199, the handlePlay function)
    - packages/editor/src/visualizers/viewZones.ts (InlineZoneHandle interface)
  </read_first>
  <action>
In `packages/editor/src/StrudelEditor.tsx`, inside the `handlePlay` function, add `viewZoneCleanupRef.current?.resume()` after the inline zone block (after line 185, after the closing `}` of the `if (_inlinePianoroll && editorRef.current)` block).

This placement ensures:
1. If zones were just freshly created (the `if` branch ran): `resume()` is a no-op since freshly mounted renderers are already running.
2. If zones already exist and were paused (the `if` branch was skipped because `_inlinePianoroll` is false, or zones persist from a previous play): `resume()` unfreezes them.

The exact edit: After the closing `}` on line 185 (end of the `if (_inlinePianoroll ...)` block), add a blank line then:
```typescript
    // Resume inline zones if they were paused by a previous stop (ZONE-04)
    viewZoneCleanupRef.current?.resume()
```

Do NOT move or modify any other lines. This is a single-line addition with a comment.
  </action>
  <verify>
    <automated>cd /Users/mrityunjaybhardwaj/Documents/projects/struCode && pnpm --filter @strucode/editor test --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "viewZoneCleanupRef\.current?\.resume()" packages/editor/src/StrudelEditor.tsx returns at least one match inside handlePlay
    - grep -c "\.resume()" packages/editor/src/StrudelEditor.tsx returns at least 1
    - pnpm --filter @strucode/editor test --run exits 0 (all tests pass)
  </acceptance_criteria>
  <done>StrudelEditor.tsx calls viewZoneCleanupRef.current?.resume() in handlePlay, completing the ZONE-04 pause/resume lifecycle contract. All existing tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -n "resume()" packages/editor/src/StrudelEditor.tsx` shows the new resume() call in handlePlay
2. `grep -n "pause()" packages/editor/src/StrudelEditor.tsx` still shows pause() call in handleStop
3. `pnpm --filter @strucode/editor test --run` — all tests pass (90/90+)
4. No other files modified
</verification>

<success_criteria>
- ZONE-04 fully satisfied: StrudelEditor calls pause() on stop AND resume() on play
- All existing tests pass without modification
- The handlePlay function contains viewZoneCleanupRef.current?.resume()
</success_criteria>

<output>
After completion, create `.planning/phases/06-inline-zones-via-abstraction/06-02-SUMMARY.md`
</output>
