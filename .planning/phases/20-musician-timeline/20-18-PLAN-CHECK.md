# 20-18-PLAN-CHECK — Goal-Backward + Standard + Cognitive Verification

**Verdict: PASS (with 3 MINOR refinements the planner should fold; none block execution).**

Checked against: live source (`parseStrudel.ts` pS:599/791/950/1033/1351 + the
G2→noteMatch→sMatch→miniMatch→looseMatch arm order — ALL byte-verified accurate),
CONTEXT (AMENDED D-03 + P70 R-1 FINDING), RESEARCH R-1..R-5 + OPAQUE BOUNDARIES,
hetvabhasa P70/P69/P68/P67, vyapti PV53/PV52/PV50/PV49, krama PK18/PK17/PK16,
20-17-PLAN D-1c + 20-17-OBSERVATIONS:~470 (producer-precedence blind spot).

This plan-check found NO structural defects of the 20-17 class (which took 2
rounds). The plan correctly anchors the GOAL on the AMENDED D-03, uses the
orchestrator whole-program-fixpoint observation (NOT the RESEARCH isolated-probe
pessimism), and front-loads the single highest-risk uncertainty (the az2
chain-ARG-signal disposition) into an EXECUTED Wave-0 prototype. The dimension
sweep is below; the 3 MINOR items are refinements, not blockers.

---

## Standard dimensions 1-7

**1. Goal clarity — PASS.** The GOAL block (PLAN:30-50) anchors verbatim on the
AMENDED D-03: crit-1 HARD (`--LsnlgQ6osk` + #3 `-6c1hEXe8Agi` + #7
`-KLGNJUtyyj1` STRUCTURED), crit-2 INFORMATIONAL (must-not-regress 86.0%, NO ≥N
threshold), no-bar-lowering on crit-1 + floor. The superseded ≥90% block is
explicitly demoted to a "PROVENANCE POINTER" (PLAN:58-63) and never treated as
live. The plan USES the whole-program-fixpoint reality
(`resolved=[rp1,beat,chords2,bass,harm2] pending=[az2-only]`, PLAN:76-90) and
explicitly rejects the RESEARCH R-1/R-5 "multi-blocker" framing as an
isolated-descriptor-probe artifact — exactly the correction CONTEXT's
orchestrator note mandates. No relitigation.

**2. Task executability — PASS.** Every task is a PROMPT: read_first with
file:line + an explicit RE-GREP-the-live-anchor first action (PLAN:197, 248,
310, 474 — the 20-17 line-shift discipline), executable numbered actions,
ownership, lifecycle (PK-tagged), pre_mortem, verify with direct-observation
gates (executed probe / build-grep / proto verdict / per-file loc STOP), done.
Wave 0 action 5 (the Lokayata az2 prototype) is a fully specified executed
probe with a (a)/(b) verdict that GATES the taxonomy scope.

**3. Dependency correctness — PASS.** Graph acyclic (PLAN:670-701): linear spine
0→A→B→C→D→E, V-1/V-2 fan from E-1, V-3 joins all code waves+V-2, V-4 closes.
Gate-bearing closers correctly sequenced: C-1 GROUNDS chord/arrange (Grounding
Check) BEFORE modelling; #3/#7 (clean) land in C BEFORE the risky `--LsnlgQ6osk`
reclaim is asserted in V-1 (fail-fast front-loaded). The PV53 consumer audit is
its OWN wave (D-1) with its OWN acceptance tests (the round-trip + the
MusicalTimeline projection spec) — the 20-17 D-1c precedent honored exactly.

**4. Verification sufficiency — PASS.** Every wave has a direct-observation
gate: Wave 0 the executed classification + prototype; A/B/C the build +
minification-stable string-literal grep + the executed strict-widen probe; B-E
the per-file loc-fidelity STOP gate; D the two new acceptance tests + collect.ts
event-neutrality; V-1 the AMENDED dual gate verbatim (crit-1 three production
assertions + crit-2 informational %+86% regression STOP). No residual ≥90%
language is treated as live anywhere (grep-confirmed: every ≥90 mention is
flagged SUPERSEDED/provenance).

**5. Scope discipline — PASS.** No relitigation of LOCKED D-01/D-02/D-04 or the
amended D-03. The FROZEN curated-set membership is locked in Wave 0 and named a
"FROZEN scope contract" that does NOT grow mid-phase without provenance + a
re-pose (PLAN:205, the 20-16 4×-cascade lesson). New gap classes →
backlog→20-19, never patched (PLAN:368, 521, 533). #156 classified-NOT-folded
(Hydra mashup, R-1), #149/#147/#153 classify-don't-fold — all per CONTEXT.

**6. Coverage — PASS.** D-01 (curated gate-bearing + flagged-general
off-by-default never-gate-counted: Wave B + Wave E with the structural one-arg
oracle guard). D-02 (genuine `Signal`/`Builder` modelling, matcher-stays-matcher
asserted PLAN:112-121, 842-849). D-04 (distinct Layer-1/2 sibling; Phase 24
untouched — stated, not re-scoped). The signal-valued-chain-ARG question is
settled EARLY by the Wave-0 Lokayata prototype by observation, NOT inference —
the single most important coverage property and it is correctly placed.

**7. Operational — PASS.** EXECUTOR NOTES (PLAN:780-895) has the structural
single-quoted-heredoc COMMIT_TEMPLATE (never `-m` with backticks/`$()` — the
20-16 2× recurrence), the P68 minification-stable STRING-LITERAL anchor
discipline (explicitly NOT a comment/param, with the keepNames caveat and the
tsup-watch-dies-on-DTS-fail P68 hardening), PK18 cascade discipline,
post-merge `git merge-base --is-ancestor` artifact verification (not the
badge — the 20-14 lesson), AnviDev branch-first / single-non-stacked-PR /
Claude-never-merges / `closes #155`-only + manual-#156-handling.

---

## Cognitive dimensions A-F

**A. Vyapti alignment — PASS.** PV53: the consumer audit is its own
primary-risk wave (D-1) with the live grep as source of truth, the R-3 list a
documented FLOOR (STOP on any non-floor hit / any missing floor name), the
20-17-OBSERVATIONS:~470 producer-precedence blind-spot EXPLICITLY re-checked
(D-1 action 4, result required in commit body). PV50: the flagged-general flag
is a stack-threaded optional param, never module state (Wave E, the G4 idiom).
PV49: per-file loc-fidelity STOP gate per-wave + cross-wave (V-3), allow-list =
exactly {3 V-2 fixtures}, definition-site loc-safety argued not defensively
extended. PV52/PV53 byte-identical-fence: the A-wave additive-union widen
mirrors the `Code.via {literal:true;raw}` precedent; opaque fence
`tag==='Code' && via===undefined` asserted byte-IDENTICAL (A-1 action 4).

**B. Krama correctness — PASS.** The `recogniseChainRoot` placement (AFTER G2,
BEFORE noteMatch) is PROVEN not asserted: B-1 action 4 is an executed
strict-widen + controls-unchanged + user-shadow probe (the 20-17 G2 placement
discipline), and the R-4 proof (every reaching input was provably bareCode) is
carried. collect.ts COMPOSE-not-SUBSUME is explicit: no RNG line removed, the
`az2 .degradeBy(perlin.range(0,1))` event-neutrality OBSERVED by a test (D-1
action 3) — Chesterton honored.

**C. Hetvabhasa resistance — PASS.** P70 is named THE governing pre-mortem and
is the spine: every classification is an EXECUTED observation recorded VERBATIM
in OBSERVATIONS, never inferred; every gate falsifying a premise → PK18 STOP /
re-pose / backlog, never push-through / 2nd workaround / bar-lower (baked into
every wave's pre_mortem + verify + the EXECUTOR NOTES). P68 anchor is a
minification-stable string literal (explicitly not a comment/param). P67
tri-state / opaque-fence byte-identity preserved by the additive union.

**D. Observation testability — PASS.** The az2 chain-arg question is settled by
an EXECUTED Wave-0 prototype whose (a)/(b) verdict GATES the taxonomy (PLAN:200-
204) — not an inferred assumption the taxonomy is built on. The 3 crit-1
fixtures are proven by EXECUTED parseStrudel in V-1 + locked as 3 permanent
VERBATIM-distilled CI fixtures in V-2 (the do-not-paraphrase 20-15 lesson).

**E. Ownership clarity — PASS.** A-1 owns the IR-tag interface; B-1/C-1 the
recogniser implementation; D-1 the consumer-ripple closure; E-1 the
flagged-general path + the never-gate-counted invariant. The flagged-general
opts param's owner + stack threading path are explicit (Wave E, not module
state). loc/offset ownership is definition-site per PV49 (stated per-tag:
Signal=root loc only; Builder=root loc + args raw; body recurses with computed
inner offset).

**F. UX precedent / Grounding — PASS.** C-1 FORCES `@strudel/tonal chord` /
`arrange` (OPAQUE BOUNDARIES 1&2, gate-critical for #3/#7) to be source-read
(find + read the real .mjs, seed `GROUND_TRUTH_SIGNAL_MJS.md`) OR marked OPAQUE
+ args-RAW-only — modelling `body` ONLY from a grounded file:line, never
inferred. The Grounding Check is the load-bearing order of Wave C. No taxonomy
is modelled against inferred semantics anywhere (P69 named in C-1 read_first).

---

## MINOR refinements (fold before execution; none block)

**MINOR-1 — Dimension A (Vyapti / PV53 + PV52). The Wave-D FLOOR omits
`parseStrudel.ts`'s OWN internal bare-Code fences.** PV52 enumerates the live
fence-bail consumer set as 8 files and the FIRST is `parseStrudel.ts` itself
(the `rhsIR`/`rootIR`/`inner` bare-Code checks at ~479/896/1192/551 — and PV53
gives 5 fence sites at pS:571/574/653/998/1330). The Wave-D R-3 FLOOR
(PLAN:420) lists 11 *downstream* consumers but NOT `parseStrudel.ts`. The live
grep command (`grep -rn "\.tag ===\|case '\|\.via\b" packages/editor/src …`)
WOULD return `parseStrudel.ts` hits, so the "live grep is source of truth"
instruction structurally catches it — BUT the named FLOOR is what an executor
eye-skips against (the exact 20-17 MusicalTimeline:298 failure mode this very
plan cites). Additionally: `Signal`/`Builder` are NEW TAGS, not a new `via`
arm, so a `\.via\b` grep alone will NOT surface every site that must learn the
new tags (e.g. a deep-walker that switches on `tag` but has no `.via`).
**Fix:** in D-1 action 1, (a) ADD `parseStrudel.ts` to the named FLOOR with the
explicit note "its own post-parse bare-Code fences must NOT bail on a
`Signal`/`Builder` root reaching them as a chain inner/RHS — verify a
`Signal`/`Builder` at each of the PV53 fence sites (~571/574/653/998/1330) is
NOT misclassified as bare", and (b) widen the grep to also include a bare
`tag ===` / `case '` deep-walker sweep distinct from the `.via\b` sweep so the
new-TAG (not new-via-arm) consumers are enumerated. This converts the implicit
structural catch into an explicit named-FLOOR obligation — the precise
hardening 20-17 needed.

**MINOR-2 — Dimension D (Observation testability) / B (Krama). The Wave-0
prototype (b)-branch sizing is asserted "sized HERE with provenance" but the
plan does not state the STOP boundary if the (b) sub-arm itself proves
unbounded.** PLAN:204/315/329 say if verdict (b), the signal-ARG sub-arm is
"Wave-0-sized with provenance" and "any new class found here is a PK18 STOP".
That is correct, but the (b) branch could legitimately need a recursive
signal-expression recogniser (signal-valued args of signal-valued args — the
az2 chain is `sometimesBy(perlin.range(...), sub(8))` nested). The plan should
state, in Wave 0 action 5, the explicit STOP test: "if the (b) sub-arm requires
recognising signal expressions as ARGS to arbitrary depth (a recursive
signal-arg parser, not a bounded one-level arg recognition), that is a
NEW-CLASS PK18 STOP → re-pose → 20-19, NOT a Wave-0-sized in-scope item." Names
the unbounded-recursion boundary so the executor cannot rationalize an
ever-growing (b) sub-arm as "Wave-0 provenance covered it." (P70/4×-cascade
hardening — the plan has the discipline but not this specific boundary line.)

**MINOR-3 — Dimension 4 (Verification) / C (Hetvabhasa). V-1 crit-1 success
depends on the Wave-0 fixpoint premise still holding at V-1 time, but no wave
between 0 and V re-confirms `pending=[az2-only]`.** Wave 0 action 3 confirms
`resolved=[rp1,beat,chords2,bass,harm2] pending=[az2-only]` on the fresh branch
(good). But Waves B/C/D/E modify `parseStrudel.ts` + 11 consumers; a regression
that re-introduces a `beat`/`bass`/`harm2` blocker would only surface at V-1 as
a confusing "crit-1 fails but az2 IS modelled" — mis-diagnosable as an az2
problem. The plan already has each of B/C/D capture `pnpm test:proto` re-runs
(PLAN:337, 387, 446) — **Fix:** make ONE of those captures an explicit ASSERT
(not just "captured"): in D-1 verify, add "the `post-fixpoint resolved=[…]
pending=[…]` line still shows `rp1,beat,chords2,bass,harm2` resolved (only
`az2`'s disposition may change vs Wave 0) — any OTHER descriptor regressing to
pending is a PK18 STOP, not a V-1 surprise." Turns the existing passive capture
into the early-warning assert that isolates a consumer-wave regression from the
crit-1 az2 question.

---

## Conclusion

PASS. The plan is goal-backward-anchored on the AMENDED D-03, uses the
whole-program-fixpoint observation correctly, front-loads the highest-risk
uncertainty into an executed Wave-0 prototype, makes the PV53 consumer audit its
own primary-risk wave with the producer-precedence blind-spot explicitly
re-checked, grounds the OPAQUE chord/arrange boundary before modelling, and
routes every falsified premise through PK18 to backlog. All structural anchors
verified against live source. The 3 MINOR items are hardening refinements
(name the FLOOR gap explicitly, name the (b)-recursion STOP boundary, promote
the inter-wave fixpoint capture to an assert) — fold them, but they do not
block execution.
