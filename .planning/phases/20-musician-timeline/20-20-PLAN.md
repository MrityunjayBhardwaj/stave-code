---
phase: 20-20
title: tokenizer-whitespace fence — splitRootAndChain PV49-extension (close #159)
created: 2026-05-21T09:00:00Z
context: .planning/phases/20-musician-timeline/20-20-CONTEXT.md (D-01..D-04 LOCKED — do NOT relitigate)
research_seed: .planning/phases/20-musician-timeline/20-20-RESEARCH.md (R-1 Wave-0 probe RUN + factoring outcome / R-2 upstream TOLERATES verdict grounded at acorn.parse + Function-eval / R-3 LAST-WINS for #153 backlog / R-4 bug surface at parseStrudel.ts:2521-2531 / R-5 PV49 carries by construction / R-6 fixture spec / R-7 dual gate measurement / R-8 5 pre-mortems)
depends_on: 20-19 MERGED (PR #161, merge ≥ `a150889`); main HEAD ≥ `a150889` — must contain `a150889` in ancestry (planning-doc-only commits since the 20-19 merge are acceptable; ANY production-src commit on main since `a150889` triggers a `pS:` anchor re-grep per Wave 0 action 2). Baseline editor 1627/1627, app 409/409 (parity-corpus 47 + loc-fidelity 47), Bakery 96.0% N=50 sha f73b3956 stamp `2026-05-20T13-22-13-320Z`.
upstream_pin_sha: f73b395648645aabe699f91ba0989f35a6fd8a3c
runtime_version: "@strudel/core@1.2.6 / @strudel/transpiler@1.2.6 / @strudel/tonal@1.2.6 / @strudel/webaudio@1.3.0 / superdough@1.3.0 (1.2.6 line numbers corroborated 20-18/20-19; SHA delta noted in provenance block)"
baseline_parity: "96.0% (48/50, N=50, 2026-05-20T13-22-13-320Z, sha f73b3956) — the must-not-regress floor"
target_parity: "≥98.0% (49/50; closing #159 alone moves residual from 2 → 1; dual gate with must-not-regress 96.0% floor)"
gate: dual (`-G2drHRNFueu` STRUCTURED in production via Wave-B permanent CI fixture asserting `body.tag !== 'Code'` AND fresh `pnpm parity:bakery --n 50` ≥ 49/50 AND must-not-regress 96.0% floor)
prior_baseline_on_main: "editor 1627/1627, app 409/409 (parity-corpus 47 + loc-fidelity 47), main HEAD a150889 (20-19 merged PR #161 2026-05-20T23:06Z)"
issues: ["#159"]
related_backlog: ["#156", "#158", "#159", "#149", "#147", "#153"]
waves: 5 (0 factoring + re-confirm Outcome 1 / A PV49-extend splitRootAndChain / B permanent CI fixture / V-1 dual gate / V-3 cross-wave loc-fidelity STOP / V-4 phase close)
branch: feat/20-20-tokenizer-whitespace
---

# Phase 20-20 — `splitRootAndChain` PV49-extension on identifier-then-whitespace-then-paren

## GOAL (goal-backward anchor — anchored on the LOCKED D-04)

`splitRootAndChain`'s identifier-then-paren branch tolerates inter-element
whitespace (and `// line-comments`) between the identifier and the open
paren by calling the existing PV49 primitive `skipWhitespaceAndLineComments`
between the identifier scan and the `(` check, then including the
consumed whitespace inside the returned `root` slice so `parseRoot`'s
already-tolerant `\s*\(` regex arms (sMatch / noteMatch / miniMatch /
looseMatch) match downstream. No other code moves. The change is a
strict widen of the recogniser surface — it MIRRORS the upstream's
pure-JS-eval whitespace tolerance grounded by RESEARCH §3 at
`@strudel/transpiler/transpiler.mjs:25-30` (`acorn.parse` with
`ecmaVersion: 2022`) + `@strudel/core/evaluate.mjs:29-39`
(`Function(body)()`). The fix flips bakery row `-G2drHRNFueu` from
the bareCode fallback to STRUCTURED via the inherited `sound("...")`
recogniser arms.

**The phase PASSES iff the LOCKED D-04 dual gate holds on the SAME
merge commit verified on real main `dist/index.js`:**

1. **HARD GATE crit-1:** A Wave-B-style permanent CI fixture
   (`bakery-159-tokenizer-whitespace.strudel`) asserts whole-program
   STRUCTURED — specifically the outer `Track` wrapper has
   `body.tag !== 'Code'` (the parity oracle's fence). Optionally a
   companion grounding spec asserts the inner shape is `Play`.
2. **HARD GATE crit-2:** Fresh `pnpm parity:bakery --n 50` (PK17
   step-6 cadence; NEW ISO stamp; SAME upstream pin SHA
   `f73b3956`) reports **≥ 49/50 (98.0%)** AND **must-not-regress**
   the **48/50 (96.0%)** baseline floor. Closing `-G2drHRNFueu`
   alone is the arithmetic +1 (residual `{#143-#7LU6zgzViSM,
   #159-#G2drHRNFueu}` → `{#143-#7LU6zgzViSM}`).
3. **No-bar-lowering / no-second-workaround / no-push-through.** If
   crit-2 falsifies the floor, OR crit-1 falsifies altogether, OR any
   wave's per-file loc-fidelity STOP gate fires, the phase does NOT
   pass. The invariant LOCKED decision is re-posed to the user with
   EVIDENCE; the SCOPE may be amended on evidence (the 20-18
   AMENDMENT-2 precedent), but never the FLOOR.

Every wave below chains to this GOAL. Wave 0 RE-CONFIRMS Outcome 1
on the fresh branch by re-running the §2.2 5-cell factoring probe;
Wave A lands the PV49-extension inside `splitRootAndChain`; Wave B
vendors the permanent CI fixture (V-2 cadence); Wave V is the
dual-gate measurement + cross-wave loc-fidelity STOP + phase close
(V-1 / V-3 / V-4).

> **PV54 FLOOR-grep obligation — EXPLICITLY NOT TRIGGERED THIS
> PHASE.** No new top-level PatternIR `tag` ships in 20-20. The fix
> is purely walker-tolerance inside an existing parser function; no
> new `kind` constant, no new arm in any tag-discriminating switch,
> no consumer surface change. The PV53/PV54 consumer-audit obligation
> is dormant. Confirm explicitly in V-4 catalogue addendum.

## CONTEXT — what 20-19 established + what RESEARCH proved (Chesterton's existence check)

20-19 (PR #161, merge `a150889`) shipped `stripSideEffectStatements`
(curated 10-token side-effect filter inside `buildBindingMap`'s
preamble) plus 11 permanent CI fixtures + the locked-STOP flip at
`_waveC-grounding.spec.ts:155`. Crit-2 hit **96.0% N=50** (+4pp over
the 92% floor); +1 BONUS flip on `-1j62z5xjyCN`. Two residuals
remained: `-7LU6zgzViSM` (#143 guarded-boot — pre-existing baseline
class, OUT of 20-20 scope) and `-G2drHRNFueu` (the 20-20 target —
`sound ("hh hh hh hh")` plus two siblings).

### What RESEARCH proved (§§2-5 verbatim — do NOT re-derive)

- **§2.2 (Wave-0 5-cell factoring probe — RUN by researcher 2026-05-21):**
  case A (`sound("hh hh hh hh")`) inner.tag=Seq STRUCTURED; case B
  (`sound ("hh hh hh hh")`) inner.tag=Code bareCode (the bug); case C
  (`sound("a")\nsound("b")`) inner.tag=Play STRUCTURED (FIRST-WINS on
  our parser today; LAST-WINS on upstream — semantic mismatch but
  NOT a parity blocker); case D (`sound ("a")\nsound ("b")`) bareCode
  via the whitespace fence; case E (verbatim `-G2drHRNFueu`)
  bareCode via the whitespace fence. **Verdict: Outcome 1 — #159 IS
  the sole dominant blocker; #153 stays backlog with the LAST-WINS
  upstream Ground Truth recorded in RESEARCH §4 for any future phase
  that picks it up.**
- **§3 (Upstream tokenizer — D-02 GROUNDED):** Upstream UNAMBIGUOUSLY
  TOLERATES whitespace between identifier and `(`. Mechanism class:
  PURE JS-EVAL PASS-THROUGH (`@strudel/transpiler/transpiler.mjs:25-30`
  `acorn.parse(input, {ecmaVersion: 2022, ...})` + `evaluate.mjs:29-39`
  `Function(body)()`). Acorn parses `sound ("hh")` as a
  `CallExpression` identically to `sound("hh")` per ECMA-262 grammar
  (`CallExpression :: MemberExpression Arguments` allows arbitrary
  whitespace). The transpiler AST walk (lines 47-181) does NOT
  normalise whitespace. Our parser is STRICTER than upstream — the
  fix mirrors upstream's permissiveness.
- **§5 (bug surface — Chesterton-scanned):** The bug is at
  `packages/editor/src/ir/parseStrudel.ts:2521-2531` (the
  `} else {` identifier-then-paren branch of `splitRootAndChain`).
  After the identifier scan stops at the first non-id char, the code
  checks `expr[i] === '('` DIRECTLY. When `expr` is `sound ("hh")`,
  `expr[i]` is a space; the check fails; the splitter returns
  `root='sound'`, `chain=' ("hh")'`; downstream `parseRoot` bareCode's
  the bare ident and the whole expression flattens to `IR.code(expr)`.
  Fix shape: insert `skipWhitespaceAndLineComments` between identifier
  scan and `(` check; INCLUDE the consumed whitespace inside the
  returned root slice; restore `i` to the identifier boundary if no
  `(` follows (the no-paren branch — keep today's behaviour for bare
  identifier roots).

### Live precedent shapes on real main `a150889` (re-grep before editing — line numbers may drift)

| Mechanism | Live line (re-grep at Wave 0) | Shape |
|---|---|---|
| `splitRootAndChain` function head | **`packages/editor/src/ir/parseStrudel.ts:2489`** | `export function splitRootAndChain(expr: string): { root: string; chain: string }` |
| Identifier-then-paren branch | **`packages/editor/src/ir/parseStrudel.ts:2521-2531`** (the `} else {` arm) | hand-rolls identifier scan + direct `expr[i] === '('` check — the surgery site |
| `skipWhitespaceAndLineComments` export | **`packages/editor/src/ir/parseStrudel.ts:1075`** (per Wave-0 verification — re-grep) | the PV49 primitive; consumes whitespace + `//` line comments; returns the new position |
| `findMatchingParen` | **`packages/editor/src/ir/parseStrudel.ts` (re-grep at Wave 0)** | already used on the next line below the surgery site; NO change |
| `parseRoot.sMatch` arm | **`packages/editor/src/ir/parseStrudel.ts:1347` (per RESEARCH §1)** | `/^(?:s|sound)\s*\(\s*"([^"]*)"\s*\)/` — already tolerates `\s*(` (the receiving end of the fix) |
| Existing PV49 callers (NOT touched by 20-20) | pS:463, 1075, 1560, 1714, 2676 | the precedent for inter-element whitespace tolerance; the new caller is at the identifier-to-paren boundary |

**Every `pS:` line above is a 2026-05-21 snapshot on real main `a150889`. Every task's first action RE-GREPs the live anchor before editing.**

### The matcher line (D-02 — PRESERVE exactly)

The fix is purely permissive — it widens the recogniser surface to
match upstream's whitespace-tolerance, NOTHING ELSE. It does NOT
change the source string; the original `code` flows through unchanged.
It does NOT add new state; the PV49 primitive is a pure function. It
does NOT introduce a new IR tag; PV54 stays dormant. The matcher line
holds: structure modelled, side-effect never evaluated by the parser,
loc-fidelity carries by byte-additive whitespace consumption.

### Composition with 20-18 / 20-19 (Chesterton — COMPOSE, do NOT SUBSUME)

- 20-18's `CHAIN_ROOT_RECOGNISER` curated Map (Signal/Builder roots)
  is NOT touched. Its scan is downstream of `splitRootAndChain` (it
  runs INSIDE `parseRoot`).
- 20-19's `stripSideEffectStatements` filter operates on the
  `splitTopLevelStatements` output BEFORE `buildBindingMap`. The
  20-20 fix operates INSIDE `splitRootAndChain` which runs LATER in
  the pipeline (after bindings are resolved). The two fixes are
  fully decoupled.
- The PV49 substrate (`skipWhitespaceAndLineComments` at pS:1075) is
  the SHARED primitive. The 20-20 fix is a 5th caller; the 4 existing
  callers (pS:463, 1560, 1714, 2676) are NOT touched.

## PRE-MORTEM (the most-likely wrong-but-green outcome — P70 is the spine)

**The dominant risk is P70 occurrence 9** — a Wave-0 V-1 observation
on the fresh branch falsifies the §2.2 5-cell matrix. Most-likely
cause: a planning-doc-only commit on main between RESEARCH and
PLAN-execution shifted some unrelated test or fixture, or a hotfix
landed on main that altered the parser's whitespace handling. The
trap: trusting the RESEARCH §2.2 stdout instead of re-running the
probe on the fresh branch. **PLANNING DIRECTIVE (not a gray area):**
Wave 0 is RUN, not inferred; the 5-cell probe's stdout is captured
verbatim into OBSERVATIONS as the empirical premise of every later
wave. When a gate falsifies a premise → PK18 cascade discipline:
STOP, record verbatim in `20-20-OBSERVATIONS.md`, re-classify,
re-pose the invalidated LOCKED decision to the user, NEVER push
through / NEVER second-workaround / NEVER bar-lower.

**Second most-likely (RESEARCH §R-8 PM-4):** the fix produces
false-positives in the full corpus (over-widening). The §5.6
Chesterton scan analytically rules this out (the `afterIdent`
restore arm preserves today's behaviour when no `(` follows), but
ANALYTICAL DOES NOT REPLACE OBSERVATIONAL — the V-3 cross-wave
per-file loc-fidelity STOP gate is the observational guard. Wave-A
also runs an explicit false-positive probe (`let allBindings = "x"`,
`let x = sine // comment`) before commit.

**Third (RESEARCH §R-8 PM-5):** the Wave-0 case B structures post-fix
but case E (the real exemplar) does NOT — the difference being the
trailing blank lines + `// @version 1.0` comment. The Wave-A probe
includes case E verbatim (action 6 below); if E does NOT flip
post-fix, STOP. Most-likely cause: an interaction with
`stripParserPrelude` or `splitTopLevelStatements` specific to the
full row's shape — re-classify and re-pose.

## WAVE 0 — Re-confirm Outcome 1 on the fresh branch; FROZEN scope lock (P70 directive discharge)

<task id="0-1" type="auto" depends_on="">
  <name>Branch; baseline RE-OBSERVATION; RE-RUN the §2.2 5-cell factoring probe on real main `a150889`; lock scope to #159 only; pre-allocate V-3 allow-list</name>
  <read_first>
    .planning/phases/20-musician-timeline/20-20-CONTEXT.md (D-01..D-04 LOCKED — strict scope discipline);
    .planning/phases/20-musician-timeline/20-20-RESEARCH.md §§2 (Wave-0 probe RUN + decision rule) + §5 (bug surface + fix-shape) + §6 (PV49 / PV54 / PV50 / PV52 / PV53 obligations) + §R-8 (5 pre-mortems);
    .planning/phases/20-musician-timeline/20-19-PLAN.md Wave 0 (the cadence template — branch, baseline, classification probe, V-3 allow-list pre-allocation, commit) + 20-19-SUMMARY.md (cadence record);
    .planning/phases/20-musician-timeline/20-19-OBSERVATIONS.md (the 96.0% baseline + the 4 PK18 re-poses-resolved-within-plan pattern);
    packages/editor/src/ir/parseStrudel.ts lines 1075-1094 (the `skipWhitespaceAndLineComments` primitive — confirm signature) + 2485-2540 (the `splitRootAndChain` function + surgery site);
    .anvi/hetvabhasa.md P70 (the eight-occurrence pattern — RUN, never infer; this wave is the P70 directive in action) + P68 (build hygiene) + P69 (grounding — discharged by RESEARCH §3);
    .anvi/krama.md PK18 (HARD-GATE cascade discipline) + PK17 (friction-first cycle — fresh PK17-step-6 measurement);
    .anvi/vyapti.md PV49 (the load-bearing precedent — the fix IS a PV49-extension); PV52 (fence predicate UNCHANGED); PV54 (NOT triggered — confirm explicitly)
  </read_first>
  <files>
    (branch only — NO production src edits this wave)
    .planning/phases/20-musician-timeline/20-20-OBSERVATIONS.md (NEW — the verbatim Wave-0 5-cell probe record + baseline re-observation + FROZEN scope lock + expected V-3 allow-list)
  </files>
  <action>
    1. **Branch FIRST (never code on main):** `git checkout main && git pull && git rev-parse HEAD` (record verbatim) and `git merge-base HEAD a150889` — confirm the merge-base equals `a150889` (i.e. main contains the 20-19 merge in its ancestry). Planning-doc-only commits on main since `a150889` (e.g. the 20-20 CONTEXT + RESEARCH) are ACCEPTABLE. ANY production-src commit on main since `a150889` triggers the action-2 re-grep. `git checkout -b feat/20-20-tokenizer-whitespace`. Confirm `git rev-parse --abbrev-ref HEAD` == `feat/20-20-tokenizer-whitespace`.
    2. **Re-grep the live anchors** (line numbers may drift across phases). Record the LIVE lines of `export function splitRootAndChain` (currently `pS:2489`), the `} else {` identifier-then-paren branch (currently `pS:2521`), the `while (i < expr.length && /[a-zA-Z0-9_$]/.test(expr[i])) i++` identifier scan (currently `pS:2523`), the `if (i < expr.length && expr[i] === '(')` check (currently `pS:2526`), `export function skipWhitespaceAndLineComments` (currently `pS:1075`), and the `parseRoot.sMatch` regex line (currently `pS:1347` per RESEARCH §1). Write the verified table into `20-20-OBSERVATIONS.md` (the source of record for every later task's `read_first` line numbers).
    3. **Baseline observation (Lokayata, not inference).** Run all of: `pnpm --filter @stave/editor test` (capture 1627/1627), `pnpm --filter @stave/app test` (capture 409/409; parity-corpus 47 + loc-fidelity 47 + other 315). Capture VERBATIM stdout into OBSERVATIONS. If ANY count differs from baseline, that is a PK18 STOP — re-pose, do NOT proceed.
    4. **RE-RUN the §2.2 5-cell factoring probe** on the fresh branch. Write a vite-node spec `_wave0-classify-20-20.spec.ts` (under `packages/editor/src/`; mirror the 20-19 `_wave0-classify.spec.ts` shape — vitest run, stdout-printed, no assertions). The exact spec is RESEARCH §2.1; the 5 cases are:
       - **A control** `sound("hh hh hh hh")` — EXPECTED inner.tag=Seq, NOT bareCode
       - **B whitespace fence** `sound ("hh hh hh hh")` — EXPECTED inner.tag=Code, bareCode (the bug pre-fix)
       - **C multi-top-level (no space)** `sound("a")\nsound("b")` — EXPECTED inner.tag=Play, NOT bareCode (FIRST-WINS on our parser today; LAST-WINS on upstream — semantic mismatch flagged in §4 but NOT 20-20 scope)
       - **D exemplar shape (with space + two siblings)** `sound ("a")\nsound ("b")` — EXPECTED inner.tag=Code, bareCode pre-fix
       - **E REAL bakery `-G2drHRNFueu` verbatim** — EXPECTED inner.tag=Code, bareCode pre-fix. Read the verbatim source from `packages/app/tests/parity-corpus/.bakery-runs/samples-2026-05-20T13-22-13-320Z.json` via `node -e "const j=JSON.parse(require('fs').readFileSync('packages/app/tests/parity-corpus/.bakery-runs/samples-2026-05-20T13-22-13-320Z.json','utf8'));console.log(JSON.stringify(j.samples.find(s=>s.hash==='-G2drHRNFueu').code))"` — copy the exact text into the spec.
       Run: `pnpm --filter @stave/editor exec vitest run src/_wave0-classify-20-20.spec.ts`. Capture VERBATIM stdout (including the per-case `outer.tag=… inner.tag=… inner.bare=…` lines) into OBSERVATIONS under heading "Wave 0 — 5-cell factoring probe RE-CONFIRMATION on `feat/20-20-tokenizer-whitespace` from `a150889`".
    5. **Lock scope per the §2.4 decision rule.** Confirm the Wave-0 V-1 stdout matches the §2.2 matrix exactly: A/C STRUCTURED (inner.bare=false); B/D/E bareCode (inner.bare=true). If it matches → D-01 LOCKS Outcome 1 → scope locks to #159 only; #153 stays backlog with the RESEARCH §4 LAST-WINS upstream verdict recorded in `20-20-OBSERVATIONS.md`'s "deferred backlog" section (for any future maintainer who picks up #153). If the stdout DIVERGES from the matrix → PK18 HARD STOP; record verbatim; re-pose D-01 to user with the diverging stdout. DO NOT proceed to fix selection.
    6. **The `_wave0-classify-20-20.spec.ts` IS gitignored** — it is a one-off Wave-0 instrument, NOT a permanent regression spec (the permanent regression coverage lands in Wave B's `bakery-159-*.strudel` fixture). Verify `.gitignore` already excludes `_wave0-*.spec.ts` (mirror the 20-19 pattern) OR delete the file after capturing stdout. Either is acceptable; the OBSERVATIONS record is the source of truth.
    7. **Pre-allocate the V-3 allow-list.** Record in OBSERVATIONS the expected V-3 allow-list:
       - certainly the new Wave-B fixtures: `bakery-159-tokenizer-whitespace.strudel` + optionally `bakery-159-NEGATIVE-no-whitespace.strudel`;
       - the corpus file (if any) corresponding to `-G2drHRNFueu` — confirmed by `ls packages/app/tests/parity-corpus/bakery-*.strudel | grep -i 159` — almost certainly NONE today (the hash lives in `.bakery-runs/samples-*.json`, not in `*.strudel`); if any pre-existing `bakery-159-*` or `-G2drHRNFueu`-related file IS in the corpus, allow-list it explicitly.
       Expected outcome: the allow-list contains AT MOST the 1-2 Wave-B fixtures. The V-3 STOP gate in Wave V-3: parity-UNCHANGED corpus files have empty loc-fidelity diffs; the parity-CHANGED set ⊆ this allow-list; ANY other moved file = silent drift = STOP.
    8. **Commit via COMMIT_TEMPLATE** (see EXECUTOR NOTES — structural single-quoted heredoc), gitmoji `:mag:`, `git add -f .planning/phases/20-musician-timeline/20-20-OBSERVATIONS.md`. Problem: "the Wave-0 5-cell factoring outcome + #159 scope lock + expected V-3 allow-list are unobserved on the fresh branch (P70 trap if inferred from RESEARCH §2.2 alone)"; Fix: "re-ran editor/app baselines + the §2.2 5-cell factoring probe on real main `a150889` from a fresh branch; locked the verbatim 5-case matrix + Outcome 1 + the deferred-backlog LAST-WINS note for #153 + the expected V-3 allow-list as the regression record".
  </action>
  <ownership>
    This wave owns the EMPIRICAL Wave-0 5-cell probe record (the empirical premise every later wave depends on), the SCOPE lock to #159 only, the deferred-backlog LAST-WINS note for #153 (the gift to any future #153 phase), and the expected V-3 allow-list. `20-20-OBSERVATIONS.md` is created + owned here; every later wave appends, never rewrites, its verbatim findings. The branch is created here. NO production code is owned/changed this wave.
  </ownership>
  <lifecycle>
    PK17 step 1-2 (measure → classify) + PK18 step 8 (vendor the gate oracle early). Runs FIRST: no fix / fixture / gate work may begin until Outcome 1 is re-observed on the fresh branch and the scope is FROZEN.
  </lifecycle>
  <pre_mortem>
    Most likely error (RESEARCH §R-8 PM-1): the Wave-0 V-1 stdout DIVERGES from RESEARCH §2.2 — case B structures unexpectedly (a hotfix on main between RESEARCH and PLAN somehow already shipped the fix), or case C bareCodes unexpectedly (a regression). Mitigation: action 4 captures stdout verbatim before any production-src edit; action 5 STOPS on divergence; NEVER push through.
    Second error: a missed line-number drift between RESEARCH-cited `pS:2521-2531` and the executor's working tree (a hotfix landed on main since RESEARCH ran at `a150889`) — the surgery site is at the wrong line. Mitigation: action 2's re-grep is non-optional; action 8's commit body cites the live line numbers as recorded.
    Third error: a Wave-0 probe spec mistake (typos / wrong import path / wrong deep-path resolution) → stdout looks like divergence but is actually a probe bug. Mitigation: the spec mirrors the 20-19 `_wave0-classify.spec.ts` exactly per RESEARCH §2.1; if stdout looks malformed, RE-RUN the probe with explicit prints around the import + `parseStrudel` resolution; only declare divergence after the probe itself is verified.
  </pre_mortem>
  <verify>
    1. `git rev-parse --abbrev-ref HEAD` == `feat/20-20-tokenizer-whitespace` and `git merge-base feat/20-20-tokenizer-whitespace main` contains `a150889`.
    2. `pnpm --filter @stave/editor test` == 1627/1627; `pnpm --filter @stave/app test` == 409/409. Counts captured VERBATIM in OBSERVATIONS.
    3. The Wave-0 5-cell probe stdout matches RESEARCH §2.2 exactly: A/C STRUCTURED (inner.bare=false); B/D/E bareCode (inner.bare=true). Captured VERBATIM under "Wave 0 — 5-cell factoring probe RE-CONFIRMATION".
    4. Scope LOCKED to #159 only; #153 stays backlog with the RESEARCH §4 LAST-WINS verdict recorded in OBSERVATIONS "deferred backlog" section.
    5. V-3 allow-list pre-allocated (likely 1-2 Wave-B fixtures only); recorded in OBSERVATIONS.
    6. NO production src changed (`git diff --name-only origin/main` shows only `.planning/phases/20-musician-timeline/20-20-OBSERVATIONS.md` and, if not gitignored, `packages/editor/src/_wave0-classify-20-20.spec.ts` — preferred is gitignored).
    7. Commit landed via COMMIT_TEMPLATE.
  </verify>
  <done>
    Branch `feat/20-20-tokenizer-whitespace` from `a150889`; editor 1627 / app 409 baselines RE-OBSERVED; Wave-0 5-cell probe matches RESEARCH §2.2 verbatim; Outcome 1 RE-CONFIRMED; scope LOCKED to #159 only; deferred-backlog LAST-WINS note for #153 recorded; expected V-3 allow-list pre-allocated; commit via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE A — PV49-extend `splitRootAndChain` with whitespace-then-paren tolerance

<task id="A-1" type="auto" depends_on="0-1">
  <name>Insert `skipWhitespaceAndLineComments` call between identifier scan and `(` check inside `splitRootAndChain`'s identifier-then-paren branch; restore `i` to identifier boundary when no `(` follows; prove cases B/D/E flip to STRUCTURED via Wave-A probe</name>
  <read_first>
    20-20-OBSERVATIONS.md (Wave-0 verified live line numbers; Outcome 1 scope lock);
    packages/editor/src/ir/parseStrudel.ts (the Wave-0 verified anchors at `splitRootAndChain` pS:2489-2538, `skipWhitespaceAndLineComments` pS:1075, `findMatchingParen`, `parseRoot.sMatch` pS:1347);
    .planning/phases/20-musician-timeline/20-20-RESEARCH.md §5 (the exact fix-shape sketch + the §5.5 critical invariant: include the consumed whitespace inside the returned `root` slice) + §5.6 (the Chesterton risk scan for the 5 non-corpus shapes) + §6.1 (PV49 addendum template);
    .anvi/vyapti.md PV49 (definition-site loc-additivity — the substrate the fix extends) + PV50 (no module state — the helper is pure; module-const data is NOT state) + PV52 (fence predicate UNCHANGED);
    .anvi/krama.md PK16 (parser pipeline — the new caller inserts INSIDE `splitRootAndChain` which runs INSIDE `parseExpression` after binding resolution; PK16 stage numbering unchanged);
    ~/.claude/projects/-Users-mrityunjaybhardwaj-Documents-projects-struCode/memory/feedback_editor_watch_mode.md (P68 — start watch BEFORE editing, but verify watch is alive)
  </read_first>
  <files>
    packages/editor/src/ir/parseStrudel.ts (ADD: the PV49-extension inside `splitRootAndChain`'s `} else {` identifier-then-paren branch at the Wave-0 verified `pS:2521-2531`; touch NOTHING else)
  </files>
  <action>
    1. **RE-GREP the live anchors** (mandatory per Wave-0 OBSERVATIONS). Confirm the `} else {` arm + the identifier scan + the `(` check at the Wave-0 verified line numbers. Re-confirm `skipWhitespaceAndLineComments`'s signature at pS:1075: `export function skipWhitespaceAndLineComments(src: string, pos: number): number` (returns the new position; pure function; PV49 substrate). If anything diverges from Wave-0 OBSERVATIONS, update OBSERVATIONS and proceed against LIVE source.
    2. **START the editor watch (P68 + memory `feedback_editor_watch_mode.md`):** `pnpm --filter @stave/editor dev` in a background terminal BEFORE editing. Verify the watch is alive — a tsup-DTS-build failure terminates the watch entirely (does NOT degrade to JS-only). The known `@strudel/mondo` TS7016 warning is benign; any new error blocks editing.
    3. **Apply the surgical edit at `splitRootAndChain` `pS:2521-2531`.** Today's code (verbatim from `parseStrudel.ts`):
       ```ts
       } else {
         // Skip identifier
         while (i < expr.length && /[a-zA-Z0-9_$]/.test(expr[i])) i++

         // If there's an opening paren, find the matching close
         if (i < expr.length && expr[i] === '(') {
           const closeIdx = findMatchingParen(expr, i)
           if (closeIdx !== -1) {
             i = closeIdx + 1
           }
         }
       }
       ```
       Becomes:
       ```ts
       } else {
         // Skip identifier
         while (i < expr.length && /[a-zA-Z0-9_$]/.test(expr[i])) i++

         // 20-20 (#159) — PV49 extension: tolerate inter-element whitespace
         // + `// line-comments` between the identifier and the call-site
         // `(`. Mirrors upstream Strudel's pure-JS-eval whitespace
         // tolerance (acorn.parse at @strudel/transpiler/transpiler.mjs:25-30
         // + Function(body)() at @strudel/core/evaluate.mjs:29-39, Codeberg
         // pin f73b3956). The consumed whitespace MUST be included in the
         // returned `root` slice so parseRoot's existing `\s*\(` regex arms
         // (sMatch, noteMatch, miniMatch, looseMatch) match downstream.
         // When no `(` follows the whitespace, restore `i` to the identifier
         // boundary so bare-identifier roots (e.g. `let x = sine`) behave
         // exactly as before (today's no-call disposition).
         const afterIdent = i
         i = skipWhitespaceAndLineComments(expr, i)

         // If there's an opening paren, find the matching close
         if (i < expr.length && expr[i] === '(') {
           const closeIdx = findMatchingParen(expr, i)
           if (closeIdx !== -1) {
             i = closeIdx + 1
           }
         } else {
           // No `(` after whitespace — restore i to the identifier boundary.
           // Preserves today's behaviour for bare-identifier roots.
           i = afterIdent
         }
       }
       ```
       EVERY OTHER LINE in `splitRootAndChain` is BYTE-UNCHANGED (the bare-string `"`-quoted arm at pS:2492-2503, the backtick arm at pS:2504-2520, the final `return { root: expr.slice(0, i), chain: expr.slice(i) }` at pS:2534-2537 — all preserved).
    4. **Verify additive-only on the surgery site.** `git diff packages/editor/src/ir/parseStrudel.ts` MUST show ONLY: (a) the new comment block (additive), (b) the `const afterIdent = i` + `i = skipWhitespaceAndLineComments(expr, i)` lines (additive), (c) the new `else { i = afterIdent }` branch (additive). ZERO edits to: the bare-string `"`-quoted arm, the backtick arm, the identifier-scan `while`, the `if (i < expr.length && expr[i] === '(')` condition itself, the `findMatchingParen` call, the final `return` statement, or any other function in `parseStrudel.ts`. State this in the commit body.
    5. **P68 build hygiene with MINIFICATION-STABLE anchors.** One-shot `pnpm --filter @stave/editor build`. Anchors MUST be STRING/SYMBOL LITERALS unique to or load-bearing on this wave. Grep:
       ```
       grep -c "splitRootAndChain" packages/editor/dist/index.js
       grep -c "skipWhitespaceAndLineComments" packages/editor/dist/index.js
       grep -c "findMatchingParen" packages/editor/dist/index.js
       grep -c "afterIdent" packages/editor/dist/index.js
       ```
       Per-anchor expectations:
       - `splitRootAndChain` — > 0 (the exported function name; tsup `keepNames` preserves);
       - `skipWhitespaceAndLineComments` — count MUST INCREASE by 1 vs the pre-fix baseline (the new 5th caller). Pre-fix occurrences = N (the 4 existing callers + 1 definition + any internal self-reference); post-fix = N + 1. Capture the pre-fix N in OBSERVATIONS (run `grep -c` on the Wave-0 commit's built dist BEFORE editing; record both counts in OBSERVATIONS).
       - `findMatchingParen` — unchanged count (defensive: confirms no other call sites were perturbed);
       - `afterIdent` — this LOCAL variable name may or may not survive minification (minifiers can rename locals); if `grep -c "afterIdent"` is 0, that is acceptable IF the function-name + primitive-name greps both pass. NOT a STOP-gate; informational.
       The first three counts MUST satisfy the per-anchor rule; the 4th is informational. Capture all 4 counts in the commit body. The known `@strudel/mondo` TS7016 may fire on the DTS step — verify it is THAT error, not new.
    6. **Wave-A throwaway probe (executed, recorded VERBATIM — the strict-widen + false-positive + exemplar round-trip verification).** Write a vite-node spec `_waveA-whitespace-probe.spec.ts` (under `packages/editor/src/`; gitignored — mirror the Wave-0 spec disposition). The spec, via the production `parseStrudel` imported from the deep path `./ir/parseStrudel`, records (with stdout-print, no assertions — the OBSERVATIONS capture IS the gate):
       - **(i) strict-widen** (the FLIP signal):
         - `parseStrudel('sound ("hh")')` — POST-FIX EXPECTED inner.bare=false (was inner.bare=true pre-fix). Inner.tag should be `Play` (the inherited `sMatch` arm fires);
         - `parseStrudel('sound ("hh hh hh hh")')` — same flip;
         - `parseStrudel('s ("bd sd")')` — the `s` alias (per RESEARCH §1 — `sMatch = /^(?:s|sound)\s*\(\s*"…/`) — same flip;
         - `parseStrudel('note ("c d")')` — the `note` arm (the `noteMatch` regex per parseRoot) — same flip;
       - **(ii) false-positive guards** (NEVER false-flip — preserve today's behaviour):
         - `parseStrudel('let allBindings = "x"; sound("y")')` — `allBindings` binding is preserved (the `let` keyword starts the stmt; the new whitespace-tolerance is INSIDE `splitRootAndChain` which is downstream of binding resolution); whole-program structured;
         - `parseStrudel('let x = sine')` — bare identifier root `sine` (no call); post-fix `i = afterIdent` restore arm fires; the disposition is the SAME as pre-fix (whatever it was — `sine` is in `CHAIN_ROOT_RECOGNISER` per 20-18, so it should structure via the Signal-Builder path);
         - `parseStrudel('sine .range(0,1)')` — leading-whitespace-before-`.` chain dotting. The §5.6 PM-3 case. Post-fix the splitter sees `sine` then whitespace then `.range(0,1)`; after `skipWhitespaceAndLineComments`, `expr[i]` is `.`, NOT `(`; the `i = afterIdent` restore fires; root = `sine`, chain = ` .range(0,1)` — IDENTICAL to today. `applyChain` already tolerates leading whitespace via its inter-method consume at pS:1714. Disposition: unchanged.
         - `parseStrudel('let x = sine // comment\n.range(0,1)')` — the §5.6 PM-3 `//`-comment case. Post-fix the splitter sees `sine` then `// comment\n` then `.range(0,1)`; after `skipWhitespaceAndLineComments` consumes BOTH the whitespace AND the line comment, `expr[i]` is `.`; restore arm fires; root = `sine`, chain remains as ` // comment\n.range(0,1)`. Today the splitter truncates at the space → same disposition (the `//` is preserved in `chain`); the `applyChain` step consumes the comment via its existing PV49 caller at pS:1714. Disposition: unchanged or strictly better.
       - **(iii) exemplar round-trip** (the GATE-BEARING observation):
         - `parseStrudel(<verbatim -G2drHRNFueu source from samples-2026-05-20T13-22-13-320Z.json>)` — POST-FIX EXPECTED inner.bare=false. The PV49 walker consumes the whitespace; `sMatch` arm fires on the first stmt; `splitTopLevelStatements` returns TWO stmts (the two `sound (…)` siblings); current parser's FIRST-WINS multi-line semantic emits `body.tag === 'Play'` for the first `sound`. The §4 LAST-WINS upstream mismatch is OUT-OF-SCOPE; the parity oracle's fence (inner.bare === false) PASSES regardless.
       Run: `pnpm --filter @stave/editor exec vitest run src/_waveA-whitespace-probe.spec.ts`. Capture VERBATIM stdout into OBSERVATIONS under "Wave A — strict-widen + false-positive + exemplar round-trip verification". If ANY (i) case does NOT flip, or ANY (ii) case CHANGES disposition, or (iii) does NOT flip, that is a PK18 STOP (RESEARCH §R-8 PM-2 / PM-3 / PM-5) — re-pose D-02 fix-shape choice to user with EVIDENCE.
    7. **Run the test gates (must hold — no expected breaks this wave).**
       - `pnpm --filter @stave/editor test` → MUST hold at 1627/1627. The PV49-extension is purely-additive code; no editor test directly asserts on `splitRootAndChain`'s whitespace-fence today. Any movement is a regression.
       - `pnpm --filter @stave/app test` → MUST hold at 409/409. The Wave-B fixture is not yet vendored; the parity-corpus tests do not yet include `bakery-159-*`. Pre-existing fixtures unaffected by construction (the fix is strictly permissive per the §5.6 Chesterton scan).
       Record VERBATIM in OBSERVATIONS.
    8. **Per-file loc-fidelity STOP gate (per-wave).** Inside `pnpm --filter @stave/app test` (action 7), the loc-fidelity test asserts every IR `loc` slices into source byte-exact. PER-WAVE rule:
       - EVERY parity-UNCHANGED corpus file's loc-fidelity diff MUST be EMPTY;
       - the parity-CHANGED set ⊆ the expected V-3 allow-list pre-allocated in Wave 0 (today: NONE in Wave A — fixtures land in Wave B);
       - ANY moved file = silent drift = STOP (RESEARCH §R-8 PM-4 / PV49 violation).
       The PV49 substrate operates by BYTE-ADDITIVE whitespace consumption (the consumed whitespace is included in the returned root slice; the original source string is never mutated; remaining offsets unchanged). Loc-fidelity carries by construction; verify per-wave. Record the actual moved set verbatim in OBSERVATIONS (expected: EMPTY).
    9. **Commit via COMMIT_TEMPLATE**, gitmoji `:sparkles:`. `git add -f packages/editor/src/ir/parseStrudel.ts .planning/phases/20-musician-timeline/20-20-OBSERVATIONS.md`. Problem: "splitRootAndChain rejects whitespace between identifier and call-site `(` — `expr[i] === '('` direct check at `pS:2526` fails on `sound ("hh")`; the splitter returns root='sound', chain=' (\"hh\")'; downstream parseRoot bareCode's the bare ident; `-G2drHRNFueu` falls back to bareCode (Wave-0 RE-CONFIRMED)"; Fix: "PV49-extend the identifier-then-paren branch: call `skipWhitespaceAndLineComments(expr, i)` between identifier scan and `(` check; include the consumed whitespace inside the returned root slice so parseRoot's existing `\\s*\\(` regex arms match; restore `i` to identifier boundary when no `(` follows (preserves bare-identifier root disposition); strict-widen + false-positive + exemplar round-trip probe recorded verbatim; editor 1627/1627; app 409/409; per-file loc-fidelity STOP gate clean".
  </action>
  <ownership>
    This wave owns the IMPLEMENTATION (the new `afterIdent` local + the `skipWhitespaceAndLineComments` call + the `else { i = afterIdent }` restore arm). PV49 itself (the primitive at pS:1075) is owned by its prior phase (the 20-1x cadence; the 4 existing callers are precedent). PV49 loc-additivity is owned by the byte-additive whitespace consumption (R-5 grounded: the consumed whitespace is included in the root slice; no source mutation). `findMatchingParen`, `parseRoot.sMatch`, `applyChain`, and every other `parseStrudel.ts` function remain owned by their prior phase — byte-unchanged.
  </ownership>
  <lifecycle>
    PK16 — the new walker tolerance inserts INSIDE `splitRootAndChain` which is called from `parseExpression` AFTER binding resolution. PK16 stage numbering unchanged: the fix lives at stage 2 (chain-root + splitRootAndChain) and does NOT cross any stage boundary. The new code runs after Wave 0 (Outcome 1 must be RE-CONFIRMED) and BEFORE Wave B (the permanent fixture is vendored only after the surgical edit + Wave-A probe both confirm the FLIP signal).
  </lifecycle>
  <pre_mortem>
    Most likely error (RESEARCH §R-8 PM-3, P70 occurrence 9 candidate): a false-positive — some corpus item that parses structured today now bareCodes post-fix because of an interaction the §5.6 Chesterton scan didn't anticipate. Mitigation: action 6's (ii) false-positive probe explicitly tests 5 cases; action 8's per-file loc-fidelity STOP gate runs on the full corpus; ANY non-allow-list movement = STOP. The `i = afterIdent` restore arm in action 3 is the structural mitigation.
    Second error (RESEARCH §R-8 PM-2): the upstream verdict was wrong (acorn's whitespace handling subtly differs from what RESEARCH §3 grounded). Mitigation: the V-1 probe re-runs case B independently of upstream; if case B does NOT flip post-fix, the upstream verdict was irrelevant — STOP, classify the missing mechanism, re-pose D-02.
    Third error (RESEARCH §R-8 PM-5): case B flips but case E does NOT (the exemplar's trailing blank lines + `// @version 1.0` somehow interact with `stripParserPrelude` or `splitTopLevelStatements`). Mitigation: action 6's (iii) exemplar round-trip is the verbatim case E test; if (i) cases flip but (iii) does NOT, STOP. RESEARCH §R-8 PM-5 explicitly anticipates this.
    Fourth error (the 20-17 plan-checker MAJOR class): the build appears clean but `skipWhitespaceAndLineComments` got inlined-and-stripped at the new call site (minifier did dead-code-elimination on the new arm). Mitigation: action 5 anchors on `skipWhitespaceAndLineComments` count INCREASE-by-1 (not just > 0); if pre-fix and post-fix counts are equal, the new call was elided — STOP.
  </pre_mortem>
  <verify>
    1. `git diff packages/editor/src/ir/parseStrudel.ts` shows ONLY the additive surgical edit at `pS:2521-2531` (the new comment block + `afterIdent` local + primitive call + restore-arm `else`). ZERO edits to: the bare-string arm pS:2492-2503, the backtick arm pS:2504-2520, the identifier-scan `while` at pS:2523, the `(` check at pS:2526 (only its else branch is added), the `findMatchingParen` call at pS:2527, the final `return` at pS:2534-2537, or any other function.
    2. `pnpm --filter @stave/editor build` succeeds (only known `@strudel/mondo` TS7016 may fire). Grep counts: `splitRootAndChain` > 0; `skipWhitespaceAndLineComments` INCREASED-by-1 vs Wave-0 pre-fix baseline; `findMatchingParen` unchanged; `afterIdent` informational. All recorded in commit body.
    3. The Wave-A probe (action 6) GREEN + recorded VERBATIM in OBSERVATIONS:
       - (i) all 4 strict-widen cases flip (inner.bare=true → false; inner.tag becomes Play);
       - (ii) all 5 false-positive cases preserved (disposition byte-identical to pre-fix);
       - (iii) exemplar round-trip → inner.bare=false; inner.tag=Play.
    4. `pnpm --filter @stave/editor test` == 1627/1627.
    5. `pnpm --filter @stave/app test` == 409/409.
    6. Per-file loc-fidelity STOP gate: every parity-UNCHANGED corpus file's loc-fidelity diff EMPTY; the parity-CHANGED set is EMPTY (Wave A makes NO fixture changes); ANY moved file = STOP (none observed).
    7. Commit via COMMIT_TEMPLATE.
  </verify>
  <done>
    `splitRootAndChain` PV49-extension landed at `pS:2521-2531` (the additive `skipWhitespaceAndLineComments` call + `afterIdent` restore arm); `git diff` additive-only; P68 build-hygiene grep INCREASE-by-1 on `skipWhitespaceAndLineComments`; strict-widen + false-positive + exemplar round-trip probe recorded verbatim; editor 1627/1627; app 409/409; per-file loc-fidelity STOP gate clean; commit via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE B — Permanent CI fixture vendoring (V-2 cadence; collapsed Wave-C-equivalent)

<task id="B-1" type="auto" depends_on="A-1">
  <name>Vendor 2 permanent CI fixtures under `packages/app/tests/parity-corpus/` — `bakery-159-tokenizer-whitespace.strudel` (the canonical positive distillation of `-G2drHRNFueu`) + `bakery-159-NEGATIVE-no-whitespace.strudel` (the negative control proving the whitespace-tolerance is the gate); update BAKERY-FIXTURES.md with upstream-grounded provenance</name>
  <read_first>
    20-20-OBSERVATIONS.md (the Wave-0 expected V-3 allow-list + the Wave-A strict-widen + exemplar round-trip records);
    packages/app/tests/parity-corpus/BAKERY-FIXTURES.md (the V-2 vendoring convention: minimal-distillation discipline; auto-discovered by `parity.test.ts` + `loc-fidelity.test.ts`; structurally excluded from `parity-refresh.mjs` TARGETS via the `parity-refresh.mjs:68-75` `bakery-*` slug guard);
    packages/app/tests/parity-corpus/bakery-158-all-shape-fence.strudel (the 20-19 V-2 fixture shape — minimal `bindings*, sideEffect, finalExpr` shape; the vendoring template);
    packages/app/tests/parity-corpus/bakery-158-NEGATIVE-no-sideeffect.strudel (the 20-19 V-2 negative-control shape — the negative-control template);
    packages/app/tests/parity-corpus/.bakery-runs/samples-2026-05-20T13-22-13-320Z.json (the verbatim `-G2drHRNFueu` row — extract via `node -e` per Wave-0 action 4);
    packages/app/scripts/parity-refresh.mjs lines 68-75 (the structural exclusion guard — `bakery-*` slug match);
    .planning/phases/20-musician-timeline/20-20-RESEARCH.md §6 (the minimal-distillation rationale + the auto-discovery mechanism); §7 (fixture content) + §7.2 (negative-control rationale) + §7.3 (V-3 STOP gate)
  </read_first>
  <files>
    packages/app/tests/parity-corpus/bakery-159-tokenizer-whitespace.strudel (NEW — the canonical positive fixture: minimal distillation of `-G2drHRNFueu` with the whitespace fence);
    packages/app/tests/parity-corpus/bakery-159-NEGATIVE-no-whitespace.strudel (NEW — the negative control: same shape MINUS the whitespace; proves the whitespace-tolerance is the gate);
    packages/app/tests/parity-corpus/BAKERY-FIXTURES.md (UPDATE — add a 20-20 section to the table documenting the 2 new fixtures + the upstream-grounded provenance note);
    packages/app/tests/parity-corpus/__snapshots__/parity.test.ts.snap (UPDATE — auto-captured on first run);
    packages/app/tests/parity-corpus/__snapshots__/loc-fidelity.test.ts.snap (UPDATE — auto-captured on first run)
  </files>
  <action>
    1. **Confirm Wave-0 disposition.** From OBSERVATIONS: Outcome 1 LOCKED, scope = #159 only. The fixture set is {1 canonical positive + 1 negative-control} per RESEARCH §6+§7. (NO Class-A backlog rows promoted this phase — Outcome 1 explicitly leaves #156/#149/#147/#153/#158-other-residuals as backlog with their existing classifications.)
    2. **Read the verbatim `-G2drHRNFueu` source** from `packages/app/tests/parity-corpus/.bakery-runs/samples-2026-05-20T13-22-13-320Z.json`:
       ```
       node -e "const j=JSON.parse(require('fs').readFileSync('packages/app/tests/parity-corpus/.bakery-runs/samples-2026-05-20T13-22-13-320Z.json','utf8'));console.log(j.samples.find(s=>s.hash==='-G2drHRNFueu').code)"
       ```
       Expected output (per RESEARCH §2.1 case E):
       ```
       sound ("hh hh hh hh")
       sound ("[bd bd][sd bd] bd sd")


       // @version 1.0
       ```
       Record the verbatim text in OBSERVATIONS under "Wave B — `-G2drHRNFueu` verbatim source".
    3. **Write the canonical positive fixture** `bakery-159-tokenizer-whitespace.strudel`. Per RESEARCH §7.1 — DISTILL TO MINIMAL CANONICAL FORM (drop the trailing 3 blank lines + `// @version 1.0`; those are inert prelude content covered by the 20-14 `stripParserPrelude` arm and add no discriminative load to the fixture for the whitespace fence). Keep BOTH `sound (…)` siblings to also lock the inherited FIRST-WINS multi-line disposition (so a future #153 phase that changes IR shape across multi-top-level breaks THIS fixture's snapshot explicitly, surfacing the cross-issue interaction). Content:
       ```strudel
       sound ("hh hh hh hh")
       sound ("[bd bd][sd bd] bd sd")
       ```
       (Two lines exactly; final newline; no trailing whitespace.)
    4. **Write the negative-control fixture** `bakery-159-NEGATIVE-no-whitespace.strudel`. Per RESEARCH §7.2 — the same shape MINUS the whitespace between identifier and `(`. Content:
       ```strudel
       sound("hh hh hh hh")
       sound("[bd bd][sd bd] bd sd")
       ```
       (Two lines; final newline.) Pre-fix AND post-fix this fixture parses STRUCTURED (the whitespace tolerance is not engaged); it locks the proposition "the whitespace-tolerance is the gate, not the bindings" — if `bakery-159-tokenizer-whitespace` is bareCode but the NEGATIVE is structured, the fix is broken; if both are bareCode, the inherited `sound("…")` recogniser arm is broken (an existing-mechanism regression); if both are structured, the system works as designed.
    5. **Update `BAKERY-FIXTURES.md`** with a new section "Phase 20-20 fixtures (#159 — tokenizer-whitespace fence relaxation)" documenting both new fixtures + the upstream-grounded provenance note. The provenance note (per RESEARCH §3 + §12):
       > Strudel upstream UNAMBIGUOUSLY TOLERATES whitespace between identifier and call-site `(` via pure JS-eval pass-through:
       > - `@strudel/transpiler@1.2.6 transpiler.mjs:25-30` — `parse(input, {ecmaVersion: 2022, …})` (acorn);
       > - `@strudel/core@1.2.6 evaluate.mjs:29-39` — `Function(body)()` (native JS).
       >
       > Acorn parses `sound ("hh")` and `sound("hh")` as IDENTICAL `CallExpression` ASTs per ECMA-262 (`CallExpression :: MemberExpression Arguments` allows arbitrary whitespace). Our parser MIRRORS that permissiveness via the PV49 extension at `splitRootAndChain` (parseStrudel.ts:2521-2531; Codeberg pin SHA `f73b3956`).
    6. **Confirm `parity-refresh.mjs:68-75` exclusion guard.** The guard's grep is on `bakery-*` prefix; the new slugs `bakery-159-tokenizer-whitespace` + `bakery-159-NEGATIVE-no-whitespace` both start with `bakery-`, so they are excluded from upstream-drift checks by construction. Run `node packages/app/scripts/parity-refresh.mjs --dry-run` (or equivalent — verify the script's CLI by reading `parity-refresh.mjs:1-30` first); confirm the new slugs are NOT flagged as missing upstream. Record in OBSERVATIONS.
    7. **Snapshot-capture the new fixtures.** Run `pnpm --filter @stave/app test`. The new fixtures are auto-discovered by `parity.test.ts` and `loc-fidelity.test.ts`; their first run captures snapshots. NO `-u` flag (snapshots write themselves on first encounter; `-u` would overwrite pre-existing snapshots). Expected:
       - parity-corpus count moves 47 → 49 (+2 new fixtures);
       - loc-fidelity count moves 47 → 49 (+2 new fixtures);
       - total app test count moves 409 → 413 (+2 parity + +2 loc-fidelity);
       - EVERY pre-existing fixture's snapshot UNCHANGED (per-file loc-fidelity STOP gate per-wave).
       Both new fixtures' snapshots assert `body.tag !== 'Code'` (structured at the parity oracle fence). Record the exact post-snapshot counts in OBSERVATIONS under "Wave B — V-2 fixtures captured".
    8. **(Optional grounding spec — RESEARCH §7.1 second-half.)** OPTIONAL: vendor `packages/app/tests/parity-corpus/_wave159-grounding.spec.ts` for inline-IR-shape assertions beyond the parity oracle's bin. Skip-or-vendor decision: SKIP unless Wave A's exemplar round-trip stdout shows ambiguity in the inner shape. If vendored, mirror the 20-18 `_waveC-grounding.spec.ts` shape (auto-discovered by vitest; assertions on `body.tag === 'Play'`, `via` undefined, etc.). DEFAULT: SKIP (the auto-discovered `parity.test.ts` snapshot already locks the fence; the explicit inner-tag check adds little discriminative load).
    9. **P68 build hygiene NOT NEEDED THIS WAVE** — no editor-src change. `git diff --name-only` shows only the new `.strudel` files, the BAKERY-FIXTURES.md update, and the new `__snapshots__/*.snap` entries.
    10. **Per-file loc-fidelity STOP gate (per-wave).** Same as Wave A action 8: every parity-UNCHANGED pre-existing corpus file's loc-fidelity snapshot byte-unchanged; the parity-CHANGED set = {2 new Wave-B fixtures} ⊆ the V-3 allow-list. ANY other moved file = STOP. Record verbatim in OBSERVATIONS.
    11. **Commit via COMMIT_TEMPLATE**, gitmoji `:test_tube:`. `git add packages/app/tests/parity-corpus/bakery-159-tokenizer-whitespace.strudel packages/app/tests/parity-corpus/bakery-159-NEGATIVE-no-whitespace.strudel packages/app/tests/parity-corpus/BAKERY-FIXTURES.md packages/app/tests/parity-corpus/__snapshots__/*.snap` (plus the optional `_wave159-grounding.spec.ts` if vendored). Problem: "20-20's tokenizer-whitespace relaxation has no permanent CI fixture; a future regex contraction or a future walker-tolerance regression would silently break without per-class regression coverage; #153 multi-top-level interaction is invisible without a fixture that locks today's FIRST-WINS disposition"; Fix: "vendor 2 permanent CI fixtures — `bakery-159-tokenizer-whitespace.strudel` (canonical positive: minimal distillation of `-G2drHRNFueu`; asserts whole-program structured via the PV49-extended splitRootAndChain) + `bakery-159-NEGATIVE-no-whitespace.strudel` (negative control: same shape minus whitespace; proves the whitespace-tolerance is the gate); BAKERY-FIXTURES.md updated with upstream-grounded provenance note (acorn + Function-eval citations at Codeberg pin f73b3956); per-file loc-fidelity STOP gate clean (no pre-existing fixture moved); structural exclusion from parity-refresh.mjs confirmed".
  </action>
  <ownership>
    This wave owns the per-class permanent CI fixtures (the anti-drift mechanism for the tokenizer-whitespace class) + the negative-control + the BAKERY-FIXTURES.md provenance documentation. The auto-discovery + snapshot mechanism is owned by `parity.test.ts` / `loc-fidelity.test.ts` (unchanged this wave). The structural exclusion from `parity-refresh.mjs` is owned by the existing `parity-refresh.mjs:68-75` guard (unchanged this wave; the `bakery-159-*` slugs are excluded by NOT being added to TARGETS).
  </ownership>
  <lifecycle>
    PK17 step 5 (vendor permanent regression coverage). Runs AFTER Wave A (the surgical edit must land; the Wave-A probe must confirm the FLIP; if either fails, no fixtures are vendored) and BEFORE Wave V-1 (the fresh parity:bakery measurement runs against a suite that already includes the new fixtures so the +1 (or +2) per-class regression coverage is on the merge-candidate head).
  </lifecycle>
  <pre_mortem>
    Most likely error (RESEARCH §R-8 PM-4 / PV49 substrate violation): a new fixture's loc-fidelity snapshot accidentally shares state with a pre-existing fixture's snapshot (some test isolation bug). Mitigation: the loc-fidelity test file structure is `__snapshots__/<fixture>.strudel.snap` per-file; no cross-file state. Action 10's per-file STOP gate confirms each pre-existing snapshot is byte-unchanged.
    Second error: the negative-control fixture is malformed (e.g. tab vs space difference or trailing-newline difference would make it equivalent to the positive fixture). Mitigation: action 4's content is byte-exact (LF newlines, no tabs, final newline only); record the byte count in OBSERVATIONS as a checksum-like anchor.
    Third error: the `parity-refresh.mjs:68-75` exclusion guard does NOT cover the new slugs — the upstream-drift tool reports them as missing upstream. Mitigation: action 6's `--dry-run` recording.
  </pre_mortem>
  <verify>
    1. 2 new fixture files present under `packages/app/tests/parity-corpus/`; canonical positive has the whitespace; negative-control does not; both have valid two-line `sound (…)` / `sound(…)` content with final newline.
    2. BAKERY-FIXTURES.md has a new "Phase 20-20 fixtures (#159)" section documenting both entries + the upstream-grounded provenance note (acorn + Function-eval citations at Codeberg pin `f73b3956`).
    3. `pnpm --filter @stave/app test` GREEN — parity-corpus count moved 47 → 49; loc-fidelity count moved 47 → 49; total app test count moved 409 → 413.
    4. Both new fixtures' snapshots assert STRUCTURED (whole-program `body.tag !== 'Code'`); captured under `__snapshots__/parity.test.ts.snap` and `__snapshots__/loc-fidelity.test.ts.snap`.
    5. EVERY pre-existing fixture's snapshot UNCHANGED. Captured in OBSERVATIONS.
    6. `pnpm --filter @stave/editor test` 1627/1627 UNCHANGED.
    7. `node packages/app/scripts/parity-refresh.mjs --dry-run` (or equivalent) confirms the structural exclusion guard covers `bakery-159-*` (none flagged as missing upstream).
    8. Commit via COMMIT_TEMPLATE.
  </verify>
  <done>
    2 permanent CI fixtures vendored (1 canonical positive + 1 negative-control); BAKERY-FIXTURES.md updated with upstream-grounded provenance; auto-discovery picks up the new files; parity-corpus 49 + loc-fidelity 49 GREEN; total app test count 413; per-file loc-fidelity STOP gate clean (no pre-existing fixture moved); structural exclusion from parity-refresh.mjs confirmed; commit via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE V — Dual gate (D-04) + cross-wave STOP gate + phase close (V-1 / V-3 / V-4 cadence)

### V-1 — Dual-gate measurement (crit-1 + crit-2 on the same merge-candidate commit)

<task id="V-1" type="auto" depends_on="B-1">
  <name>Fresh `pnpm parity:bakery --n 50` (PK17 step-6 cadence; new ISO stamp; same upstream pin); confirm crit-1 (Wave-B fixture asserts whole-program structured on `-G2drHRNFueu`) + crit-2 (fresh ≥ 49/50 = 98.0% AND ≥ 48/50 = 96.0% floor); record VERBATIM</name>
  <read_first>
    20-20-OBSERVATIONS.md (the 96.0% baseline + the FROZEN scope lock + the Wave-A flip evidence + the Wave-B captured fixture counts);
    .planning/phases/20-musician-timeline/20-20-RESEARCH.md §8 (the dual gate commands + crit-2 arithmetic + the regression-classify procedure);
    .planning/phases/20-musician-timeline/20-19-SUMMARY.md "V-1" section (the 20-19 measurement template — the verbatim PK17 step-6 invocation; the artifact stamp pattern);
    packages/app/tests/parity-corpus/.bakery-runs/result-2026-05-20T13-22-13-320Z.json (the BASELINE 48/50 = 96.0% — the must-not-regress floor; the 2 residual rows `{-7LU6zgzViSM, -G2drHRNFueu}`);
    packages/app/tests/parity-corpus/_bakery-classify.spec.ts (line ~77 per Wave-0 verification — the one-arg `parseStrudel(s.code)` invariant; confirm unchanged this phase)
  </read_first>
  <files>
    .planning/phases/20-musician-timeline/20-20-OBSERVATIONS.md (APPEND — V-1 measurement record);
    packages/app/tests/parity-corpus/.bakery-runs/samples-<NEW-ISO>.json (NEW — fresh stamp; gitignored under .bakery-runs);
    packages/app/tests/parity-corpus/.bakery-runs/result-<NEW-ISO>.json (NEW — fresh stamp; gitignored)
  </files>
  <action>
    1. **Confirm the one-arg oracle invariant** (the 20-18 Wave E hardened invariant). `grep -n 'parseStrudel(s.code)' packages/app/tests/parity-corpus/_bakery-classify.spec.ts` → the call site passes EXACTLY ONE arg (no opts). 20-20 ships no opts threading change. Record in OBSERVATIONS.
    2. **Confirm the parity:bakery script form.** Read `packages/app/package.json` and the workspace root `package.json` to determine the canonical invocation (`pnpm --filter @stave/app parity:bakery -- --n 50` or root-level `pnpm parity:bakery --n 50` — confirm in Wave-0 OBSERVATIONS reading). Record the canonical form in OBSERVATIONS.
    3. **Fresh PK17 step-6 measurement.** Run the canonical parity:bakery invocation with `--n 50`. Capture the NEW ISO stamp + the UPSTREAM_SHA + the structured count + the percentage. Expected: `samples-<NEW-ISO>.json` + `result-<NEW-ISO>.json` under `.bakery-runs/` (gitignored), SHA `f73b3956` unchanged, structured == 49 (or higher if any bonus exemplar flips à la 20-19's `-1j62z5xjyCN`) ≥ 49 = ≥ 98.0%, AND structured ≥ 48 (the must-not-regress 96.0% floor).
    4. **Classify the diff** vs `result-2026-05-20T13-22-13-320Z.json`. Specifically:
       - row `-G2drHRNFueu` (#159): baseline `code` → fresh expected `structured` (the +1 from the fix);
       - row `-7LU6zgzViSM` (#143): baseline `code` → fresh expected unchanged (`code`); any flip is a bonus and recorded but not in 20-20 scope;
       - every other row: baseline `structured` → fresh expected `structured` (no regression). ANY regression here is a PK18 STOP regardless of the % — re-run `pnpm parity:bakery --replay <old-stamp>` + `--replay <new-stamp>` (if the script supports replay; otherwise diff the per-row classifications from the two `result-*.json` files via `node -e "..."`), diff the per-row verdicts, classify the regressing row's mechanism (likely: an existing structured-path row had a `splitRootAndChain` call that consumed whitespace it shouldn't have — but the §5.6 Chesterton scan + Wave-A action 6 (ii) probe ruled this out analytically AND empirically; if it happens, the most-likely cause is RESEARCH §R-8 PM-4 — investigate); route to backlog OR if it traces back to our fix, re-pose D-02 fix-shape choice to user with EVIDENCE; NEVER amend the 96.0% floor downward.
    5. **Dual-gate check on the same head:**
       - **Crit-1 HARD:** the Wave-B `bakery-159-tokenizer-whitespace.strudel` fixture's parity snapshot asserts whole-program structured (`body.tag !== 'Code'`); re-confirm by running `pnpm --filter @stave/app test -- bakery-159` isolated. Confirm both new fixtures (canonical positive + negative-control) PASS. Confirm the inner shape is `Play` if the optional `_wave159-grounding.spec.ts` was vendored.
       - **Crit-2 HARD:** fresh structured ≥ 49 (≥ 98.0%) AND ≥ 48 (must-not-regress 96.0% floor).
       - **Bar-lowering check:** no premise was bar-lowered; no scope-expansion mid-phase (Wave-0 #153 stayed backlog); no second-workaround; the floor is the 96.0% baseline unchanged.
       BOTH crit hold on the SAME head SHA. Record VERBATIM in OBSERVATIONS under "V-1 — D-04 dual-gate measurement", mirroring the 20-19 V-1 record block.
    6. **If crit-2 falsifies the floor (structured < 48):** PK18 HARD STOP (RESEARCH §R-8 PM-4). Capture the regressing row(s) verbatim from the `result-<NEW-ISO>.json` diff. Re-pose D-04 to user with EVIDENCE. Do NOT proceed to V-3/V-4. Do NOT amend the floor downward (the 20-17/20-18 AMENDMENT-2 precedent: amend the SCOPE on EVIDENCE; never the FLOOR or the must-not-regress invariant).
    7. **If crit-1 falsifies (the Wave-B fixture's parity snapshot does NOT assert structured):** PK18 HARD STOP. This means Wave A's surgical edit did not actually flip the canonical positive fixture (the Wave-A action 6 (iii) exemplar round-trip was a stub that lied, or Wave A's commit was reverted, or a B-1 edit broke the fixture). Re-classify; re-pose D-02 to user.
    8. **Commit via COMMIT_TEMPLATE**, gitmoji `:bar_chart:`. `git add -f .planning/phases/20-musician-timeline/20-20-OBSERVATIONS.md` (the .bakery-runs/*.json files are gitignored; that is correct — the record lives in OBSERVATIONS). Problem: "the 20-20 PV49-extension needs a fresh PK17 step-6 measurement to confirm crit-2 ≥ 98.0% + must-not-regress 96.0% (D-04 dual gate)"; Fix: "fresh `pnpm parity:bakery --n 50` recorded; SHA `f73b3956` unchanged; structured = <N>/50 = <X.X>% (≥ 98.0% AND ≥ 96.0% floor); `-G2drHRNFueu` flipped code → structured (the +1 from the fix); `-7LU6zgzViSM` unchanged (out-of-scope per D-04); no regressions on the 48 baseline-structured rows; crit-1 (Wave-B fixture asserts structured) + crit-2 (98.0%) + bar-lowering check ALL clean".
  </action>
  <ownership>
    This wave owns the dual-gate verdict. Crit-1 was technically satisfied by Wave B (the fixture snapshot is structured); V-1 re-runs it on the merge-candidate HEAD to confirm it holds alongside the parity-corpus 49 + loc-fidelity 49 + editor 1627. Crit-2 is owned exclusively here — the fresh PK17 step-6 measurement is the gate. The must-not-regress floor enforcement (no bar-lowering) is owned here.
  </ownership>
  <lifecycle>
    PK17 step 6 (measure on the merge candidate). Runs after Wave B (the fixtures are landed; the snapshot baseline is captured) and BEFORE V-3/V-4 (the close-out cadence depends on the dual gate PASSING — no PR drafted until V-1 confirms).
  </lifecycle>
  <pre_mortem>
    Most likely error (RESEARCH §R-8 PM-4): crit-1 PASSES but crit-2 < 96.0% — meaning the fix caused a regression on some OTHER row that 20-18/20-19 had structured. Mitigation: action 4 diffs per-row; ANY non-`-G2drHRNFueu` row flipping structured → code is the regression; classify; never bar-lower.
    Second error: the new ISO stamp's UPSTREAM_SHA differs from `f73b3956` (upstream pin drift — unlikely between 20-19 and 20-20 but possible). Mitigation: action 3 captures the SHA; if it differs, STOP (a baseline shift invalidates the comparison; re-pose to user about whether to re-pin or run on the new SHA).
    Third error: the one-arg oracle invariant is broken (someone added an opts arg to the call site). Mitigation: action 1's grep is non-optional.
  </pre_mortem>
  <verify>
    1. The one-arg oracle invariant `_bakery-classify.spec.ts` `parseStrudel(s.code)` has EXACTLY one arg.
    2. Fresh `samples-<NEW-ISO>.json` + `result-<NEW-ISO>.json` under `.bakery-runs/`; SHA `f73b3956` unchanged; structured ≥ 49 = ≥ 98.0% (or ≥ 48 = ≥ 96.0% floor at minimum).
    3. Per-row diff: `-G2drHRNFueu` flipped code → structured; `-7LU6zgzViSM` unchanged; the 48 baseline-structured rows ALL still structured.
    4. The Wave-B `bakery-159-tokenizer-whitespace.strudel` fixture's parity snapshot asserts structured; isolated run PASSES.
    5. Crit-1 + crit-2 + bar-lowering check ALL hold on the SAME HEAD SHA; recorded VERBATIM in OBSERVATIONS.
    6. Commit via COMMIT_TEMPLATE.
  </verify>
  <done>
    Fresh PK17 step-6 measurement recorded; SHA `f73b3956` unchanged; structured ≥ 49 (≥ 98.0%) AND ≥ 48 (must-not-regress 96.0% floor); per-row diff verifies `-G2drHRNFueu` alone (or +bonus) flipped, no regressions; crit-1 + crit-2 + bar-lowering check pass; commit via COMMIT_TEMPLATE.
  </done>
</task>

### V-3 — Cross-wave per-file loc-fidelity STOP gate (full-corpus pass on the merge-candidate)

<task id="V-3" type="auto" depends_on="V-1">
  <name>Run the FULL `pnpm --filter @stave/app test` against the merge-candidate HEAD; confirm parity-corpus 49 + loc-fidelity 49 hold; confirm the parity-CHANGED set ⊆ the enumerated V-3 allow-list; any other moved file = STOP</name>
  <read_first>
    20-20-OBSERVATIONS.md (the Wave-0 expected V-3 allow-list + the per-wave loc-fidelity STOP gate records);
    .planning/phases/20-musician-timeline/20-19-SUMMARY.md "V-3" section (the 20-19 12-file allow-list precedent — the structure of this STOP gate)
  </read_first>
  <files>
    .planning/phases/20-musician-timeline/20-20-OBSERVATIONS.md (APPEND — V-3 full-corpus record)
  </files>
  <action>
    1. **Run the full suite cleanly.** `pnpm --filter @stave/app test` against the V-1 head SHA. Expected: parity-corpus (47 + 2) = 49 GREEN + loc-fidelity (47 + 2) = 49 GREEN + the rest of the app tests = 409 + 4 = 413 GREEN total. NO `-u` flag — every snapshot is committed.
    2. **Enumerate the V-3 allow-list — the EXACT files that may have moved across the entire phase:**
       - 2 new Wave-B fixtures (`bakery-159-tokenizer-whitespace.strudel` + `bakery-159-NEGATIVE-no-whitespace.strudel`);
       - the corpus file (if any) for `-G2drHRNFueu` IF it maps to an existing `*.strudel` file (per Wave-0 it does NOT; the hash lives in `.bakery-runs/samples-*.json`, not in `*.strudel`; the canonical positive fixture IS the corpus representative of the hash but it's new in Wave B);
       - any other parity-CHANGED file from the fresh PK17 step-6 (V-1 action 4 enumerates these).
       Expected allow-list size: EXACTLY 2 (the 2 Wave-B fixtures' snapshots).
    3. **STOP gate.** Every parity-UNCHANGED corpus file's loc-fidelity diff MUST be EMPTY. The parity-CHANGED set ⊆ the enumerated allow-list. ANY other moved file = silent drift = STOP.
       - Run `git diff --name-only HEAD~4..HEAD packages/app/tests/parity-corpus/__snapshots__/` (the 4-commit window covers Wave 0 + Wave A + Wave B + V-1; the only snapshot writes should be the 2 new fixtures' entries).
       - Diff against the allow-list. Each moved entry MUST justify itself by the allow-list.
       - Record verbatim in OBSERVATIONS under "V-3 — cross-wave full-corpus per-file loc-fidelity STOP gate".
    4. **If any file outside the allow-list moved:** PK18 STOP (RESEARCH §R-8 PM-4). The PV49 substrate's byte-additive whitespace consumption proves no parity-UNCHANGED file's loc-spans should shift; an observed violation is silent drift. Classify the moved file's content shift; if traceable to a `splitRootAndChain` false-positive (some user code's identifier-then-paren shape with embedded whitespace now flips classification), the fix is broken — re-pose D-02 to user. Do NOT extend the allow-list defensively (the 20-18/20-19 V-3 lesson — the PV49 mechanism is the proof, not the allow-list).
    5. **Commit via COMMIT_TEMPLATE**, gitmoji `:white_check_mark:`. `git add -f .planning/phases/20-musician-timeline/20-20-OBSERVATIONS.md`. Problem: "the cross-wave full-corpus per-file loc-fidelity STOP gate is the silent-drift guard — the PV49 byte-additive whitespace-consumption substrate proves no movement by construction but the gate observes"; Fix: "ran `pnpm --filter @stave/app test` full-corpus pass against the V-1 head; (47+2) parity + (47+2) loc-fidelity GREEN; parity-CHANGED set = {2 new Wave-B fixtures} — exactly the enumerated allow-list; NO other moved file = no silent drift".
  </action>
  <ownership>
    This wave owns the cross-wave PV49 silent-drift guard's verdict. PV49 itself (the byte-additive whitespace-consumption substrate) is owned by the existing primitive at pS:1075 + the Wave-A surgical edit (R-5 / RESEARCH §6.1 grounded).
  </ownership>
  <lifecycle>
    PK17 step 6.5 (cross-wave validation on the merge candidate). Runs AFTER V-1 (the merge-candidate HEAD must be the V-1 SHA) and BEFORE V-4 (no SUMMARY / catalogue updates / PR draft until the silent-drift guard is CLEAN).
  </lifecycle>
  <pre_mortem>
    Most likely error: a moved file outside the allow-list (RESEARCH §R-8 PM-4). Mitigation: action 3's diff against the allow-list; action 4's NEVER-defensively-extend rule.
    Second error: a snapshot was over-written silently (someone ran `pnpm test -u` mid-phase). Mitigation: NO `-u` flag in action 1; the snapshot diff against `HEAD~4` enumerates every change across commits.
  </pre_mortem>
  <verify>
    1. `pnpm --filter @stave/app test` GREEN end-to-end on the V-1 head; counts (47+2) parity + (47+2) loc-fidelity + 315-base = 413.
    2. The parity-CHANGED set across the entire phase ⊆ the enumerated allow-list = {2 Wave-B fixtures}; ZERO other moved files.
    3. Record verbatim in OBSERVATIONS.
    4. Commit via COMMIT_TEMPLATE.
  </verify>
  <done>
    Cross-wave full-corpus per-file loc-fidelity STOP gate CLEAN; the parity-CHANGED set ⊆ enumerated allow-list = {bakery-159-tokenizer-whitespace.strudel + bakery-159-NEGATIVE-no-whitespace.strudel}; ZERO silent drift; commit via COMMIT_TEMPLATE.
  </done>
</task>

### V-4 — Phase close: SUMMARY + catalogue updates + Ground Truth extension + single non-stacked PR

<task id="V-4" type="auto" depends_on="V-3">
  <name>Write 20-20-SUMMARY.md; update .anvi catalogues (PK16/PK17/PK18 + PV49 addenda; PV54 explicit NOT-triggered note; P70 cadence row); EXTEND GROUND_TRUTH_SIGNAL_MJS.md with the Parser/Evaluator Pipeline section; open the single non-stacked PR with `closes #159`; record post-merge artifact verification recipe; Claude NEVER merges</name>
  <read_first>
    20-20-OBSERVATIONS.md (every wave's verbatim record);
    .planning/phases/20-musician-timeline/20-19-SUMMARY.md (the SUMMARY template + the catalogue-update section pattern);
    .anvi/krama.md PK16 (parser pipeline — extend with a PV49-extension addendum for the new caller at stage 2) + PK17 (friction-first cycle — add 20-20 measured ≥ 98.0% row + N PK18 re-poses) + PK18 (HARD-GATE cascade discipline — count any 20-20 PK18 re-poses);
    .anvi/hetvabhasa.md P70 (the eight-occurrence pattern — if 0 re-poses, P70 stays at 8 with a new clean-run cadence row);
    .anvi/vyapti.md PV49 (the loc-additivity substrate — the 20-20 identifier-to-paren extension is a new occurrence to record; mirror the §6.1 ORIGIN/WHY/HOW template) + PV54 (FLOOR-grep obligation — confirm explicit NOT-triggered note);
    ~/.anvideck/projects/struCode/ref/GROUND_TRUTH_SIGNAL_MJS.md (the 20-18 Ground Truth doc — EXTEND with a "Parser/Evaluator Pipeline" section per RESEARCH §12);
    ~/.claude/projects/-Users-mrityunjaybhardwaj-Documents-projects-struCode/memory/feedback_commit_msg_heredoc.md (the COMMIT_TEMPLATE heredoc enforcement);
    ~/.claude/projects/-Users-mrityunjaybhardwaj-Documents-projects-struCode/memory/feedback_stacked_pr_base_retarget.md (the post-merge artifact verification recipe)
  </read_first>
  <files>
    .planning/phases/20-musician-timeline/20-20-SUMMARY.md (NEW — the phase close-out);
    .anvi/krama.md (UPDATE — PK16/PK17/PK18 addenda for 20-20);
    .anvi/hetvabhasa.md (UPDATE — P70 occurrence-9 IF Wave 0 surfaced a new one; else: cadence row noting 20-20 PK18 re-pose count);
    .anvi/vyapti.md (UPDATE — PV49 new occurrence for the identifier-to-paren extension with ORIGIN/WHY/HOW per RESEARCH §6.1; PV54 explicit NOT-triggered note);
    ~/.anvideck/projects/struCode/ref/GROUND_TRUTH_SIGNAL_MJS.md (UPDATE — add "Parser/Evaluator Pipeline" section per RESEARCH §12)
  </files>
  <action>
    1. **Write 20-20-SUMMARY.md** mirroring the 20-19-SUMMARY.md shape: status / verdict / branch / V-1 commit / baseline pre + post / bakery fresh stamp + percentage / closes-list / catalogue updates / fixtures-added / per-wave deltas / the FROZEN Outcome-1 scope shipped / the N PK18 re-poses (target: 0 — replicate 20-19) / Ground-Truth-doc disposition (EXTEND `GROUND_TRUTH_SIGNAL_MJS.md` per RESEARCH §12) / V-3 allow-list / deferred backlog (#143 + #153 with LAST-WINS evidence + #156/#149/#147/#158-other-residuals untouched) / operational-discipline-recap / Cognitive Discoveries section.
    2. **Update `.anvi/krama.md`** with PK16 + PK17 + PK18 addenda specific to 20-20:
       - PK16 addendum: PV49-extension at stage 2 (`splitRootAndChain` identifier-then-paren branch); PK16 stage numbering unchanged; the new caller extends the existing PV49 substrate at a different boundary (identifier-to-paren rather than chain-method-to-dot).
       - PK17 addendum: 20-20 measured ≥ 98.0% (+2pp from 96.0% baseline); N PK18 re-poses (target: 0 if Wave 0 confirmed cleanly).
       - PK18 addendum: any 20-20 PK18 re-poses (target: 0). If 0, that's the SECOND consecutive clean-run phase (20-19 was the first per its SUMMARY) — document this as substrate-works-hypothesis-strengthening.
    3. **Update `.anvi/hetvabhasa.md`** with P70 cadence:
       - If Wave 0 surfaced a P70 occurrence 9 (Wave-0 V-1 stdout diverged from RESEARCH §2.2), add P70 occurrence 9 with the verbatim trace.
       - Otherwise, add a row in P70's existing cadence table noting 20-20 ran clean (no new occurrence). P70 stays at 8 occurrences.
    4. **Update `.anvi/vyapti.md`**:
       - Add a new PV49 occurrence row per RESEARCH §6.1 template:
         > **PV49 occurrence (20-20):** `splitRootAndChain` identifier-then-paren boundary tolerance.
         > - **ORIGIN:** 20-20 V-1 N=50 observation of bakery `-G2drHRNFueu` bareCode'ing on `sound ("hh hh hh hh")`; Wave-0 5-cell factoring probe RUN'd at pin `f73b3956` (RESEARCH §2); upstream-grounded TOLERATES verdict at `@strudel/transpiler/transpiler.mjs:25-30` + `@strudel/core/evaluate.mjs:29-39`.
         > - **WHY:** without this extension, every call site with discretionary whitespace between identifier and `(` bareCodes — a parity gap whose source is recogniser-narrowness, not upstream rejection. The class is open-ended (any user could space-pad any call site).
         > - **HOW:** `splitRootAndChain` calls `skipWhitespaceAndLineComments` between identifier-scan and `(`-check; the consumed whitespace is included inside the returned `root` slice so `parseRoot`'s already-tolerant `\s*\(` regex arms match downstream. Single function change; no new IR shape; no new state.
       - Add an explicit PV54 note: "PV54 FLOOR-grep obligation NOT triggered this phase — 20-20 ships no new top-level PatternIR tag. The Signal/Builder tags from 20-18 are unchanged; the new code is purely walker-tolerance inside an existing parser function. The PV53/PV54 consumer-audit obligation is dormant for 20-20."
    5. **EXTEND `~/.anvideck/projects/struCode/ref/GROUND_TRUTH_SIGNAL_MJS.md`** with a new "Parser/Evaluator Pipeline" section per RESEARCH §12. The section covers:
       - `@strudel/core@1.2.6 repl.mjs:222-282` — the `evaluate` flow (transpiler → safeEval → eachTransform → setPattern);
       - `@strudel/core@1.2.6 evaluate.mjs:29-39` — `safeEval` wraps as `Function(body)()`;
       - `@strudel/core@1.2.6 evaluate.mjs:41-53` — top-level `evaluate` wires transpiler + safeEval;
       - `@strudel/transpiler@1.2.6 transpiler.mjs:25-30` — `acorn.parse(input, {ecmaVersion: 2022, …})`;
       - `@strudel/transpiler@1.2.6 transpiler.mjs:21-213` — AST walk (no whitespace normalisation; selective node rewrites);
       - `@strudel/transpiler@1.2.6 transpiler.mjs:198-204` — `addReturn` rewrites ONLY `body[body.length-1]` (the LAST-WINS mechanism for unlabeled sibling top-level pattern expressions; the upstream grounding for #153 IR-shape choice in a future phase);
       - `@strudel/transpiler@1.2.6 transpiler.mjs:464-491` — `labelToP` (LabeledStatement `$: …` or `name: …` → `.p(name)`); only fires on labels, NOT on bare expressions.
       Each section has a one-paragraph summary + the `file:line` citation. Add REFs from PV49 (the new addendum) and from any new hetvabhasa entry to this section. The Ground Truth doc lives in `~/.anvideck/projects/struCode/ref/` (NOT in the repo); the doc edit is OUT of `git diff` scope but recorded in OBSERVATIONS + SUMMARY.
    6. **Self-review pass on the diff.** Per AnviDev step 5: audit every changed file for dead code, redundant logic, missing edge cases, parameter leaks, type safety holes. Specifically: confirm the surgical edit at `pS:2521-2531` is strictly permissive (the `i = afterIdent` restore arm preserves today's behaviour for bare-identifier roots); confirm the comment block cites the upstream pin SHA `f73b3956` + the acorn + Function-eval `file:line` citations; confirm the `skipWhitespaceAndLineComments` import path is unchanged (it's the same module-internal function — no new import needed).
    7. **Open the PR (single non-stacked → main).**
       - `git push -u origin feat/20-20-tokenizer-whitespace`.
       - `gh pr create --base main --head feat/20-20-tokenizer-whitespace --title "feat(20-20): splitRootAndChain PV49-extension on identifier-to-paren whitespace (#159)"`. Body via HEREDOC:
         ```
         ## Summary
         - Insert `skipWhitespaceAndLineComments` between identifier scan and `(` check inside `splitRootAndChain`'s `} else {` identifier-then-paren branch at `parseStrudel.ts:2521-2531`; include the consumed whitespace inside the returned `root` slice so `parseRoot`'s already-tolerant `\s*\(` regex arms (sMatch / noteMatch / miniMatch / looseMatch) match downstream.
         - Mirrors upstream Strudel's pure-JS-eval whitespace tolerance: `@strudel/transpiler@1.2.6 transpiler.mjs:25-30` (`acorn.parse(input, {ecmaVersion: 2022, …})`) + `@strudel/core@1.2.6 evaluate.mjs:29-39` (`Function(body)()`). Codeberg pin SHA `f73b3956`.
         - `-G2drHRNFueu` flips whole-program STRUCTURED via the inherited `sound("…")` recogniser arm.
         - 2 permanent CI fixtures (1 canonical positive + 1 negative-control) vendored as per-class regression coverage.

         ## Gate
         - **Crit-1 HARD:** `bakery-159-tokenizer-whitespace.strudel` parity snapshot asserts whole-program structured (`body.tag !== 'Code'`); inner shape `Play`; negative-control also structured (proves the whitespace-tolerance is the gate, not the bindings).
         - **Crit-2 HARD:** fresh `pnpm parity:bakery --n 50` = <N>/50 = <X.X>% (≥ 98.0% AND ≥ 96.0% must-not-regress floor).
         - **No bar-lowering. No second-workaround. No scope-expansion.**

         ## Test plan
         - [ ] `pnpm --filter @stave/editor test` 1627/1627
         - [ ] `pnpm --filter @stave/app test` 413/413
         - [ ] `pnpm parity:bakery --n 50` structured ≥ 49/50 (recorded in OBSERVATIONS)
         - [ ] Per-file loc-fidelity STOP gate clean (V-3 allow-list = {2 Wave-B fixtures})
         - [ ] Post-merge: `git merge-base --is-ancestor <PR-head> origin/main` exit 0; `dist/index.js` grep `splitRootAndChain` > 0 and `skipWhitespaceAndLineComments` INCREASED-by-1

         closes #159
         ```
       - GitHub honors ONE auto-close keyword; `closes #159` only. (No Class-A bonus closes this phase per Outcome 1; #153 stays backlog with the LAST-WINS evidence recorded in OBSERVATIONS + SUMMARY for a future maintainer.)
    8. **Record the post-merge artifact verification recipe** (for user to execute post-merge — Claude NEVER merges). In SUMMARY.md under "Operational discipline":
       ```
       git fetch origin
       git merge-base --is-ancestor <PR-HEAD-SHA> origin/main && echo "ancestor: OK"
       pnpm --filter @stave/editor build
       grep -c "splitRootAndChain" packages/editor/dist/index.js          # > 0
       grep -c "skipWhitespaceAndLineComments" packages/editor/dist/index.js  # > pre-fix baseline (INCREASE by 1)
       grep -c "findMatchingParen" packages/editor/dist/index.js          # unchanged
       pnpm --filter @stave/editor test   # 1627/1627
       pnpm --filter @stave/app test      # 413/413
       pnpm parity:bakery --n 50          # ≥ 49/50 (≥ 98.0%)
       ```
    9. **Commit via COMMIT_TEMPLATE**, gitmoji `:memo:`. `git add -f .planning/phases/20-musician-timeline/20-20-SUMMARY.md .anvi/krama.md .anvi/hetvabhasa.md .anvi/vyapti.md`. Problem: "phase 20-20 needs a SUMMARY + catalogue provenance + a PR opened for user-merge review (Claude never merges); the Ground Truth doc needs the Parser/Evaluator Pipeline section for future-phase REF wires"; Fix: "write 20-20-SUMMARY.md mirroring the 20-19 close-out structure; update .anvi/{krama,hetvabhasa,vyapti}.md with 20-20 addenda (PK16 PV49-extension at stage 2 / PK17 +2pp / PK18 N re-poses / P70 cadence / PV49 identifier-to-paren occurrence with ORIGIN/WHY/HOW / PV54 NOT-triggered note); extend GROUND_TRUTH_SIGNAL_MJS.md with the Parser/Evaluator Pipeline section per RESEARCH §12; open single non-stacked PR `feat/20-20-tokenizer-whitespace` → main with `closes #159`; record post-merge artifact verification recipe; Claude stops at PR-up".
    10. **STOP at PR-up.** Do NOT merge. Checkpoint to the user: "PR is up at #<N>. Review and merge when ready. Post-merge: execute the artifact verification recipe in SUMMARY.md."
  </action>
  <ownership>
    This wave owns the phase close-out: SUMMARY, catalogue addenda, Ground Truth extension, PR draft, post-merge recipe. Claude NEVER merges (AnviDev step 6). The catalogues' new entries' provenance (ORIGIN/WHY/HOW) is owned here. The Ground Truth doc extension lives outside the repo at `~/.anvideck/projects/struCode/ref/` (owned by the project's reference materials).
  </ownership>
  <lifecycle>
    PK17 step 7 (phase close + lessons compound forward) + PK18 step 9 (catalogue the discipline used). Runs after V-3 (the silent-drift guard is CLEAN). Strictly the last wave.
  </lifecycle>
  <pre_mortem>
    Most likely error: a Co-Authored-By trailer accidentally added to a commit (the AnviDev rule — "No Co-Authored-By"). Mitigation: COMMIT_TEMPLATE single-quoted heredoc never includes a Co-Authored-By line; pre-PR `git log --format='%(trailers:key=Co-Authored-By)' main..feat/20-20-tokenizer-whitespace` is empty.
    Second error: Claude merges the PR (a violation of AnviDev step 6). Mitigation: action 10 STOPS at PR-up.
    Third error: post-merge, the artifact verification surfaces a regression (the merge race / squash mangled commits). Mitigation: action 8's recipe is verbose and recorded in SUMMARY.md for the user to execute; if any grep returns 0 or any test count regresses on real main, that is a P70-class hetvabhasa entry — file a hotfix issue immediately.
    Fourth error: the Ground Truth doc edit is OUT of `git diff` scope (the doc lives at `~/.anvideck/projects/struCode/ref/GROUND_TRUTH_SIGNAL_MJS.md`, not in the repo); without explicit reminder this is easily forgotten. Mitigation: action 5 is a separate step with file path written out; SUMMARY.md "Ground-Truth-doc disposition" section records the disposition.
  </pre_mortem>
  <verify>
    1. `20-20-SUMMARY.md` exists with status / verdict / per-wave deltas / FROZEN Outcome-1 scope / V-3 allow-list / catalogue updates / fixtures-added / Ground-Truth-doc disposition / deferred backlog (#143 + #153 LAST-WINS evidence + #156/#149/#147/#158-other-residuals) / Cognitive Discoveries / post-merge recipe.
    2. `.anvi/{krama,hetvabhasa,vyapti}.md` have 20-20 addenda with ORIGIN/WHY/HOW provenance; PV54 explicit NOT-triggered note present.
    3. `~/.anvideck/projects/struCode/ref/GROUND_TRUTH_SIGNAL_MJS.md` has the new "Parser/Evaluator Pipeline" section with the 7 `file:line` citations.
    4. `git log --format='%(trailers:key=Co-Authored-By)' main..feat/20-20-tokenizer-whitespace` is empty.
    5. PR opened on GitHub; body has `closes #159` exactly once; counts + gate satisfied in the PR description.
    6. Self-review pass recorded in OBSERVATIONS (the AnviDev step-5 critical analysis).
    7. Claude DID NOT merge; final message is the checkpoint to the user.
  </verify>
  <done>
    20-20-SUMMARY.md written; `.anvi/{krama,hetvabhasa,vyapti}.md` updated with 20-20 addenda + provenance; GROUND_TRUTH_SIGNAL_MJS.md extended with the Parser/Evaluator Pipeline section; single non-stacked PR `feat/20-20-tokenizer-whitespace` → main opened with `closes #159`; post-merge artifact verification recipe recorded; Claude stops at PR-up; commit via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE DEPENDENCY GRAPH (acyclic)

```
0-1 (Wave 0 — re-confirm Outcome 1; FROZEN scope)
  └─→ A-1 (Wave A — PV49-extend splitRootAndChain; cases B/D/E flip)
         └─→ B-1 (Wave B — 2 permanent CI fixtures)
                 └─→ V-1 (Wave V-1 — dual gate measurement)
                          └─→ V-3 (Wave V-3 — cross-wave full-corpus STOP gate)
                                    └─→ V-4 (Wave V-4 — SUMMARY + catalogues + GT extension + PR)
```

No parallel waves (each strictly serial because each builds on the prior's observation record). No re-converging branches.

## GOAL-BACKWARD VERIFICATION CHAIN

1. **GOAL = LOCKED D-04 dual gate:** crit-1 (Wave-B canonical-positive fixture asserts whole-program structured on `-G2drHRNFueu`) AND crit-2 (fresh ≥ 98.0% AND ≥ 96.0% floor) AND no-bar-lowering on the SAME merge commit.
   → **V-1** asserts both crit on the same head; **V-3** verifies the cross-wave silent-drift guard; **V-4** opens the PR for user-merge review.
2. **For crit-1 to pass,** the Wave-B fixture's parity snapshot must assert structured — which requires `-G2drHRNFueu` actually flipping structured, which requires `splitRootAndChain` to tolerate the whitespace fence → **A-1** ships the PV49-extension at `pS:2521-2531`; **B-1** vendors the fixture.
3. **For the canonical positive to flip,** the inherited `sMatch` regex (`/^(?:s|sound)\s*\(\s*"…/`) must match — which requires the splitter to include the consumed whitespace in the root slice → **A-1**'s `skipWhitespaceAndLineComments` call + the `i = afterIdent` restore-arm is the mechanism. The inherited `sMatch` arm + Track wrapper do the rest.
4. **For the upstream-grounded verdict to be honest (not over-fit),** the fix must MIRROR the upstream's pure-JS-eval whitespace tolerance → RESEARCH §3 grounded the `acorn.parse` + `Function(body)()` mechanism at file:line; **V-4** records the Ground Truth extension to GROUND_TRUTH_SIGNAL_MJS.md.
5. **For no silent loc-fidelity drift,** the per-file STOP gate runs per-wave (Wave A, Wave B) + cross-wave (V-3) → the PV49 substrate's byte-additive whitespace consumption carries by construction (RESEARCH §5.5 grounded: the consumed whitespace is included in the root slice; the original source string is never mutated; remaining offsets unchanged).
6. **For permanent regression coverage,** 1 canonical positive + 1 negative-control fixture (Wave B V-2 cadence) → 2 fixtures vendored, snapshot-captured, auto-discovered.
7. **For the lessons to compound into 20-21+,** SUMMARY + catalogue provenance + Ground Truth extension + single non-stacked PR + post-merge artifact verification → **V-4**. If 20-20 is a clean-run (0 PK18 re-poses), that fact strengthens the 20-19 substrate-works hypothesis — document explicitly.

## DOES THIS PLAN ACHIEVE THE GOAL? (closing check — anchored on D-04)

1. **Crit-1 (HARD) is reachable:** the RESEARCH §2 5-cell probe (RUN'd by the researcher) + the §5.6 Chesterton scan + the §5.5 fix-shape sketch together prove the PV49-extension flips case B and case E to structured; the §5 mechanism is grounded against `parseRoot.sMatch` at pS:1347 (the receiving end); Wave A's probe re-observes (action 6 (iii)). The fixture's parity snapshot is auto-captured on first run.
2. **Crit-2 (HARD) is reachable:** baseline 48/50 = 96.0%; `-G2drHRNFueu`'s flip alone = 49/50 = 98.0%; any bonus from a coincidental exemplar flip (the 20-19 `-1j62z5xjyCN` precedent) only goes UP. The must-not-regress 96.0% floor holds by construction (the fix is strictly permissive on the identifier-paren boundary; no existing structured-path outcome regresses per the §5.6 Chesterton scan + the `i = afterIdent` restore-arm + Wave-A action 6 (ii)'s 5 false-positive probes).
3. **D-01 (Wave-0 factoring outcome) is RE-CONFIRMED on the fresh branch:** action 4 re-runs the §2.2 probe; ANY divergence → STOP.
4. **D-02 (upstream-grounded fix-shape) is HONEST:** the upstream verdict TOLERATES is grounded at `acorn.parse` + `Function(body)()` (RESEARCH §3); the fix-shape choice is constrained to walker-tolerance (NOT curated-regex / NOT source-pre-process — RESEARCH §3.4 rejects those); Wave A action 3 implements the constrained walker-tolerance.
5. **D-03 (multi-top-level) stays DEFERRED:** the §4 LAST-WINS upstream verdict is recorded in OBSERVATIONS + SUMMARY for any future #153 phase; 20-20 does NOT change IR shape for sibling top-level expressions; the FIRST-WINS-on-our-parser disposition for case C is unchanged.
6. **D-04 dual-gate is enforced:** V-1 measures both crit on the same head; the must-not-regress 96.0% floor is encoded; no bar-lowering.
7. **P70 is the spine:** Wave 0 RE-OBSERVES; every Wave-A probe + Wave-V measurement is EXECUTED + recorded verbatim; PK18 STOP applies at every gate.
8. **PV49 carries by construction:** the byte-additive whitespace-consumption substrate is the proof; the V-3 cross-wave STOP gate observes.
9. **PV54 does NOT trigger:** no new top-level PatternIR tag; the V-4 catalogue addendum records this explicitly.
10. **Operational discipline:** COMMIT_TEMPLATE single-quoted heredoc (never `-m` with backticks/`$()` under zsh); P68 build hygiene with INCREASE-by-1 grep on `skipWhitespaceAndLineComments` (the new caller); per-file loc-fidelity STOP gate per-wave + cross-wave; single non-stacked PR → main; `closes #159` only; Claude never merges; post-merge artifact verification recipe recorded.

The plan achieves the D-04 GOAL: it widens the recogniser surface narrowly + via the EXISTING PV49 substrate's new 5th caller, restores the structured projection for `-G2drHRNFueu` via the inherited `sound("…")` recogniser arm, ships per-class permanent regression coverage, enforces the cross-wave silent-drift guard, measures fresh parity ≥ 98.0% with the 96.0% must-not-regress floor, records the upstream Ground Truth for future-phase REF wires (and #153 in particular), and routes every falsified premise through PK18 to backlog — never a push-through.

## EXECUTOR NOTES

- AnviDev: issue → branch → fix → test → observe → PR → self-review → merge. Issue #159 is filed (Wave-0 re-confirmed). Branch FIRST (never code on main): `git checkout -b feat/20-20-tokenizer-whitespace` from `main` (HEAD `a150889`). Single non-stacked PR → main; Claude NEVER merges (stop at PR-up; checkpoint to the user).
- `.anvi/` + `.planning/` are gitignored — `git add -f` for catalogue + planning artifacts. NO Co-Authored-By. Commits use gitmoji + Problem/Fix body via the COMMIT_TEMPLATE below.
- **COMMIT_TEMPLATE (structural — every task's commit step says "Commit via COMMIT_TEMPLATE"; do NOT reconstruct from memory; `feedback_commit_msg_heredoc.md` zsh-substitution trap recurred 2× in 20-16):**
  ```
  COMMIT_TEMPLATE:
  git -c commit.gpgsign=false commit -q -F - <<'MSG'
  <gitmoji> <type>(<scope>): <summary>

  Problem: <what/why>
  Fix: <what/how>
  MSG
  ```
  NEVER `git commit -m "…"` with backticks or `$()` — zsh executes the substitution and silently strips it. The single-quoted heredoc (`<<'MSG'`) + `-F -` is the only sanctioned form.
- **GitHub closes-keyword limit:** the PR body uses `closes #159` ONLY (GitHub honors ONE keyword's worth of auto-close — the 20-15/16 lesson). No Class-A bonus closes this phase per Outcome 1; #153 stays backlog with the LAST-WINS evidence in OBSERVATIONS + SUMMARY.
- **P68 build hygiene per editor-src commit:** one-shot `pnpm --filter @stave/editor build`, then `grep -c <anchor> packages/editor/dist/index.js` BEFORE `git commit`. **The anchor MUST be a STRING/SYMBOL LITERAL (minification-stable) — NOT a comment (stripped), NOT a parameter name (minifiers rename params).** This phase's anchors: `splitRootAndChain` (exported function name; tsup `keepNames` preserves) + `skipWhitespaceAndLineComments` (export; the gate is INCREASE-by-1 vs pre-fix baseline — NOT just > 0) + `findMatchingParen` (defensive; unchanged count) + `afterIdent` (local variable, informational — may be minified away). The known pre-existing `@strudel/mondo` TS7016 may fire on the DTS step — verify it is THAT error, not new. **Start `pnpm --filter @stave/editor dev` (tsup --watch) BEFORE editing `packages/editor/src/`** (memory `feedback_editor_watch_mode.md` / P66 / PV48 — workspace exports via dist/); P68: a DTS-build failure terminates the watch ENTIRELY (does NOT degrade to JS-only) — verify the watch is ALIVE, not just started.
- **Per-file loc-fidelity STOP gate (per-wave + cross-wave V-3):** every parity-UNCHANGED corpus file's loc-fidelity snapshot diff MUST be EMPTY; the parity-CHANGED set ⊆ the enumerated allow-list = {2 Wave-B fixtures}. The fix is PV49 loc-additive by construction (RESEARCH §5.5 + §6.1 grounded: byte-additive whitespace consumption; no source mutation) — verify per-wave; ANY non-allow-list movement = STOP. The allow-list is NOT defensively extended (the 20-18/20-19 V-3 lesson; the PV49 mechanism is the proof, not the allow-list).
- **vite-node import path for any local test/script importing `parseStrudel` directly:** use the deep path `./ir/parseStrudel` from `packages/editor/src/` (the Wave-0 + Wave-A probes live there) or `../../../editor/src/ir/parseStrudel` from `packages/app/tests/parity-corpus/` (the `@stave/editor` barrel crashes standalone node via `@strudel/draw → gifenc`). Mirror `_bakery-classify.spec.ts` / `_proto-d01.spec.ts` / the 20-19 `_wave0-classify.spec.ts`.
- **The parser is a STRUCTURAL MATCHER (D-02 matcher line — PRESERVE exactly):** the new walker tolerance is purely permissive — it consumes whitespace + `//` line comments between identifier and call-site `(` and includes the consumed bytes inside the returned root slice. It does NOT mutate the source string; does NOT add new state; does NOT introduce a new IR tag; does NOT widen the parser's accepted language beyond what upstream Strudel evaluates (acorn + Function-eval already tolerates this). The matcher line holds.
- **PK18 HARD-GATE cascade discipline (the P70 spine — the most likely trigger is the Wave-0 V-1 stdout diverging from RESEARCH §2.2, or the Wave-A exemplar round-trip NOT flipping case E, or crit-2 regressing below 96.0%):** a gate falsifying a premise → STOP, record VERBATIM in `20-20-OBSERVATIONS.md`, re-classify, re-pose the invalidated LOCKED decision (D-01 outcome / D-02 fix-shape / D-04 floor) to the user, reframe — NEVER push through, NEVER add a second workaround, NEVER lower the bar. New gap classes → backlog issues (issue-before-fix) → 20-21+, NOT fixed this phase.
- **Grounding Check (P69):** RESEARCH §3 grounded the upstream TOLERATES verdict at `acorn.parse` + `Function(body)()` (Codeberg pin `f73b3956`); RESEARCH §4 grounded the LAST-WINS upstream verdict for #153 (deferred to a future phase but recorded here for inheritance); RESEARCH §5 grounded the bug surface at `parseStrudel.ts:2521-2531`. V-4 records the Ground Truth extension to GROUND_TRUTH_SIGNAL_MJS.md.
- **PV54 NOT triggered:** no new top-level PatternIR tag. The PV53/PV54 consumer-audit obligation is dormant. Confirm explicitly in V-4 catalogue addendum.
- **Post-merge artifact verification (NOT the badge):** `git merge-base --is-ancestor <PR-head-sha> origin/main` exit 0; HEAD on origin/main == merge commit; `dist/index.js` `splitRootAndChain` grep > 0 + `skipWhitespaceAndLineComments` grep INCREASED-by-1 vs pre-fix; editor 1627 + app 413 counts hold; fresh parity:bakery N=50 still ≥ 98.0%. The EXACT 20-14 failure class (`feedback_stacked_pr_base_retarget.md`) — kept explicit even for a single non-stacked PR. V-4 records the recipe; the USER executes it post-merge.
- **String quoting** (memory `feedback_strudel_quote_style.md` / P62 / PV44): the Strudel transpiler converts double-quoted strings to mini Patterns; use SINGLE quotes for chain-method string-id args in any fixture body — though the canonical positive fixture this phase uses double-quotes INSIDE `sound(…)` (because that IS the mini-pattern; the double-quote is intentional and matches the upstream Strudel idiom). The P62 single-quote rule applies to `.p('name')` and friends, NOT to the `sound("hh sd")` mini-pattern body.
- **What's genuinely new in 20-20:** the PV49-extension at the identifier-to-paren boundary inside `splitRootAndChain` (a 5th caller of `skipWhitespaceAndLineComments` at a new boundary class — the existing 4 callers all operated at inter-token boundaries within a chain or argument list; this one operates at the call-site boundary itself, the canonical idiomatic shape `ident WS (`); the `i = afterIdent` restore-arm that preserves today's bare-identifier-root disposition (the §5.6 Chesterton scan's structural mitigation); the Ground Truth doc extension covering the parser/evaluator pipeline (the substrate for any future phase that touches the same surface — including #153). 20-20 is likely a *clean-run* phase (0 PK18 re-poses) replicating the 20-19 substrate-works hypothesis — that fact is itself a 20-1x cadence milestone worth cataloguing in V-4.
