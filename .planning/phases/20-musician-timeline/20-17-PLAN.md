---
phase: 20-17
title: D-01 binding resolution — pervasive-context substitution + bounded least-fixpoint
created: 2026-05-18
revised: 2026-05-19 (plan-checker NEEDS-REVISION — 7 fixes applied; D-02 BLOCKER resolved by CONTEXT D-02 CORRECTION)
depends_on: 20-16 MERGED (PR #154, merge aaae98c — main HEAD aaae98c, editor 1564/1564 + parity-corpus 32/32 + loc-fidelity 32/32 green, real-world Bakery 80.0% N=50 sha f73b3956 stamp 2026-05-18T14-34-02-237Z)
issues: ["#140", "#141"]
context: .planning/phases/20-musician-timeline/20-17-CONTEXT.md (D-01/D-03 LOCKED; D-02 SUPERSEDED by the "D-02 CORRECTION (2026-05-19)" section — sacrosanct)
research_seed: .planning/phases/20-musician-timeline/20-16-OBSERVATIONS.md (WAVE A-0 4-gap source-scan; replaces the falsified "primitive already built" premise)
upstream_pin_sha: f73b395648645aabe699f91ba0989f35a6fd8a3c
baseline_parity: "80.0% (40/50, N=50, 2026-05-18T14-34-02-237Z, sha f73b3956)"
target_parity: "≥85.0% (fresh PK17-step-6 re-measure)"
waves: 6 (0 vendor-oracle, A signature-refactor+byte-gate, C G1+G2, D G3 literal-union, E fixpoint+occurs-check, Verification)
---

# Phase 20-17 — D-01 binding resolution done right

## GOAL (goal-backward anchor — anchored on the D-03 DUAL gate)

Real-world Bakery parity moves from the 20-16 baseline (80.0%, 40/50, N=50,
sha `f73b3956`, stamp `2026-05-18T14-34-02-237Z`) to **≥ 85.0%** on a FRESH
PK17-step-6 `pnpm parity:bakery --n 50` re-measure, AND the canonical
D-01-eligible transitive repro `--LsnlgQ6osk` grounds STRUCTURED in
production (`parseStrudel(<verbatim repro>) → unwrap Track('d1', body)`,
`body.tag !== 'Code' || body.via !== undefined`). **BOTH gates required** —
the D-03 dual criterion. There is NO bar-lowering escape: if criterion 2
fails, the phase does NOT pass; the residual fallbacks are triaged into
named classes and filed as backlog (a future 20-18), never patched in
20-17 (the 20-16 4×-cascade lesson).

D-01's matcher line — *"we compute the least fixpoint of binding
**substitution** (pure term-rewriting); we never **evaluate** a term"* —
becomes a derived property of the bounded least-fixpoint, not a
stipulated fence. The kept γ-3 fences (dup-key, opaque-RHS, shape,
BINDING_RE LHS filter) remain exactly. PV49 loc-additivity holds at every
recursion site. PV50 hazard (per-evaluate engine state) is explicitly
avoided by D-01 G4 = optional-arg threading, not module-level mutable
context.

Every wave below chains to this GOAL; V-1 measures the D-03 dual gate;
V-2 locks the canonical hard case as permanent CI; V-3 is the cross-wave
per-file loc-fidelity STOP gate.

> **D-03 AMENDMENT (2026-05-19 — provenance pointer, see CONTEXT.md):**
> criterion 1 re-anchored on EVIDENCE to dual `_72eEl7NwK9e` AND
> `_LHtBlF8peGC` STRUCTURED in production (Wave E E-1 empirical
> falsification of the `--LsnlgQ6osk` cascade premise — `az2` is
> opaque-by-shape, NOT binding-blocked; deferred to 20-18 backlog).
> Criterion 2 (≥85% Bakery parity) UNCHANGED. No bar-lowering.

## CONTEXT — what 20-16 established (Chesterton's existence check)

The 20-16 Wave 0 segmenter fixes (#148/#150/#151/#152) and the
strip+root closures (#142/#143/#144) merged on main via PR #154 (merge
`aaae98c`). The Wave A-0 source-scan (`20-16-OBSERVATIONS.md` §"WAVE A-0
— D-01 mechanism investigation") falsified CONTEXT's "primitive already
built / minimal fixpoint" premise: `bindings` reaches only 2 narrow
sites today. The 4 gaps are mapped, file:line-cited, and re-verified
against current source DURING THIS REVISION (the source drifted once
post-20-16; every `pS:` below was re-confirmed against the live
`packages/editor/src/ir/parseStrudel.ts` on 2026-05-19). **Do NOT
relitigate.** Read before extending.

### Current `bindings` reach (RE-VERIFIED 2026-05-19 — live source)

REACHES (substitution works today, leave unchanged):
- `parseExpression:869-873` — WHOLE-expression bare ident: `if (bindings)`
  then `if (/^[A-Za-z_$][\w$]*$/.test(bareId) && bindings.has(bareId))
  return bindings.get(bareId)`. (Covers `stack(p1)` single-arg unwrap.)
- `parseStrudel.ts:1099` — the `stack(a,b,c)` arg loop:
  `parseExpression(a.value, innerAbsOffset + a.offset, undefined,
  bindings)`. (The γ-3 / #134 case `stack(boundId, boundId)`.)
- The D-01 entry point: `parseStrudel.ts:542-544` — `const bound =
  buildBindingMap(stripped.body, stripped.offset)`, then `const inner =
  parseExpression(bound.finalExpr, bound.finalOffset, undefined,
  bound.bindings)`. (parseStrudel already threads `bound.bindings` into
  the final-expr parse — the WIRE the signature refactor must reach.)

DOES NOT REACH (the 4 gaps this phase closes):
- **G1 (chain-arg).** `parseStrudel.ts:1052-1056` — the loose recursive
  arm for `note|n|s|sound|mini(<chained inner>)` calls
  `parseExpression(innerTrimmed, innerAbsOffset, callerIsSample)` with NO
  4th `bindings` arg. `sound(rp1)` with `rp1` resolved → still bareCode.
- **G2 (root-ident).** `parseRoot` (signature pS:927-940, arms pS:957–
  1100) has note/n/s/sound/mini/loose/stack arms but **no** "bare ident
  bound in `bindings` → splice bound IR as root, run chain" arm.
  `chords2.rootNotes(2).note()` splits to root=`chords2` + chain;
  `parseRoot('chords2', …, bindings)` matches no arm → bareCode.
- **G3 (literal RHS opaque).** `var numChords = 4`: `buildBindingMap`
  (pS:449) parses RHS `4` via `parseExpression(rhs, rhsOffset)` (pS:477,
  NO bindings arg today) → bareCode → opaque-RHS fence at pS:478-482
  fires (`return null`) → whole program bails. A number / quoted-string
  literal IS a valid Strudel value (Strudel code IS JavaScript;
  `.slow(4)` works), not opaque.
- **G4 (method-arg / pervasive recursion).** `parseExpression:911`'s
  `applyChain(rootIR, chain, chainOffset)` call has no `bindings` arg;
  `applyChain` (pS:1213, signature `(ir, chain, baseOffset = 0)`) does
  not accept one; `parseTransform` (pS:1829, signature `(transformStr,
  defaultIr, baseOffset = 0)`) does not accept one; the inner
  `parseExpression` recursions inside `parseTransform` do not thread it.
  So `.slow(numChords)` / `.scale(chords2.slow(numChords))` inside any
  chain NEVER substitutes. This is the pervasive-context refactor.

### Kept γ-3 fences (Chesterton — PRESERVE every one, exactly)

| Fence | Line (RE-VERIFIED 2026-05-19) | Bails on | Disposition |
|---|---|---|---|
| `< 2` statements | pS:456 | need ≥1 binding + 1 final expr | KEEP |
| Dup-key / redeclaration | pS:473 | `bindings.has(name)` | KEEP — functional-dependency (D-02 dissolved by order-independence but the check still catches genuine same-name redecl) |
| Opaque-RHS | pS:478-482 | RHS → bare Code (`tag==='Code' && via===undefined`) | KEEP — byte-identical; becomes the occurs-check terminal POST-fixpoint (Wave E) |
| Final-expr shape | pS:485 & pS:488 | `finalIdx===-1` OR `finalIdx!==stmts.length-1` | KEEP — orthogonal to resolution order |
| BINDING_RE LHS filter | (regex, top of file) | LHS not a bare ident → destructuring / arrow / etc. fall to graceful Code | KEEP |

D-02 (the 2026-05-19 CORRECTION) widens the per-binding RHS
classification (number/string literals → **Code-with-via via the NEW
additive `{ literal:true; raw }` union arm**, structured per P67). The
opaque fence still fires on truly bare Code (arrow-fn RHS, `${}`
template, function-call RHS — all `via === undefined`). D-01 adds the
fixpoint iteration AROUND the fence — does not remove it. **The fence
test (`tag === 'Code' && via === undefined`) is byte-UNCHANGED**: a
literal sets `via = { literal:true, raw }` → `via !== undefined` →
already on the "structured, don't bail" side with ZERO fence-code edit
(P67 tri-state preserved exactly).

### PV49 substrate (RE-VERIFIED 2026-05-19)

`skipWhitespaceAndLineComments` lives at **pS:820** (was 730 pre-20-16;
moved when SEG-1/#150/#151/#152 landed; confirmed 2026-05-19). Route any
new inter-token scan through it. Offset arithmetic stays additive: a
consumed-prefix length adds to the element's base offset. The
20-16-PV49 span addendum (vyapti.md:1496-1508) covers the parseRoot
paren-string arm; this phase's G2 bound-ident-root arm joins that span.

## A-WAVE BYTE-IDENTICAL SPLIT (the Option-b pattern, reused verbatim from 20-16)

Wave A is split into TWO commits inside ONE wave (the 20-16 A-1↔A-2
boundary, internalized — there is NO separate observe-only wave; the
plan-checker correctly flagged a standalone "observe the wire" wave as
decorative because the real behaviour change is C/D/E, not the
signature refactor):

- **A-1 (the SIGNATURE refactor, byte-identical by construction).** Add
  `bindings?: ReadonlyMap<string, PatternIR>` as an optional argument to
  `applyChain`, `parseTransform`, and every internal recursion site that
  currently lacks it. Existing callers omit it (default undefined). The
  D-01 entry point at pS:544 already passes `bound.bindings`; once the
  recursion accepts it, that map flows THROUGH the now-accepting
  recursion automatically. **A-1's own verify INCLUDES the
  byte-identical gate AND the post-refactor proto re-run = 2/6
  unchanged** — the refactor is byte-unchanged for the corpus + editor
  suites (it is a pure signature widen; `undefined` everywhere existing
  callers omit), and the proto's verdict is unchanged from Wave-0 (the
  WIRE is now in place but the chain-arg / root-ident / literal / cyclic
  positions still need C/D/E). "Any byte diff = STOP" — correct by
  construction.

Why no separate "observe" wave: the proto re-run that confirms 2/6
unchanged is FOLDED into A-1's verify. There is no A↔B attribution
ambiguity to resolve with a standalone wave — A-1 is the byte-identical
refactor whose gate is "byte-unchanged + proto 2/6 unchanged"; C/D/E are
the behaviour changes, each individually committable + observable. This
collapses the prior decorative Wave B (the plan-checker MAJOR, dim 3/4).

## PRE-MORTEM (the most-likely wrong-but-green outcome)

**A pervasive-threading change passes the parity-corpus snapshots + the
editor suite but silently shifts absolute offsets so runtime `loc` /
click-to-source / highlight-on-event breaks.** Same axis as 20-16's
pre-mortem; new surface (every G4 recursion site).

`parity.test.ts` strips `loc` via `normalize.ts` — it is BLIND to offset
drift BY CONSTRUCTION. The complementary gate is `loc-fidelity.test.ts`.
**Its actual mechanism (read line-by-line during this revision —
loc-fidelity.test.ts:97-116):** for each `*.strudel` in the corpus dir
it does `code = fs.readFileSync(file)`, `ir = parseStrudel(code)`,
`collectLeafLocs(ir, code, locs)` where `collectLeafLocs` does
`src.slice(start, end)` with `src === code` (THE SAME parsed string —
there is NO separate "original source"; the slice is from the one input
string) and snapshots `{tag, text}` per file. It has NO
parity-classification awareness and NO allow-list — it is a pure
per-file snapshot of every loc's sliced token text. **This is why γ-3's
existing splice passes today and why the fixpoint's N splices are
covered — proven in E-1's pre_mortem with the file:line citation.**

**THE STOP gate (mandatory, every wave that touches the parser —
PER-FILE correlation, never the corpus aggregate):** for every corpus
file whose parity classification is UNCHANGED across the phase, that
file's loc-fidelity snapshot diff MUST be EMPTY. A loc-fidelity diff on
a parity-unchanged file = silent offset drift = STOP, do not commit,
bisect which wave (A signature, C G1+G2, D G3 union, E fixpoint)
re-based an offset, fix, re-run from that wave's regression oracle. A
file whose parity classification legitimately CHANGED (bare-Code →
structured) is expected to move its loc snapshot — enumerated as the
phase's allow-list (Wave-0 baseline ∪ {V-2 fixture(s)}; any other
parity-changed file is treated as drift).

Secondary pre-mortem (silent partial reach — D-01 G4 specific): forget
to thread `bindings` on a recursive site → the substitution only
reaches some positions; the bug is visible only on a real binding using
the un-threaded path. Mitigation: D-03 hard criterion 1 (`--LsnlgQ6osk`
grounds STRUCTURED) uses chain-arg + root-ident + nested method-arg
paths together — silent partial reach is OBSERVED, not inferred. If
post-Wave-E the proto shows `--LsnlgQ6osk` still bareCode at any
iteration, the un-threaded site is named in the diagnostics and fixed.

Tertiary pre-mortem (D-02 literal regex over-matches): a non-literal
RHS that LOOKS like a literal slips past the regex and is classified
non-opaque. Mitigation: STRICT anchored regex (`^-?\d+(\.\d+)?$` for
numbers; `^"[^"]*"$` for double-quoted; `^'[^']*'$` for single-quoted;
NO internal `${}`, NO escape decoding, NO operators — `4 + 1`, a
`${}`-template, any expression all stay bareCode and the opaque fence
fires correctly).

Quaternary pre-mortem (Wave-E fixpoint non-termination): bug in the
bound or the progress check loops forever. Mitigation: hard bound
`iter < descs.length` where N = total binding-descriptor count; the
cyclic synthetic (`const a=b; const b=a; stack(a,b)`) MUST bail to
graceful Code via the post-fixpoint pending check (occurs-check terminal
— both stay pending → `return null`). Already proven by the preserved
`/tmp/proto-d01-fixpoint.spec.ts` (`20-16-OBSERVATIONS.md` "SYNTHETICS"
block).

Quinary pre-mortem (D-02 consumer ripple — the phase's PRIMARY risk
surface): a `via.` reader or a `tag === 'Code'` wrapper-shape assumer
that does not expect the new `{ literal:true; raw }` arm reads
`via.inner`/`via.method` on a literal and silently produces wrong
output (a P67 silent-wrong). This is given its OWN verification (D-2's
mandatory consumer audit + a round-trip acceptance test).

Build hygiene (P68/PV48): editor watch is UNRELIABLE (tsup --watch dies
silently on the pre-existing `@strudel/mondo` DTS error). Per
editor-src commit: one-shot `pnpm --filter @stave/editor build`, then a
build-hygiene grep against a MINIFICATION-STABLE anchor (NOT a parameter
name — minifiers rename params; NOT a comment — comments are stripped;
see the keepNames decision in EXECUTOR NOTES) MUST pass BEFORE
`git commit`.

---

## WAVE 0 — Vendor the gate oracle (make the regression oracle durable)

Goal-link: the preserved Task-1 prototype + 6 repros live in `/tmp` and
may be lost on reboot. Vendoring is the single move that converts the
ephemeral oracle into a CI-runnable, branch-scoped regression wall for
every subsequent wave. Without this, Wave A–E have no observable gate;
with this, every wave's behaviour change has a `pnpm test:proto` that
prints a per-repro verdict + per-iteration diagnostics.

<task id="0-1" type="auto" depends_on="">
  <name>Vendor /tmp/proto-d01-fixpoint.spec.ts + 6 repros + vitest.proto.config.ts; capture baseline ON MAIN (no D-01 impl yet) = 2/6 structured</name>
  <read_first>
    /tmp/proto-d01-fixpoint.spec.ts (the preserved Task-1 prototype — vendors copies of `splitTopLevelStatements`/`BINDING_RE` byte-for-byte and imports `parseExpression` + `parseStrudel` from editor SOURCE under vite-node);
    /tmp/repro__LsnlgQ6osk.strudel /tmp/repro_1j62z5xjyCN.strudel /tmp/repro_72eEl7NwK9e.strudel /tmp/repro_CyO42BOyp5a.strudel /tmp/repro_L13nBhrqGR_.strudel /tmp/repro_LHtBlF8peGC.strudel (6 #141 repros distilled in 20-16);
    packages/app/vitest.bakery.config.ts (the maintainer-only `mergeConfig`+`include` pattern to mirror);
    packages/app/tests/parity-corpus/_bakery-classify.spec.ts:11-19 (the proven vite-node import of editor SOURCE via `../../../editor/src/ir/parseStrudel`);
    .planning/phases/20-musician-timeline/20-16-OBSERVATIONS.md (the post-Wave-0-bundle baseline — score 2/6 production-structured: `-CyO42BOyp5a` + `-L13nBhrqGR_`)
  </read_first>
  <files>
    packages/app/tests/parity-corpus/_proto-d01.spec.ts (new — vendored from /tmp/proto-d01-fixpoint.spec.ts, name prefixed `_` to match `_bakery-classify.spec.ts` maintainer convention);
    packages/app/tests/parity-corpus/bakery-runs/repro__LsnlgQ6osk.strudel (new);
    packages/app/tests/parity-corpus/bakery-runs/repro_1j62z5xjyCN.strudel (new);
    packages/app/tests/parity-corpus/bakery-runs/repro_72eEl7NwK9e.strudel (new);
    packages/app/tests/parity-corpus/bakery-runs/repro_CyO42BOyp5a.strudel (new);
    packages/app/tests/parity-corpus/bakery-runs/repro_L13nBhrqGR_.strudel (new);
    packages/app/tests/parity-corpus/bakery-runs/repro_LHtBlF8peGC.strudel (new);
    packages/app/vitest.proto.config.ts (new — `mergeConfig(base, { test: { include: ['tests/parity-corpus/_proto-d01.spec.ts'] }})` mirroring vitest.bakery.config.ts);
    packages/app/package.json (add `"test:proto": "vitest run --config vitest.proto.config.ts"`);
    .planning/phases/20-musician-timeline/20-17-OBSERVATIONS.md (NEW — append the Wave-0 baseline capture)
  </files>
  <action>
    1. **Branch first.** `git checkout -b feat/20-17-d01-pervasive` from `main` (current HEAD `aaae98c`, the 20-16 merge). Branch FIRST (never code on main, AnviDev).
    2. **Vendor the prototype:**
       - `cp /tmp/proto-d01-fixpoint.spec.ts packages/app/tests/parity-corpus/_proto-d01.spec.ts`
       - Adjust the spec to LOAD the 6 repros from `bakery-runs/repro_*.strudel` via `fs.readFileSync(path.join(__dirname, 'bakery-runs', f), 'utf8')`. Keep the byte-for-byte copies of `splitTopLevelStatements`/`BINDING_RE`/the synthetic gates.
       - Keep the spec's DIAGNOSTIC `console.log` lines (per-iteration `rhs= -> tag=… bareCode=…` + `post-fixpoint resolved=[…] pending=[…]`). They are the wave-by-wave observation evidence; do NOT silence them.
    3. **Vendor the 6 repros:** `cp /tmp/repro_*.strudel packages/app/tests/parity-corpus/bakery-runs/`. Six files. VERBATIM Bakery code; do not modify.
    4. **Create `packages/app/vitest.proto.config.ts`** — copy `vitest.bakery.config.ts` verbatim; replace `_bakery-classify.spec.ts` with `_proto-d01.spec.ts` in the `include` array. CI `vitest.config.ts` `include` is NOT widened (maintainer-only discipline preserved).
    5. **Add `test:proto` to `packages/app/package.json`**: `"test:proto": "vitest run --config vitest.proto.config.ts"`.
    6. **Capture baseline on MAIN behaviour (no D-01 impl yet).** `pnpm --filter @stave/app test:proto`. Expected pre-D-01 score per `20-16-OBSERVATIONS.md` = **2/6 structured**: `-CyO42BOyp5a` + `-L13nBhrqGR_`; the other 4 (`--LsnlgQ6osk`, `-1j62z5xjyCN`, `-72eEl7NwK9e`, `-LHtBlF8peGC`) are Code.
    7. Paste the verbatim test:proto stdout into `20-17-OBSERVATIONS.md` under §"Wave-0 baseline — proto on main behaviour". This is the regression oracle for every subsequent wave (Wave A-E MUST monotonically improve from 2/6 — never regress below it).
    8. **Commit via COMMIT_TEMPLATE** (see EXECUTOR NOTES — the heredoc form is structural, not memory-dependent). `git add -f packages/app/tests/parity-corpus/_proto-d01.spec.ts packages/app/tests/parity-corpus/bakery-runs/repro_*.strudel packages/app/vitest.proto.config.ts packages/app/package.json .planning/phases/20-musician-timeline/20-17-OBSERVATIONS.md`.
  </action>
  <ownership>
    `_proto-d01.spec.ts` owns the per-repro classification + per-iteration diagnostics (the wave-by-wave regression oracle). `bakery-runs/repro_*.strudel` owns the verbatim Bakery input (auditable via the named hash). `vitest.proto.config.ts` owns the maintainer-only test-run scope (NOT in CI `vitest.config.ts` include). `20-17-OBSERVATIONS.md` owns the durable per-wave evidence (the prototype's stdout is ephemeral; the appended capture is the audit trail).
  </ownership>
  <lifecycle>
    Pre-impl gate for Waves A-E. Synchronous, run-once-per-wave maintainer tool. No source change to `parseStrudel.ts` — vendoring + baseline capture only. The proto's import of editor SOURCE is via the proven `_bakery-classify.spec.ts:11` deep-path mechanism (the `@stave/editor` barrel crashes standalone-node via `gifenc`; vite-node bypasses that).
  </lifecycle>
  <pre_mortem>
    Most likely error: the spec's relative import path `../../../editor/src/ir/parseStrudel` is correct from `packages/app/tests/parity-corpus/` (three `..`s up to `packages/`), but a careless copy could put it at the wrong depth. Mitigation: open the file post-copy, verify the import resolves (`pnpm --filter @stave/app test:proto` either runs or fails on import — observation, not inference).
    Second error: vendoring the 6 repros silently mangles them (line endings) → the proto's hash-based identity drifts. Mitigation: `diff /tmp/repro_X.strudel packages/app/tests/parity-corpus/bakery-runs/repro_X.strudel` empty for each; the baseline 2/6 capture confirms identity by reproducing the recorded result.
    Third error: the baseline run ≠ 2/6, falsifying CONTEXT's recorded baseline → oracle broken or CONTEXT wrong; STOP, do not advance to Wave A.
    Fourth error: editor 1564 + parity-corpus 32/32 + loc-fidelity 32/32 must stay GREEN; if not, vendoring broke test discovery (proto picked up under the wrong config) — STOP.
  </pre_mortem>
  <verify>
    1. `pnpm --filter @stave/app test:proto` exits 0 and prints the 6-repro classification + per-iteration diagnostics. Classification = `__LsnlgQ6osk: code, -1j62z5xjyCN: code, -72eEl7NwK9e: code, -CyO42BOyp5a: structured, -L13nBhrqGR_: structured, -LHtBlF8peGC: code`. Score = 2/6.
    2. `pnpm --filter @stave/editor test` exits 0; editor counts ≥ 1564.
    3. `pnpm --filter @stave/app test` exits 0; parity-corpus 32/32 + loc-fidelity 32/32 (Wave 0 changed no `.strudel` corpus file).
    4. `diff <(cat /tmp/repro__LsnlgQ6osk.strudel) packages/app/tests/parity-corpus/bakery-runs/repro__LsnlgQ6osk.strudel` empty (and the 5 others).
    5. `20-17-OBSERVATIONS.md` contains the verbatim test:proto stdout under §"Wave-0 baseline".
  </verify>
  <done>
    Branch `feat/20-17-d01-pervasive` created from main `aaae98c`; 6 repros + proto spec + vitest config + package.json script vendored; `pnpm test:proto` runnable + recorded baseline 2/6 in 20-17-OBSERVATIONS.md (verbatim stdout); editor + parity-corpus + loc-fidelity baselines unchanged; commit landed via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE A — Signature refactor + byte-identical gate (the Option-b PATTERN; collapses prior Wave B)

Goal-link: add `bindings?: ReadonlyMap<string, PatternIR>` as an optional
argument to `applyChain`, `parseTransform`, and every internal recursion
site in `parseStrudel.ts` that currently lacks it. Every existing caller
omits it (default undefined). Behaviour is byte-identical to current —
same as 20-16 A-1. The D-01 entry point at pS:544 already passes
`bound.bindings`; once the recursion accepts it, the map flows THROUGH
the now-accepting recursion. **A-1's verify folds in the post-refactor
proto re-run (= 2/6 unchanged) — the byte-identical gate. There is no
separate observe-only wave** (the prior Wave B was decorative; collapsed
here per the plan-checker MAJOR dim 3/4).

<task id="A-1" type="auto" depends_on="0-1">
  <name>Add `bindings?: ReadonlyMap<string, PatternIR>` to applyChain + parseTransform + every internal parseExpression recursion that currently lacks it; ZERO behaviour change; verify INCLUDES post-refactor proto = 2/6 unchanged (the byte-identical gate)</name>
  <read_first>
    packages/editor/src/ir/parseStrudel.ts:848-862 (parseExpression signature — already has `bindings?` as 4th arg, pS:861);
    packages/editor/src/ir/parseStrudel.ts:911 (applyChain CALL inside parseExpression — needs ", bindings" once applyChain accepts it);
    packages/editor/src/ir/parseStrudel.ts:1213 (applyChain definition — current signature `(ir, chain, baseOffset = 0)` — add `bindings?`);
    packages/editor/src/ir/parseStrudel.ts:1305-1798 (parseTransform CALL sites inside applyChain — each `wrapAsOpaque` neighbour / typed arm; enumerate every `parseTransform(` call in applyChain via `grep -n "parseTransform(" packages/editor/src/ir/parseStrudel.ts` and thread `, bindings` into each);
    packages/editor/src/ir/parseStrudel.ts:1829 (parseTransform definition — current signature `(transformStr, defaultIr, baseOffset = 0)` — add `bindings?`);
    packages/editor/src/ir/parseStrudel.ts (every `applyChain(` and `parseExpression(` CALL inside parseTransform's body — enumerate via grep; the inner parseExpression calls need `, undefined, bindings` since the 3rd arg is `isSampleKey`);
    packages/editor/src/ir/parseStrudel.ts:927-940 (parseRoot signature — already has `bindings?` 4th arg — UNCHANGED here, used by Wave C);
    packages/editor/src/ir/parseStrudel.ts:542-544 (the D-01 entry point — `parseExpression(bound.finalExpr, bound.finalOffset, undefined, bound.bindings)` — UNCHANGED; it already passes bindings; this wave makes the recursion downstream of it ACCEPT the map);
    .planning/phases/20-musician-timeline/20-17-CONTEXT.md §"D-01: G4 architecture" (the locked decision: optional-arg threading, NOT module state per PV50);
    .anvi/vyapti.md PV50 (per-evaluate engine accumulators — the hazard D-01 G4 explicitly avoids);
    .anvi/vyapti.md PV49 (offset additivity — preserved by construction here: signature-only, no offsets shift);
    .planning/phases/20-musician-timeline/20-16-PLAN.md §"A-1 ↔ A-2 BOUNDARY (resolved — Option (b))" (the reusable pattern)
  </read_first>
  <files>packages/editor/src/ir/parseStrudel.ts</files>
  <action>
    1. **applyChain (pS:1213) — add `bindings?` as 4th param.** Current: `(ir: PatternIR, chain: string, baseOffset = 0): PatternIR`. New: `(ir: PatternIR, chain: string, baseOffset = 0, bindings?: ReadonlyMap<string, PatternIR>): PatternIR`. Add a 20-17 G4 comment block (mirror pS:857-861 style) stating the intent + "`undefined` everywhere existing callers omit it (default; behaviour identical to pre-20-17 by construction)."
    2. **parseTransform (pS:1829) — add `bindings?` as 4th param.** Same shape; same comment block.
    3. **Thread `bindings` from applyChain into parseTransform at EVERY call site.** Enumerate with `grep -n "parseTransform(" packages/editor/src/ir/parseStrudel.ts`; for each call inside applyChain append `, bindings`. Mechanical.
    4. **Thread `bindings` from parseTransform back into applyChain at the in-parseTransform `applyChain(` recursion** (the `applyChain(defaultIr, '.' + arrowMatch[1], chainOffset)` shape → append `, bindings`). Enumerate via grep.
    5. **Thread `bindings` from parseTransform into the inner `parseExpression(` calls** inside parseTransform. parseExpression's 4th arg is `bindings` (3rd is `isSampleKey`). Pattern: `parseExpression(wrapped, base, undefined, bindings)` — the `undefined` is the existing `isSampleKey` slot. Enumerate via grep.
    6. **Thread `bindings` from parseExpression into applyChain at pS:911** (`applyChain(rootIR, chain, chainOffset)` → `applyChain(rootIR, chain, chainOffset, bindings)`). This is the site where the bindings parseExpression already accepts (pS:861) flows into applyChain. Byte-unchanged when caller passed undefined; behaviour-bearing when the pS:544 entry point passes `bound.bindings` — that flow is what makes Wave C/D/E reach.
    7. **DO NOT** add G1 (pS:1052), G2 (new parseRoot arm), G3 (the buildBindingMap literal union — Wave D), or the fixpoint loop (Wave E) in this wave. Signature-only.
    8. **P68 build hygiene with a MINIFICATION-STABLE anchor.** Configure `keepNames: true` in the editor tsup/esbuild config (`packages/editor/tsup.config.ts` or equivalent) so the `bindings` parameter + function names survive minification — DOCUMENT this decision + rationale in the commit body AND it is recorded in EXECUTOR NOTES (the P68 caveat now also forbids grepping a parameter name UNLESS keepNames is on; with keepNames the param IS a stable anchor). Then `pnpm --filter @stave/editor build`; the known pre-existing `@strudel/mondo` TS7016 on the DTS step may fire — verify it is THAT error, not a new one. Build-hygiene grep: `grep -c "bindings" packages/editor/dist/index.js` MUST be > 0 (with keepNames the new param survives) AND `node -e 'const s=require("fs").readFileSync("packages/editor/dist/index.js","utf8"); console.log(/applyChain[\s\S]{0,400}bindings/.test(s))'` returns `true`. Capture both in the commit body.
    9. **Commit via COMMIT_TEMPLATE**, gitmoji `:recycle:` (refactor), Problem: "bindings reaches only 2 sites today (pS:869, pS:1099)"; Fix: "extend the optional-arg signature to applyChain + parseTransform + their inner recursions; all existing callers omit (default undefined) — byte-unchanged by construction; keepNames:true enabled so the new param is a minification-stable P68 anchor."
  </action>
  <ownership>
    The optional-arg threading signature is owned by this wave. `parseExpression` (already accepts `bindings`) and `parseRoot` (already accepts `bindings`) signatures UNCHANGED. The default-undefined contract is owned by the type system (no implicit truthiness changed). PV50 honored: no module-level mutable state; the context flows on the stack. The keepNames build-config decision is owned by this wave's commit body + EXECUTOR NOTES.
  </ownership>
  <lifecycle>
    PK16(b) refactor — touches stages 3 (parseRoot), 4 (applyChain), and parseTransform's internal recursion. Synchronous, pure. No initialization side-effect. Every existing call site keeps its current default-undefined behaviour; the pS:544 entry point's `bound.bindings` now flows THROUGH the accepting recursion (the WIRE) — but no chain-arg / root-ident / literal / cyclic position resolves yet (those are C/D/E).
  </lifecycle>
  <pre_mortem>
    Most likely error: shadowing a default arg or swapping arg positions when the new `bindings?` is inserted (e.g. `bindings` lands in the `baseOffset` slot). Mitigation: TypeScript catches positional-type errors at compile (`ReadonlyMap` not assignable to `number`); the build step is the gate.
    Second error: a side-effect in the "bindings undefined" branch (a logged warning, a `Map` allocation for a `??` default). Mitigation: the default IS `undefined`, no allocation; the bindings argument is only READ inside Wave C/D/E conditional branches, none added here. The diff is exclusively signature lines + pass-through threading.
    Third error: a recursion site MISSED, making C/D/E's threading silently partial. Mitigation: `grep -nE "parseExpression\(|applyChain\(|parseTransform\(" packages/editor/src/ir/parseStrudel.ts | wc -l` BEFORE and AFTER — count unchanged; each BEFORE line audited: either it already accepts/threads `bindings` (pS:477 buildBindingMap RHS — UNCHANGED, Wave E re-iterates it; pS:544 entry-point; pS:869/1099 already-threaded; pS:1052 the G1 site — Wave C) or it now accepts/threads it (this wave).
    Fourth error (the 20-16 lesson — silent offset drift): signature-only refactor does NOT shift offsets. The per-file STOP gate must show ZERO loc-fidelity diff across the FULL 32-file corpus. Any diff = STOP, the refactor is not byte-unchanged.
  </pre_mortem>
  <verify>
    1. `pnpm --filter @stave/editor build` exits 0 (or only the known `@strudel/mondo` TS7016 — message identical to the 20-16 baseline; any new TS error = STOP). keepNames:true confirmed in the editor build config. Build-hygiene anchor per action 8 (`grep -c "bindings"` > 0 AND the `node -e` regex returns `true`).
    2. `pnpm --filter @stave/editor test` — 1564/1564 GREEN, BYTE-UNCHANGED (NO snapshot moves; any moved snapshot = STOP).
    3. `pnpm --filter @stave/app test` — parity-corpus 32/32 + loc-fidelity 32/32 GREEN, BYTE-UNCHANGED. Per-file STOP gate: every file's loc-fidelity snapshot diff EMPTY (no offsets shifted).
    4. **THE BYTE-IDENTICAL GATE (folded-in proto re-run, replaces the deleted Wave B):** `pnpm --filter @stave/app test:proto` — **2/6 structured, UNCHANGED from the Wave-0 baseline.** The refactor is byte-unchanged so the proto's verdict is identical; the `--LsnlgQ6osk` iter1 diagnostics still show `tag=Code bareCode=true` for the chain-arg/root-ident/literal positions — Wave C/D/E change that. Any proto delta here = the "refactor" changed behaviour = STOP.
    5. Paste the verbatim editor + parity + proto stdouts (or "BYTE-UNCHANGED — diff empty") into the commit body.
  </verify>
  <done>
    `applyChain` + `parseTransform` accept `bindings?` as optional 4th arg; all internal recursion sites thread it (default-undefined preserved); editor 1564/1564 + parity-corpus 32/32 + loc-fidelity 32/32 + **proto 2/6 (byte-identical gate)** ALL byte-unchanged from Wave-0 baseline; keepNames:true enabled + documented; P68 build-hygiene gate passed; commit landed via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE C — G1 (chain-arg) + G2 (root-ident bound-ident arm)

Goal-link: G1 is the missing `, bindings` on the loose-recursive arm
(pS:1052); G2 is the new bound-ident-root arm in parseRoot (placed
BEFORE the note/n/s/sound/mini/loose arms so a bound ident matched in
the map splices the IR as root and the chain runs over it). Both are
narrow named sites. Together they unblock `sound(rp1)` chain-arg
substitution (G1) and `chords2.rootNotes(2).note()` root-ident
substitution (G2). The pS:544 entry point's `bound.bindings` (now
flowing through Wave A's accepting recursion) reaches these sites.

<task id="C-1" type="auto" depends_on="A-1">
  <name>G1 — thread `, bindings` at parseStrudel.ts:1052 loose-recursive arm parseExpression call</name>
  <read_first>
    packages/editor/src/ir/parseStrudel.ts:1018-1075 (the loose-recursive arm — note|n|s|sound|mini with chained inner; the parseExpression call is at pS:1052-1056);
    packages/editor/src/ir/parseStrudel.ts:927-940 (parseRoot signature — `bindings?` already 4th arg; the loose arm runs inside parseRoot so `bindings` is in scope);
    .planning/phases/20-musician-timeline/20-17-CONTEXT.md §"D-01: G4 architecture";
    .anvi/vyapti.md PV49 (offset additivity — preserved: parameter-only change, no offset arithmetic touched);
    .anvi/vyapti.md PV51 (isSampleKey threading — verify `callerIsSample` continues to flow alongside the new `bindings`)
  </read_first>
  <files>packages/editor/src/ir/parseStrudel.ts</files>
  <action>
    1. At pS:1052-1056, the current call is `parseExpression(innerTrimmed, innerAbsOffset, callerIsSample,)`. Change to add `bindings,` as the 4th arg: `parseExpression(innerTrimmed, innerAbsOffset, callerIsSample, bindings,)`. Single 1-line addition. `bindings` is in scope from parseRoot's 4th param.
    2. Add a 20-17 G1 comment block above the call (mirror pS:857-861): the intent + "Without this thread, `sound(rp1)` with `rp1` in `bindings` still parsed `rp1` as a bare ident → bareCode → opaque-RHS fence → whole program bails. The `callerIsSample` (PV51) arg unchanged."
    3. **P68 build hygiene:** `pnpm --filter @stave/editor build`. With keepNames:true (Wave A), grep a minification-stable anchor unique to this change: the G1 comment is stripped; instead `node -e 'const s=require("fs").readFileSync("packages/editor/dist/index.js","utf8"); const ms=s.match(/parseExpression\([^)]{0,200}\)/g)||[]; console.log("4-arg parseExpression calls:", ms.filter(m=>m.split(",").length===4).length);'`. The count INCREASES by 1 vs Wave A (one new 4-arg site). Capture in the commit body.
    4. **Commit via COMMIT_TEMPLATE**, gitmoji `:sparkles:`, Problem/Fix body.
  </action>
  <ownership>
    The loose-recursive arm now owns "thread the caller's bindings context into the inner chained expression's parse." The inner parseExpression — and through it parseRoot/applyChain/parseTransform — receive the same map parseRoot was given. Offset arithmetic unchanged: `innerAbsOffset` was already computed correctly pre-20-17 (pS:1048-1051); bindings does not affect it.
  </ownership>
  <lifecycle>
    PK16(b) stage 3 (parseRoot arms). Runs after splitRootAndChain selects the root; the loose-arm matches before falling to the bare-Code fallback. The bindings-context now flows into the inner recursion — definition-site offsets preserved (R6, PV49): the spliced bound IR carries its OWN definition-site offset (stored in the map at parse time), independent of the use-site chain offset.
  </lifecycle>
  <pre_mortem>
    Most likely error: misordering the args so `bindings` lands in the `isSampleKey` slot. Mitigation: TypeScript catches this (3rd param is `boolean | undefined`, not `ReadonlyMap`); the build step is the gate.
    Second error: forgetting the LOOSE arm runs INSIDE parseRoot, which already accepts `bindings` at pS:940. Mitigation: the action references parseRoot's 4th-param scope explicitly.
    Third error (silent partial reach if G2 isn't done same wave): G1 alone unblocks `sound(rp1)` but NOT `chords2.rootNotes(2)`. Mitigation: C-2 is in the same wave, runs immediately after C-1; the proto re-run is at the END of Wave C (after C-2), capturing the combined effect.
  </pre_mortem>
  <verify>
    1. `pnpm --filter @stave/editor build` exits 0 (or known TS7016 only); the `node -e` snippet prints a 4-arg-call increase of 1.
    2. `pnpm --filter @stave/editor test` ≥ 1564.
    3. `pnpm --filter @stave/app test` parity-corpus 32/32 + per-file loc-fidelity correlation: every parity-UNCHANGED file's loc-fidelity diff EMPTY. (The 32 corpus files have no `sound(boundIdent)` shape; expected byte-unchanged. Any file moving = STOP.)
    4. `pnpm --filter @stave/app test:proto` — capture verbatim. Score expected 2/6 still (until G2/G3/E land); diagnostics for `--LsnlgQ6osk` iter1 should show `sound(rp1)` now resolves bindings IF `rp1` resolved at iter0. Record the new diagnostic line in 20-17-OBSERVATIONS.md.
    5. Commit body pastes verbatim test:proto stdout + the per-iteration diagnostic comparison vs the Wave-A baseline.
  </verify>
  <done>
    G1 threaded; loose-arm bindings flow; proto's `beat` RHS iter1 diagnostic shows substitution for chain-arg positions; build + tests + per-file loc-fidelity clean; commit landed via COMMIT_TEMPLATE.
  </done>
</task>

<task id="C-2" type="auto" depends_on="C-1">
  <name>G2 — add bound-ident-root arm in parseRoot (BEFORE note/n/s/sound/mini/loose arms) splicing `bindings.get(trimmed)` when trimmed is a bare ident in the map</name>
  <read_first>
    packages/editor/src/ir/parseStrudel.ts:927-957 (parseRoot signature + start of arm matching; the `trimmed` variable is the root token in scope around pS:941);
    packages/editor/src/ir/parseStrudel.ts:957-1100 (the existing parseRoot arms in order: note/n/s/sound/mini/loose/stack);
    packages/editor/src/ir/parseStrudel.ts:869-873 (parseExpression's whole-expr substitution — the precedent shape; mirror the regex `/^[A-Za-z_$][\w$]*$/`);
    .anvi/vyapti.md PV49 (offset additivity — the spliced subtree carries DEFINITION-SITE offset; the chain runs at the USE-SITE chain offset; additivity holds because applyChain's chain arithmetic is independent of the root's offset)
  </read_first>
  <files>packages/editor/src/ir/parseStrudel.ts</files>
  <action>
    1. Immediately AFTER the parseRoot variable-setup block and BEFORE the first existing arm (`noteMatch`, ~pS:957), insert a new arm:
       ```ts
       // 20-17 G2 — bound-ident-root substitution. When the root token is
       // a bare identifier present in `bindings`, splice the bound subtree
       // as the root; the existing applyChain (called by parseExpression
       // at pS:911) then runs the chain over the spliced IR. PV49 holds:
       // the spliced subtree carries its DEFINITION-SITE offset (stored
       // at parse time inside buildBindingMap), and applyChain uses the
       // USE-SITE chain offset arithmetic independently. Without this
       // arm, `chords2.rootNotes(2).note()` with `chords2` resolved still
       // parsed `chords2` as a bare ident → bare Code → opaque fence.
       if (bindings && /^[A-Za-z_$][\w$]*$/.test(trimmed) && bindings.has(trimmed)) {
         return bindings.get(trimmed) as PatternIR
       }
       ```
    2. **DO NOT** place the arm AFTER the existing strict regexes. BEFORE is correct because (a) a bare ident in `bindings` can never syntactically match `note(...)`/`s(...)`/`mini(...)` — those are anchored on a function-call shape requiring `(`; a bare ident has none, so the existing arms won't match it pre-20-17 (it falls to the bare-Code fallback at the bottom of parseRoot). (b) Placement before cannot REGRESS any existing parse — existing tests pass a bound ident only via the stack-arg path (parseExpression's whole-expr substitution at pS:869-873, already handles it), NOT a parseRoot call. The new arm strictly widens.
    3. **PV49 audit:** the spliced IR is `bindings.get(trimmed)`. Its loc range was set at definition-site offset during `buildBindingMap`'s RHS parse (Wave E stores `rhsOffset` on the descriptor). parseRoot returns it; parseExpression at pS:911 wraps the chain via applyChain with `chainOffset = trimmedOffset + root.length` (pS:910) — USE-SITE; the spliced subtree's INTERNAL loc is unchanged. This is the documented R6 (definition-site loc preserved) behaviour from γ-3 — extended to a new substitution position.
    4. **P68 build:** `pnpm --filter @stave/editor build`. Anchor grep: `grep -c "bindings.has(trimmed)" packages/editor/dist/index.js` > 0 (the new arm's exact regex/method-call survives minification as a string literal — minification-stable regardless of keepNames).
    5. **Commit via COMMIT_TEMPLATE**, gitmoji `:sparkles:`.
  </action>
  <ownership>
    The new arm owns the bound-ident-root substitution at the parseRoot level. The spliced subtree's INTERNAL loc is owned by buildBindingMap (definition-site at parse time). The chain offset is owned by applyChain (use-site at root.length past trimmedOffset).
  </ownership>
  <lifecycle>
    PK16(b) stage 3 (parseRoot). Runs FIRST inside parseRoot — before the strict regex arms. A miss falls through unchanged.
  </lifecycle>
  <pre_mortem>
    Most likely error: placing the arm AFTER the existing arms (a future regex that DOES match a bare ident would make the new arm dead). Mitigation: the placement comment explicitly states "BEFORE the strict regex arms" + the reason.
    Second error: the spliced IR carries the wrong loc. Mitigation: PV49 audit step 3 traces the offset path; the per-file loc-fidelity correlation in verify catches drift (a parity-changed enumerated file has its NEW snapshot verified by sliced-token inspection against the input).
    Third error: the regex over-matches keywords (`stack`, `note`). Mitigation: those are only meaningful PRECEDING `(`; a bare `stack` without `(` cannot appear in the map unless the user explicitly bound it (in which case substituting is correct). The regex is correct.
  </pre_mortem>
  <verify>
    1. `pnpm --filter @stave/editor build` exits 0; `grep -c "bindings.has(trimmed)" packages/editor/dist/index.js` > 0.
    2. `pnpm --filter @stave/editor test` ≥ 1564.
    3. `pnpm --filter @stave/app test` parity-corpus 32/32; per-file loc-fidelity STOP gate: every UNCHANGED-parity file → loc-fidelity diff EMPTY. The 32 corpus files have no `bareBoundIdent.method()` shape the existing parser tolerates structurally → NO parity-changes on the existing corpus. Any moved file = STOP.
    4. `pnpm --filter @stave/app test:proto` — verbatim capture. Score may move from 2/6 up: `--LsnlgQ6osk` may now ground (both `sound(rp1)` (G1) AND `chords2.rootNotes(...)` (G2) resolve). At iter1 the RHSs whose root was `chords2`/`bass`/`harm2` should classify non-bareCode. If `--LsnlgQ6osk` is STRUCTURED post-Wave-C, D-03 criterion 1 is provisionally met (Wave E still REQUIRED for the cyclic / forward-ref synthetics + final confirmation).
    5. Commit body pastes the verbatim per-iter diagnostics for `--LsnlgQ6osk`.
  </verify>
  <done>
    G2 arm added; bound idents as roots splice cleanly; build + 1564 + parity 32 + loc-fidelity per-file clean; proto re-run captured (`--LsnlgQ6osk` diagnostic shows root-ident substitution reaches `chords2`); commit landed via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE D — G3 literal-RHS classification via the ADDITIVE `Code.via` union arm (D-02 CORRECTION)

Goal-link: `var numChords = 4`'s RHS `4` parses to bareCode → the
opaque-RHS fence fires (`return null`) → `-LHtBlF8peGC` bails BEFORE the
fixpoint iterates. **The original D-02 ("store as Code-with-via, no
ripple") was UNCONSTRUCTIBLE** — `Code.via` (PatternIR.ts:100-105) is
specifically the `wrapAsOpaque` shape `{ method, args, callSiteRange,
inner: PatternIR }`; a literal `4` has no method/args/inner, and the
only constructor is `wrapAsOpaque` (parseStrudel.ts:73-86). **Resolved
by the LOCKED user decision (CONTEXT "D-02 CORRECTION (2026-05-19)"):
G3 ships via Option 2 — widen the `Code.via` union with an ADDITIVE
`{ literal: true; raw: string }` arm.** G3 is **firmly IN scope, ships
by principle not measured frequency** (Strudel code IS JavaScript;
standard JS literal bindings are first-class — the "measurement-gated
drop G3" path is REJECTED per CONTEXT; the D-03 crit-2 fresh re-measure
still runs but does NOT gate G3's existence).

<task id="D-1a" type="auto" depends_on="C-2">
  <name>Widen the `Code.via` union with an ADDITIVE `{ literal:true; raw:string }` arm; add a NAMED constructor/classifier helper (`classifyLiteralRhs` / `IR.codeLiteral`); the kept opaque fence is byte-IDENTICAL</name>
  <read_first>
    packages/editor/src/ir/PatternIR.ts:85-106 (the `Code` tag + the `via?:` union — the EXISTING `wrapAsOpaque` arm `{ method; args; callSiteRange; inner }`);
    packages/editor/src/ir/PatternIR.ts:210-211 (the `IR.code(code, meta?)` smart constructor — NO `via` option today; `attachMeta({tag:'Code', code, lang:'strudel'}, meta)`);
    packages/editor/src/ir/parseStrudel.ts:73-86 (`wrapAsOpaque` — the ONLY current `via` constructor; proves the original D-02 was unconstructible);
    .anvi/hetvabhasa.md P67 (Code tri-state: bare Code via=undefined / Code-with-via=structured);
    .planning/phases/20-musician-timeline/20-17-CONTEXT.md §"D-02 CORRECTION (2026-05-19)" (the LOCKED user decision — sacrosanct: additive arm, byte-unchanged opaque arm, byte-identical fence, named helper, STRICT scope)
  </read_first>
  <files>packages/editor/src/ir/PatternIR.ts; packages/editor/src/ir/parseStrudel.ts</files>
  <action>
    1. **Widen the `Code.via` union (PatternIR.ts:100-105) — ADDITIVE only.** The EXISTING arm stays **byte-unchanged** (do NOT add a `kind` field to it — minimize ripple per the CONTEXT correction):
       ```ts
       via?:
         | {
             method: string
             args: string
             callSiteRange: [number, number]
             inner: PatternIR
           }
         | { literal: true; raw: string }   // 20-17 G3 (D-02 CORRECTION) — literal-RHS marker
       ```
       The opaque fence (`tag === 'Code' && via === undefined`) is **byte-IDENTICAL**: a literal sets `via = { literal:true, raw }` → `via !== undefined` → already on the "structured, don't bail" side with ZERO fence-code change (P67 tri-state preserved exactly). Discriminate the two arms by `'literal' in via` (or `via.inner === undefined`) at the deep-walker sites only (D-1c).
    2. **Add the NAMED constructor/classifier helper.** In `parseStrudel.ts` (alongside `wrapAsOpaque`, the existing `via`-constructor idiom), export a named function:
       ```ts
       // 20-17 G3 (D-02 CORRECTION). Classifies a STRICT bare literal RHS
       // and returns the structured Code-with-via literal node, or null.
       // STRICT scope (matcher line held): ONLY bare literals match —
       // number | plain double-quoted | plain single-quoted. `4 + 1`,
       // `${}`-template, any expression → null → caller keeps bare Code →
       // opaque fence fires correctly. Substitution of a literal is
       // term-splicing, NEVER evaluation.
       export function classifyLiteralRhs(rhs: string): { tag: 'Code'; code: string; lang: 'strudel'; via: { literal: true; raw: string } } | null {
         const t = rhs.trim()
         const isNum = /^-?\d+(\.\d+)?$/.test(t)
         const isDq  = /^"[^"]*"$/.test(t)
         const isSq  = /^'[^']*'$/.test(t)
         if (!(isNum || isDq || isSq)) return null
         return { tag: 'Code', code: t, lang: 'strudel', via: { literal: true, raw: t } }
       }
       ```
       (Or, equivalently, an `IR.codeLiteral(raw)` smart constructor in `PatternIR.ts` building `{ tag:'Code', code:raw, lang:'strudel', via:{ literal:true, raw } }` plus the regex-classifier in parseStrudel — pick ONE named home and state it in the commit body. **This helper is the provenance source consumed by E-1** — E-1's fixpoint loop calls `classifyLiteralRhs` post-parse; the provenance link is stated in both task bodies.)
    3. **DO NOT** call the helper from `buildBindingMap` yet (that is E-1 — the fixpoint loop calls it post-parse at each iteration). This task ships the TYPE widen + the named helper + the consumer audit (D-1c). The buildBindingMap wiring is E-1 so the single-pass→fixpoint restructure and the literal classification land together (avoids a throwaway single-pass literal call).
    4. **P68 build:** `pnpm --filter @stave/editor build`; `grep -c "classifyLiteralRhs\|codeLiteral" packages/editor/dist/index.js` > 0 (a named export — minification-stable with keepNames; also the regex literal `/^-?\\d+/` survives). Capture in the commit body.
    5. **Commit via COMMIT_TEMPLATE**, gitmoji `:sparkles:`, Problem: "the original D-02 was unconstructible — `Code.via` is the `wrapAsOpaque` shape; a literal `4` has no method/args/inner"; Fix: "widen `Code.via` with an ADDITIVE `{literal:true;raw}` arm (existing arm byte-unchanged, fence byte-identical) + a named `classifyLiteralRhs` helper; per CONTEXT D-02 CORRECTION (LOCKED)."
  </action>
  <ownership>
    `PatternIR.ts` owns the widened `Code.via` discriminated union. `classifyLiteralRhs` (or `IR.codeLiteral`) owns the literal-marker construction + the STRICT recognition regex. The opaque-RHS fence (`tag === 'Code' && via === undefined`) is UNCHANGED — it owns the post-classification gate; a literal node has `via !== undefined` so the fence does not fire on it. P67 tri-state is the contract consumers see.
  </ownership>
  <lifecycle>
    PK16 — type-level + helper-level change; no pipeline-order change. The helper is PURE (string → node | null). Consumed by E-1's fixpoint loop (the provenance link).
  </lifecycle>
  <pre_mortem>
    Most likely error: adding a `kind`/discriminant to the EXISTING `wrapAsOpaque` arm (ripple explosion — every existing `via.method`/`via.inner` reader breaks). Mitigation: the CONTEXT correction LOCKS "do NOT add a `kind` field to it"; discriminate by `'literal' in via` / `via.inner === undefined` instead — the existing arm is byte-unchanged.
    Second error: the regex over-matches (`"a"+"b"` as a plain dq string). Mitigation: anchored `^"[^"]*"$` rejects any interior `"`; a concat/template stays bare → fence fires. `4 + 1` fails `^-?\d+(\.\d+)?$` (the `+` and space). Strict by construction.
    Third error: the fence's behaviour silently changes because the union widened. Mitigation: the fence tests `via === undefined`; a literal sets `via` to a defined object → `via !== undefined` → ZERO fence-code edit; the fence's bytes are not touched at all (verified by `git diff` showing the fence lines unchanged).
  </pre_mortem>
  <verify>
    1. `pnpm --filter @stave/editor build` exits 0 (or known TS7016); anchor grep > 0; `git diff` shows the opaque-fence lines (pS:478-482) are UNCHANGED (byte-identical fence — the acceptance check).
    2. `pnpm --filter @stave/editor test` ≥ 1564 (the type widen + helper add no behaviour — no test moves; the helper is unused until E-1).
    3. `pnpm --filter @stave/app test` parity-corpus 32/32 + loc-fidelity 32/32 byte-unchanged (no behaviour yet).
    4. `pnpm --filter @stave/app test:proto` — 2/6 unchanged (the helper is not yet wired into buildBindingMap; E-1 does that).
    5. Commit body states the chosen named home (`classifyLiteralRhs` in parseStrudel.ts OR `IR.codeLiteral` in PatternIR.ts) — E-1's provenance source.
  </verify>
  <done>
    `Code.via` widened with the additive `{literal:true;raw}` arm (existing arm byte-unchanged); the opaque fence is byte-identical (`git diff` proves it); the named `classifyLiteralRhs`/`IR.codeLiteral` helper exists + is the documented provenance source for E-1; build + 1564 + parity 32 + proto 2/6 all byte-unchanged; commit landed via COMMIT_TEMPLATE.
  </done>
</task>

<task id="D-1b" type="auto" depends_on="D-1a">
  <name>buildBindingMap: STRICT literal regex recognition (number | "…" | '…') — substitution of a literal is term-splicing, NEVER evaluation (inside the locked Datalog discipline)</name>
  <read_first>
    packages/editor/src/ir/parseStrudel.ts:449-494 (buildBindingMap — full body, single-pass today; RHS parse pS:477, opaque fence pS:478-482);
    packages/editor/src/ir/parseStrudel.ts (the `classifyLiteralRhs`/`IR.codeLiteral` helper from D-1a — the named provenance source);
    .planning/phases/20-musician-timeline/20-17-CONTEXT.md §"D-02 CORRECTION" ("Strict scope (matcher line held)")
  </read_first>
  <files>packages/editor/src/ir/parseStrudel.ts</files>
  <action>
    NOTE: the buildBindingMap RESTRUCTURE into the descriptor-list + fixpoint loop is E-1. This task ONLY pins the STRICT literal recognition contract that E-1's loop consumes via the D-1a helper, and adds a focused editor unit-test asserting the regex boundary cases — so the regex contract is locked + tested BEFORE the fixpoint restructure builds on it.
    1. Add `packages/editor/src/ir/__tests__/parseStrudel.literalRhs.test.ts` exercising `classifyLiteralRhs` (imported from D-1a's named home):
       - `4`, `-3`, `0.5`, `-12.75` → returns a `{ via:{ literal:true, raw } }` node.
       - `"bd"`, `'<sd hh>'` → returns the literal node (raw = the quoted string verbatim).
       - `4 + 1`, `\`x${n}\``, `makeBass()`, `() => x`, `[1,2]`, `"a" + "b"`, `n("0")` → returns `null` (stays bare Code → opaque fence fires correctly).
       - Round-trip the `raw`: the helper's `via.raw` is byte-equal to the trimmed RHS for every literal case (the term-splicing contract — NEVER evaluation; `4` stays `"4"`, not `4` the number).
    2. **DO NOT** modify buildBindingMap in this task (E-1 restructures it; wiring the helper into a soon-to-be-replaced single pass would be throwaway). This task locks + tests the contract.
    3. **P68 build:** `pnpm --filter @stave/editor build`; the helper anchor already grepped in D-1a — re-confirm > 0 (no regression).
    4. **Commit via COMMIT_TEMPLATE**, gitmoji `:white_check_mark:`, Problem/Fix body.
  </action>
  <ownership>
    This task owns the LOCKED STRICT-regex contract test for `classifyLiteralRhs`. E-1 owns the call site (the fixpoint loop). The boundary cases (`4 + 1` etc. → null) are the matcher-line guarantee: substitution of a literal is term-splicing, never evaluation.
  </ownership>
  <lifecycle>
    PK16 — test-only + contract-pin. No pipeline change. Pure helper under test.
  </lifecycle>
  <pre_mortem>
    Most likely error: the regex passes `4 + 1` because a future loosening adds `\s` or `[+\-]`. Mitigation: the unit test asserts `4 + 1 → null` explicitly; any loosening breaks the test = STOP.
    Second error: the helper coerces `4` to the number `4` (evaluation, not splicing). Mitigation: the round-trip assertion (`via.raw` byte-equal to trimmed RHS) catches any coercion — `raw` MUST be the string `"4"`.
  </pre_mortem>
  <verify>
    1. `pnpm --filter @stave/editor test` — the new `parseStrudel.literalRhs.test.ts` GREEN; editor count ≥ 1564 + the new spec's cases.
    2. `pnpm --filter @stave/app test` parity-corpus 32/32 + loc-fidelity 32/32 byte-unchanged (no production behaviour change; helper not yet wired).
    3. `pnpm --filter @stave/app test:proto` — 2/6 unchanged.
    4. Commit body pastes the new spec's stdout (the boundary-case matrix).
  </verify>
  <done>
    The STRICT literal-recognition contract is locked + unit-tested (number | "…" | '…' → literal node; `4 + 1`/template/expression → null; `raw` is byte-equal to the trimmed RHS = term-splicing not evaluation); editor green; parity/loc/proto byte-unchanged; commit landed via COMMIT_TEMPLATE.
  </done>
</task>

<task id="D-1c" type="auto" depends_on="D-1b">
  <name>MANDATORY consumer audit (the phase's PRIMARY risk surface) — grep every `via.` reader + every `tag === 'Code'` wrapper-shape assumer; add a `'literal' in via` (or `via.inner === undefined`) guard at each; round-trip + timeline-projection acceptance test on the literal-binding repro</name>
  <read_first>
    packages/editor/src/ir/toStrudel.ts (round-trip — the named acceptance check: `.slow(numChords)` → `.slow(4)` MUST emit `via.raw` VERBATIM to preserve code-invariance);
    packages/editor/src/ir/parseStrudel.ts (every internal `via.` reader — the fences themselves + the `rootIsBareCode`/`rhsIsBareCode` checks);
    packages/editor/src/ir/collect.ts (deep walk);
    packages/editor/src/ir/serialize.ts (deep walk);
    packages/editor/src/ir/PatternIR.ts (any `via`-shape consumer);
    packages/editor/src/ir/parseStrudelStages.ts:119,175 (the RAW/mini-expanded stage — branches on `input.tag === 'Code'` (pSS:119) and `rootIR.tag === 'Code'` (pSS:175); a `tag === 'Code'` wrapper-shape assumer that the live grep returns — confirm a literal-binding RHS does not regress its Code-fallback);
    packages/app/src/components/irProjection.ts (IR Inspector deep walk);
    packages/app/src/components/collectLeafIrNodeIds.ts;
    packages/app/src/components/IRInspectorChrome.ts;
    packages/app/src/components/IRInspectorPanel.tsx;
    packages/app/src/components/IRInspectorTimeline.tsx;
    packages/app/src/components/MusicalTimeline.tsx:298-299 (**HIGH severity — the single most dangerous live consumer**; the timeline-projection deep-walk `visit()`. At pS-MT:298-299: `if (anyN.via && typeof anyN.via === 'object') { const inner = (anyN.via as { inner?: PatternIR }).inner; if (inner) visit(inner) }`. A literal node's `via = {literal:true,raw}` is **truthy + object** → this branch ENTERS → `via.inner` is `undefined` → the `if (inner)` silently skips → the literal subtree is silently dropped from the timeline projection. This is the EXACT P67 silent-wrong shape D-1c exists to prevent, and it is NOT in the toStrudel/IRInspector* set — an easy eye-skip because the plan enumerated `IRInspectorTimeline.tsx` but the unguarded deref lives in `MusicalTimeline.tsx`);
    packages/app/src/components/musicalTimeline/layoutTrackRows.ts;
    .anvi/hetvabhasa.md P67 (a missed site = silent-wrong — the audit's WHOLE purpose)
  </read_first>
  <files>
    packages/editor/src/ir/toStrudel.ts (+ guard if it assumes `via.method`/`via.inner`);
    packages/editor/src/ir/collect.ts (+ guard if it walks `via.inner`);
    packages/editor/src/ir/serialize.ts (+ guard);
    packages/editor/src/ir/parseStrudelStages.ts (+ guard if it reads via.inner / assumes tag==='Code' wrapper shape — pSS:119/175 are `tag === 'Code'` branches; confirm a literal RHS does not regress the Code-fallback);
    packages/app/src/components/irProjection.ts (+ guard);
    packages/app/src/components/collectLeafIrNodeIds.ts (+ guard if applicable);
    packages/app/src/components/IRInspectorChrome.ts (+ guard if applicable);
    packages/app/src/components/IRInspectorPanel.tsx (+ guard if applicable);
    packages/app/src/components/IRInspectorTimeline.tsx (+ guard if applicable);
    packages/app/src/components/MusicalTimeline.tsx (**HIGH severity — MUST guard pS-MT:298-299** if it reads via.inner / assumes tag==='Code' wrapper shape; the literal `via` is truthy+object so the existing `if (anyN.via && typeof anyN.via === 'object')` branch enters and reads a non-existent `via.inner` → silent subtree drop);
    packages/app/src/components/musicalTimeline/layoutTrackRows.ts (+ guard if applicable);
    packages/editor/src/ir/__tests__/parseStrudel.literalRoundTrip.test.ts (new — the round-trip acceptance test);
    packages/app/src/components/__tests__/MusicalTimeline.literalProjection.test.tsx (new — the timeline-projection acceptance test that WALKS a literal node through MusicalTimeline's `visit()`)
  </files>
  <action>
    1. **Enumerate every site — the grep is the source of truth; the named list is a FLOOR, not an exhaustive-and-final list.** Run `grep -rn '\.via\|tag === .Code.' packages/editor/src packages/app/src --include='*.ts' --include='*.tsx' | grep -v __tests__ | grep -v '/tests/'` (the precise via-shape grep `\.via\b\|via\.inner\|via\.method\|via\.args\|via\.callSiteRange\|via ===\|via !==\|via\?\.` is the narrowing follow-up). **This grep output is the source of truth.** The KNOWN suspects the grep MUST at minimum reproduce (the FLOOR — if the grep returns a file NOT in this list, **STOP and disposition that file before proceeding**; an under-enumeration can NEVER silently pass): `toStrudel.ts`, `parseStrudel.ts` (the fences + `rootIsBareCode`/`rhsIsBareCode`), `parseStrudelStages.ts` (pSS:119/175 `tag === 'Code'`), `collect.ts`, `serialize.ts`, `PatternIR.ts`, `irProjection.ts`, `collectLeafIrNodeIds.ts`, `IRInspectorChrome.ts`, `IRInspectorPanel.tsx`, `IRInspectorTimeline.tsx`, `MusicalTimeline.tsx` (**HIGH severity — pS-MT:298-299**), `layoutTrackRows.ts`. Conversely, a name in this floor that the live grep does NOT return is also a STOP-and-disposition (the code moved/renamed — re-derive, do not assume). Also grep `tag === 'Code'` / `tag === "Code"` wrapper-shape assumers across the same trees.
    2. **At each site that reads `via.method`/`via.inner`/`via.args`/`via.callSiteRange` (the `wrapAsOpaque`-shape assumption), add the literal guard:** branch on `'literal' in via` (TS narrows the union) BEFORE touching `via.inner` etc. For a deep-walker, a literal `via` has NO `inner` → it is a LEAF (do not recurse into `via.inner`). For `toStrudel`, the literal arm emits `via.raw` **VERBATIM** (this is the NAMED acceptance check: a binding `var numChords = 4` substituted into `.slow(numChords)` round-trips as `.slow(4)` — `via.raw` emitted byte-for-byte, NOT re-serialized, NOT coerced — code-invariance preserved).
    2-HIGH. **`MusicalTimeline.tsx:298-299` — HIGH-severity guarded site (state the failure shape inline so the executor cannot eye-skip it).** The current code is:
       ```ts
       if (anyN.via && typeof anyN.via === 'object') {
         const inner = (anyN.via as { inner?: PatternIR }).inner
         if (inner) visit(inner)
       }
       ```
       A literal node carries `via = { literal: true, raw }`. That value is **truthy AND `typeof === 'object'`**, so this branch **ENTERS**; `(via as {inner?}).inner` is `undefined`; `if (inner)` is false; the literal subtree is **silently dropped from the timeline projection** — no throw, no log, wrong-but-plausible render (the exact P67 silent-wrong this whole task exists to prevent). **Fix:** add a `'literal' in via` (or equivalently `via.inner === undefined`) guard BEFORE the `via.inner` read so a literal `via` is treated as a LEAF and the projection does not lose the node:
       ```ts
       if (anyN.via && typeof anyN.via === 'object' && !('literal' in anyN.via)) {
         const inner = (anyN.via as { inner?: PatternIR }).inner
         if (inner) visit(inner)
       }
       ```
       (the literal node itself is still visited as a leaf by the surrounding walk; only the spurious `via.inner` recursion is suppressed). This site is HIGH severity because it is the single most dangerous live consumer and the unguarded deref is in `MusicalTimeline.tsx`, NOT the `IRInspector*` files the plan otherwise enumerates.
    3. **The fences themselves** (`tag === 'Code' && via === undefined`): UNCHANGED — a literal has `via !== undefined` so it never hits the bareCode branch (verified by `git diff` showing the fence lines untouched). State this in the commit body as part of the audit checklist. Likewise `parseStrudelStages.ts:119/175` (`input.tag === 'Code'` / `rootIR.tag === 'Code'`): a literal-binding RHS reaches these as a *resolved* node (tag !== 'Code') so the Code-fallback is not regressed — disposition this explicitly in the checklist (guarded / not-a-via-reader / fence-unchanged).
    4. **Round-trip + timeline-projection acceptance tests:**
       4a. `packages/editor/src/ir/__tests__/parseStrudel.literalRoundTrip.test.ts` (new):
       - Input: the minimal literal-binding repro `const numChords = 4\nstack(s("bd")).slow(numChords)` (and a string variant `const tag = "bd"\nstack(s(tag))`).
       - Assert: `toStrudel(parseStrudel(input))` re-emits `.slow(4)` / `s("bd")` VERBATIM (code-invariance) — NOT `.slow(numChords)` (the binding is resolved) and NOT `.slow(4.0)`/coerced.
       - Assert: the IR Inspector deep-walk (`irProjection` or `collectLeafIrNodeIds`) does not throw / does not recurse into a non-existent `via.inner` on the literal node (a smoke assertion that the guard works).
       4b. `packages/app/src/components/__tests__/MusicalTimeline.literalProjection.test.tsx` (new — the HIGH-severity site's own acceptance, the literal node WALKED through the MusicalTimeline projection path, NOT just toStrudel):
       - Construct (or parse) an IR containing a literal-binding node (`via = {literal:true,raw}`) under a track and drive it through MusicalTimeline's `visit()` / projection path (the same `visit` containing pS-MT:298-299).
       - Assert: the projection does NOT throw, does NOT silently drop the literal subtree (the literal node IS present in the projected/visited output), and the `via.inner`-undefined branch is provably not entered for the literal node (observe the visited node set / row output, not inferred).
       - This is the observation that proves the HIGH-severity guard works in the timeline path specifically — distinct from the editor-side round-trip.
    5. **The audit's verify = a grep-enumerated checklist** (every site the live grep returns — the named list is only the FLOOR — with its disposition: "guarded" / "fence-unchanged" / "not a via-shape reader"; any grep hit NOT in the floor list must be explicitly dispositioned, never skipped) + BOTH acceptance tests GREEN (the editor round-trip AND the MusicalTimeline literal-projection walk). A missed site = a silent-wrong (P67) — this is the phase's primary risk surface, hence its own task + its own verify.
    6. **P68 build:** `pnpm --filter @stave/editor build`; the audit adds no new unique symbol per se — anchor on the new test file's import OR confirm the existing helper anchor still > 0. Also `pnpm --filter @stave/app build` (the app-side guards incl. the MusicalTimeline.tsx HIGH-severity guard) if the app has a build step; otherwise the app test suite is the gate.
    7. **Commit via COMMIT_TEMPLATE**, gitmoji `:shield:` (security/robustness — the silent-wrong guard), Problem/Fix body INCLUDING the full grep-enumerated checklist AND an explicit line for the `MusicalTimeline.tsx:298-299` HIGH-severity guard.
  </action>
  <ownership>
    This task owns the D-02 consumer-ripple closure (the phase's PRIMARY risk surface per the CONTEXT correction). Each guarded site owns its own literal-vs-opaque discrimination (`'literal' in via`). `MusicalTimeline.tsx`'s `visit()` (pS-MT:298-299) owns the timeline-projection leaf discrimination — the HIGH-severity site. `toStrudel`'s literal arm owns the verbatim `via.raw` re-emission (code-invariance). The round-trip test owns the named editor acceptance check; the MusicalTimeline literal-projection test owns the timeline-path acceptance.
  </ownership>
  <lifecycle>
    PK16 — consumer-side guards; no pipeline-order change. Runs after D-1a/D-1b (the type + helper exist) and BEFORE E-1 (the fixpoint that actually PRODUCES literal nodes in production) — so the consumers are guarded before any literal node can flow.
  </lifecycle>
  <pre_mortem>
    Most likely error: a `via.`-reader is missed because the plan's named list was treated as exhaustive-and-final rather than a grep-reproduced FLOOR — exactly how `MusicalTimeline.tsx:298-299` (the single most dangerous live consumer) was eye-skipped (the plan enumerated `IRInspectorTimeline.tsx` but the unguarded `via.inner` deref is in `MusicalTimeline.tsx`). Mitigation: action 1 makes the live grep the source of truth; the named list is only a floor; ANY grep hit not in the floor → STOP-and-disposition; ANY floor name not returned by the grep → STOP-and-disposition. The MusicalTimeline literal-projection test exercises the HIGH-severity path end-to-end on a real literal node.
    Second error: `toStrudel` re-serializes the literal (emits `4` as a Code dump or coerces `"bd"` quoting) → code-invariance broken (the P62/code-invariance class). Mitigation: the round-trip test asserts BYTE-VERBATIM `via.raw` re-emission.
    Third error: a deep-walker recurses into `via.inner` on a literal (undefined) → crash OR (the MusicalTimeline shape) silently drops the subtree because `if (inner)` is false after a spurious branch-entry — no crash, wrong render. Mitigation: the `'literal' in via` guard makes the literal a LEAF; the MusicalTimeline literal-projection acceptance test asserts the literal node is NOT dropped from the projection (observed, not inferred).
  </pre_mortem>
  <verify>
    1. The commit body contains the grep-enumerated checklist: every `via.`-reader + every `tag === 'Code'` assumer the LIVE grep returns across editor+app non-test src, each with disposition (guarded / fence-unchanged / not-a-via-reader) — the named list is a floor; the checklist reflects the actual grep output; `MusicalTimeline.tsx:298-299` is present and marked HIGH-severity / guarded.
    2. `pnpm --filter @stave/editor test` — the new `parseStrudel.literalRoundTrip.test.ts` GREEN (`.slow(4)` / `s("bd")` re-emitted verbatim; IR-Inspector smoke passes); editor ≥ 1564 + the new spec.
    3. `pnpm --filter @stave/app test` — the new `MusicalTimeline.literalProjection.test.tsx` GREEN (a literal node WALKED through MusicalTimeline's `visit()` is NOT dropped and does NOT enter the `via.inner` branch — observed); parity-corpus 32/32 + loc-fidelity 32/32 byte-unchanged (no literal node is produced in production until E-1; the guards are defensive + the tests construct the node directly).
    4. `pnpm --filter @stave/app test:proto` — 2/6 unchanged (still no buildBindingMap wiring).
    5. `git diff` proves the opaque-fence lines (pS:478-482) are byte-unchanged AND `parseStrudelStages.ts:119/175` Code-branch lines are byte-unchanged (literal RHS reaches them as a resolved node, not Code).
  </verify>
  <done>
    Every production `via.`-reader + `tag === 'Code'` wrapper-shape assumer the LIVE grep returns enumerated + guarded (or dispositioned fence-unchanged / not-a-reader) — the grep-enumerated checklist is in the commit body and the named list is documented as a FLOOR (grep is source of truth); `MusicalTimeline.tsx:298-299` is a HIGH-severity enumerated + guarded site with a `'literal' in via` guard before the `via.inner` read; the named round-trip acceptance test proves `.slow(numChords)` → `.slow(4)` emits `via.raw` VERBATIM (code-invariance); the MusicalTimeline literal-projection acceptance test proves a literal node is WALKED through the timeline projection without the `via.inner`-undefined drop (observed); IR-Inspector smoke passes; fence + parseStrudelStages Code-branch byte-unchanged; commit landed via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE E — Bounded least-fixpoint + occurs-check terminal (consumes D-1's named literal helper)

Goal-link: replace `buildBindingMap`'s single forward pass with the ≤N
iteration loop (the 20-16 CONTEXT D-01 design). At each iteration,
re-parse pending RHSs with the partially-resolved bindings map AND apply
D-1a's named `classifyLiteralRhs` helper post-parse; add to resolved if
non-bareCode; stop when no progress. The opaque-RHS fence post-fixpoint
= the occurs-check terminal: any binding still pending after the bound =
cyclic or opaque → graceful Code (no new fence).

<task id="E-1" type="auto" depends_on="D-1c">
  <name>buildBindingMap fixpoint loop — bounded ≤N iterations, monotone progress check, D-1a `classifyLiteralRhs` applied post-parse each iter, occurs-check terminal via the kept opaque fence</name>
  <read_first>
    packages/editor/src/ir/parseStrudel.ts:449-494 (buildBindingMap — full body, single-pass today; RHS parse pS:477, opaque fence pS:478-482, the kept `<2`/dup-key/shape fences pS:456/473/485/488);
    packages/editor/src/ir/parseStrudel.ts (the `classifyLiteralRhs`/`IR.codeLiteral` helper from D-1a — E-1 CONSUMES this named helper; the provenance link: D-1a defines it, E-1 calls it post-parse at each iteration);
    /tmp/proto-d01-fixpoint.spec.ts (the preserved prototype's fixpoint loop — adapt the production version from this shape; the proto already proves the loop terminates on the synthetics);
    packages/app/tests/parity-corpus/loc-fidelity.test.ts:97-116 (READ LINE-BY-LINE — the mechanism that determines whether the fixpoint's N splices are loc-fidelity-safe; cited in this task's pre_mortem);
    .planning/phases/20-musician-timeline/20-17-CONTEXT.md §"Inherited north star" (the Datalog discipline: total / PTIME / order-independent / occurs-check stratified);
    .anvi/krama.md PK16 stage-0.5 (the no-`$:` pipeline order — buildBindingMap is stage 0.5; the fixpoint sits inside this stage and does not change the pipeline order);
    .planning/phases/20-musician-timeline/20-16-OBSERVATIONS.md §"SYNTHETICS" (forward-ref / cyclic / dup-key / OQ1 5c — the 5 gates the fixpoint must continue passing)
  </read_first>
  <files>packages/editor/src/ir/parseStrudel.ts; packages/editor/src/ir/__tests__/parseStrudel.fixpoint.test.ts (new)</files>
  <action>
    1. **Restructure buildBindingMap into a descriptor list + fixpoint loop.** Adapted from the 20-16 prototype:
       - First pass: build `descs = [{ name, rhs, rhsOffset }, …]` from `stmts` (still split by `splitTopLevelStatements`; still detect `finalIdx` as first non-binding). The kept `< 2 statements` (pS:456) / dup-key (pS:473) / shape (pS:485/488) fences run during this pass UNCHANGED.
       - `pending = new Set<number>(descs.indices)`. `bindings = new Map<string, PatternIR>()`.
       - **Fixpoint loop:**
         ```ts
         for (let iter = 0; iter < descs.length && pending.size > 0; iter++) {
           let progress = false
           for (const i of [...pending]) {
             const d = descs[i]
             const parsed = parseExpression(d.rhs, d.rhsOffset, undefined, bindings)
             // D-1a provenance: classifyLiteralRhs is the NAMED helper
             // defined in Wave D (D-1a). E-1 consumes it post-parse so a
             // bare literal RHS becomes the structured {literal:true;raw}
             // node instead of bare Code.
             const lit = classifyLiteralRhs(d.rhs)
             const ir = lit ?? parsed
             const bare = ir.tag === 'Code' && (ir as { via?: unknown }).via === undefined
             if (!bare) { bindings.set(d.name, ir); pending.delete(i); progress = true }
           }
           if (!progress) break
         }
         ```
       - **Occurs-check terminal:** AFTER the loop, `if (pending.size > 0) return null`. Any binding still pending = its RHS keeps parsing to bareCode even with all other bindings resolved = cyclic or genuinely opaque → graceful Code-fallback. This SUBSUMES the old opaque-RHS fence at pS:478-482 — it fires LATER (post-fixpoint) and for the right reason. **The fence's TEST (`tag==='Code' && via===undefined`) is byte-unchanged — it now lives in the post-loop `bare` computation; `git diff` shows the predicate text is identical, only its position moved (single-pass → post-fixpoint).**
    2. **Bound N:** `descs.length` (in a non-cyclic graph each iteration resolves ≥1 new descriptor → all resolve within `descs.length` iters; a cyclic descriptor never resolves → exits via `!progress` or the iter bound).
    3. **Monotone progress check:** the `progress` boolean exits the loop when an iteration adds zero resolutions (cycles + persistently-opaque RHSs). iter-bound AND progress-check = belt-and-suspenders (either alone suffices).
    4. **Kept γ-3 fences:** dup-key on descriptor `name` in the FIRST pass (same first-dup-wins). Shape (`finalIdx !== stmts.length - 1`) + `<2 statements` also first-pass. Byte-equivalent semantically; `git diff` shows the predicates unchanged.
    5. **Definition-site offset preserved:** each descriptor stores `rhsOffset = stmts[i].offset + rhsStartInText` (the SAME arithmetic as pS:474-476 today). Splicing a resolved IR into a use site (parseExpression whole-expr / parseRoot G2 / G4-internal) reuses the SAME IR object — its loc is the definition-site offset, unchanged.
    6. **PV49 audit:** the fixpoint adds NO new offset arithmetic. Re-parsing a RHS at iter `k` uses the SAME `d.rhsOffset` as iter 0 (same input text, same base). No offset re-basing.
    7. **Editor unit-test (new spec):** `packages/editor/src/ir/__tests__/parseStrudel.fixpoint.test.ts` covering the 4 proto synthetics: forward-ref → STRUCTURED, cyclic → Code (graceful), dup-key → Code, OQ1-5c pair → as the proto records. All MUST pass.
    8. **P68 build:** `pnpm --filter @stave/editor build`; anchor `grep -c "iter < descs.length\|pending" packages/editor/dist/index.js` > 0 (minification-stable: `descs.length` survives as a property access string; with keepNames the loop var too). Capture in the commit body.
    9. **Commit via COMMIT_TEMPLATE**, gitmoji `:sparkles:`, Problem/Fix body citing the matcher-line-derived property + the D-1a provenance ("consumes the named classifyLiteralRhs helper defined in D-1a").
  </action>
  <ownership>
    buildBindingMap now owns the fixpoint loop. The descriptor list + pending set are local; NO module-level state (PV50 hazard avoided by construction). The `bindings` map IS the substitution lattice; the fixpoint computes its least element under monotonicity (each iter only ADDS). Cycle detection via the !progress / iter-bound exit; the post-fixpoint pending check is the occurs-check terminal (the kept opaque-fence predicate, repositioned). `classifyLiteralRhs` (owned by D-1a) is CONSUMED here — the provenance link.
  </ownership>
  <lifecycle>
    PK16 stage 0.5 (buildBindingMap). Synchronous. Pure. No async, no engine state. Stage order unchanged (the loop is internal to stage 0.5; stages 1-4 downstream unaffected). Total + PTIME by the bound.
  </lifecycle>
  <pre_mortem>
    Most likely error: the loop doesn't terminate. Mitigation: the hard `iter < descs.length` bound + the `!progress` early exit are both belt-and-suspenders. The proto's `(c) cyclic` synthetic (`const a=b; const b=a; stack(a,b)`) exercises the cycle path and must return Code via the post-fixpoint pending check. If the proto post-Wave-E shows the cyclic synthetic non-`code` or timing out, the bound/check is wrong.
    Second error: order-dependent resolution producing a different `bindings` map. Mitigation: monotone least-fixpoint is order-independent by construction; the `(b) forward-ref` synthetic exercises this and must still return STRUCTURED.
    Third error: literal-RHS programs classify as Code-with-via and the consumers mis-handle the new arm. Mitigation: D-1c's mandatory consumer audit + round-trip test already closed this surface BEFORE E-1 produces any literal node in production.
    Fourth error (the 20-16 lesson — silent offset drift): if `rhsOffset` is recomputed inside the loop instead of stored on the descriptor, an iter-k call re-bases wrong. Mitigation: action 5 stores `rhsOffset` on the descriptor at first pass — never recomputed.
    **Fifth error — THE Fix-4 OBSERVED finding (loc-fidelity vs the fixpoint's N spliced subtrees):** the fixpoint creates N spliced subtrees; each carries its DEFINITION-SITE offset (R6 — correct). The question: does `loc-fidelity.test.ts` flag these? **READ THE TEST SOURCE (loc-fidelity.test.ts:97-116):** for each `*.strudel` it does `code = fs.readFileSync(file)`, `ir = parseStrudel(code)`, then `collectLeafLocs(ir, code, locs)` where (loc-fidelity.test.ts:82) `out.push({ tag, text: src.slice(start, end), bad })` with **`src === code` — the ONE-AND-ONLY parsed string; there is NO separate "original source"**. γ-3 ALREADY produces spliced subtrees (the stack-arg path, pS:1099) and loc-fidelity passes today. **MECHANISM (cited): a spliced subtree's loc points back into the SAME `code` string at its DEFINITION SITE; `src.slice(defStart, defEnd)` therefore yields the binding's RHS token text — stable as long as the definition-site arithmetic (`rhsOffset = stmts[i].offset + rhsStartInText`, pS:474-476) is correct, which it is because the descriptor STORES it once at first pass and never recomputes (action 5).** Therefore: **the SAME mechanism covers the fixpoint's N splices → loc-fidelity is expected GREEN for every parity-UNCHANGED file, with NO allow-list change.** The ONLY expected loc-fidelity snapshot delta is the V-2 fixture (a parity-changed file, by construction). V-3's per-file STOP-gate allow-list therefore stays `{Wave-0 baseline} ∪ {V-2 fixture}` — NOT extended to "files containing spliced subtrees" (the splice is loc-safe by the definition-site mechanism, observed against the actual test source, not assumed). If `git diff` of the loc-fidelity snapshots shows ANY parity-unchanged file moved, the definition-site arithmetic drifted (a real bug) — STOP, bisect.
    Sixth error (silent-partial-reach): `--LsnlgQ6osk` does NOT ground STRUCTURED post-Wave-E. Mitigation: the proto's per-iter diagnostics reveal which RHS position still parses bareCode → the un-threaded G4 site is named + fixed. If all positions resolve but the final expression is bareCode because a binding's RHS was pending-cyclic, the program is correctly a graceful Code-fallback for the cyclic-class — the canonical case should NOT be cyclic; re-audit `--LsnlgQ6osk`'s structure.
  </pre_mortem>
  <verify>
    1. `pnpm --filter @stave/editor build` exits 0; anchor grep > 0; `git diff` shows the opaque-fence predicate text byte-unchanged (only repositioned post-fixpoint).
    2. `pnpm --filter @stave/editor test` ≥ 1564 + the new `parseStrudel.fixpoint.test.ts` (4 synthetics) GREEN.
    3. `pnpm --filter @stave/app test` parity-corpus 32/32 + per-file loc-fidelity STOP gate. Per the Fifth-error finding: every parity-UNCHANGED file's loc-fidelity snapshot diff EMPTY (the definition-site mechanism is loc-safe — observed against loc-fidelity.test.ts:82). Any moved parity-unchanged file = STOP.
    4. `pnpm --filter @stave/app test:proto` — capture verbatim. **`--LsnlgQ6osk` MUST be STRUCTURED (D-03 criterion 1 hard gate).** The other 5 verdicts recorded but not gating. Per-iter diagnostics pasted into the commit body.
    5. The synthetics (b)/(c)/(d)/OQ1-5c — all 4 pass per the proto's `=== SYNTHETICS ===` block.
    6. Commit body pastes the verbatim proto stdout + the new fixpoint spec stdout.
  </verify>
  <done>
    Fixpoint loop landed (consuming D-1a's named `classifyLiteralRhs`); occurs-check terminal via the post-fixpoint pending check (the kept opaque-fence predicate, repositioned, byte-unchanged); the 4 proto synthetics pass; **`--LsnlgQ6osk` grounds STRUCTURED in the proto (D-03 criterion 1 met provisionally — V-1 confirms in production parity-bakery)**; loc-fidelity per-file clean (definition-site mechanism observed loc-safe — no allow-list extension); commit landed via COMMIT_TEMPLATE.
  </done>
</task>

---

## VERIFICATION WAVE — PK17 step 6 + fixture vendor + per-file STOP gate + summary/catalogue/PR

<task id="V-1" type="auto" depends_on="E-1">
  <name>D-03 hard DUAL gate — proto criterion 1 (`--LsnlgQ6osk` STRUCTURED) + FRESH PK17-step-6 `pnpm parity:bakery --n 50` ≥ 85.0%; NO bar-lowering escape</name>
  <read_first>
    packages/app/scripts/parity-bakery.mjs (the maintainer sampler);
    packages/app/tests/parity-corpus/_bakery-classify.spec.ts:11-67 (the isCodeFallback discriminator);
    .anvi/krama.md PK17 step 5 + step 6 (triage residual fallbacks into named classes → backlog; FRESH ~50-sample re-measure, NEVER on samples you fixed for);
    .planning/phases/20-musician-timeline/20-17-CONTEXT.md §"D-03" + §"D-02 CORRECTION" (G3 is IN by principle; the re-measure does NOT gate G3's existence; the dual gate has NO honesty escape)
  </read_first>
  <files>.planning/phases/20-musician-timeline/20-17-OBSERVATIONS.md (append §"V-1 D-03 dual gate")</files>
  <action>
    1. **D-03 criterion 1 — proto verdict.** `pnpm --filter @stave/app test:proto`. Read the `--LsnlgQ6osk` row. **MUST be `structured`** (production's `parseStrudel(<verbatim>)` unwrap of the top-level Track('d1', body) shows `body.tag !== 'Code'` OR `body.via !== undefined`). If `code` → STOP, return to E-1 pre-mortem 6. Record the verdict + the unwrap inspection verbatim.
    2. **D-03 criterion 2 — fresh real-world re-measure.** `pnpm parity:bakery --n 50` (the script pulls FRESH live Supabase; NOT the 20-16 .bakery-runs file). Confirm a new stamp ≠ `2026-05-18T14-34-02-237Z`. Record the new stamp + `%` + N + upstream SHA `f73b3956`. **MUST be ≥ 85.0%** vs the 80.0% baseline.
    3. **THE STRICT CONTRACT (no escape hatch — the plan-checker MINOR dim 5/6):** D-03 requires BOTH criteria. **If criterion 2 (< 85.0% fresh re-measure) fails, the phase does NOT pass. There is NO "user decides whether to lower the gate" option.** Triage the remaining fallbacks into named classes (PK17 step 5), file EACH as a backlog issue (NEVER fixed in 20-17 — the 4× cascade lesson), and **the dominant new class becomes a 20-18**. No bar-lowering, regardless of how close to 85.0%. (Note: with G3 now IN by principle, criterion 2 is more likely met — but the no-escape-hatch language stands regardless.)
    4. Classify each NEW Code-fallback into a NAMED gap class with a minimal repro. NEW classes → backlog issues (V-4).
    5. Record both verdicts in 20-17-OBSERVATIONS.md.
  </action>
  <ownership>
    The proto's verdict on `--LsnlgQ6osk` owns D-03 criterion 1. parity-bakery.mjs's printed stamp + SHA + % + N owns the durable D-03 criterion 2 evidence (recorded into 20-17-OBSERVATIONS.md; the gitignored .bakery-runs/ JSON is NOT the durable record). The strict no-escape contract is owned by this task + the GOAL section.
  </ownership>
  <lifecycle>
    Post-impl, pre-PR gate. Run-once maintainer measurement. PK17 step 6 — FRESH pull (the proto's 6 repros DO count as "samples fixed for"; the fresh-population re-measure is the correct PK17 step 6 instance).
  </lifecycle>
  <pre_mortem>
    Most likely error (PK17 "Common violation"): re-measuring on the same .bakery-runs/.json from 20-16 → circular, inflated %. Mitigation: `pnpm parity:bakery` pulls fresh; the stamp MUST differ from `2026-05-18T14-34-02-237Z`; same stamp → re-pull.
    Second error: trusting an inflated proto-side measurement as population proof. Mitigation: proto's 6 repros are the AUDIT TRAIL for the canonical case; the population gate is parity-bakery's independent N=50 fresh pull.
    Third error: parity ≥ 85% but `--LsnlgQ6osk` is still Code (gain came from other cases). Mitigation: D-03 is DUAL — both must pass; criterion-1 fail = phase fails even if criterion-2 passes. State this in the captured verdict.
    Fourth error: introducing a "lower the gate" escape because criterion 2 lands at e.g. 84.6%. Mitigation: action 3 LOCKS no-bar-lowering — triage to named classes + 20-18, never patch in 20-17.
  </pre_mortem>
  <verify>
    20-17-OBSERVATIONS.md contains: the proto's verbatim verdict on `--LsnlgQ6osk` (STRUCTURED + the unwrap inspection); the fresh parity % + N + FRESH stamp (≠ 2026-05-18T14-34-02-237Z) + UPSTREAM_SHA (still f73b3956); the classified NEW fallback classes (each minimal repro + backlog-issue title; the dominant one tagged "→ 20-18"); BOTH criteria pass OR the phase is declared NOT-PASS with the triage recorded (no bar-lowering).
  </verify>
  <done>
    D-03 dual gate met (criterion 1 `--LsnlgQ6osk` STRUCTURED + criterion 2 fresh re-measure ≥ 85.0%) with audit-trail evidence; OR the phase is declared NOT-PASS with the residual triaged into named classes (dominant → 20-18 backlog), NO bar-lowering; NEW fallback classes enumerated for V-4 backlog (NOT fixed).
  </done>
</task>

<task id="V-2" type="auto" depends_on="E-1">
  <name>Vendor permanent CI fixture `bakery-140-binding-transitive.strudel` + provenance + parity-refresh exclusion</name>
  <read_first>
    packages/app/tests/parity-corpus/parity.test.ts (auto-discovers `bakery-*.strudel`, snapshot per file + loc-strip);
    packages/app/tests/parity-corpus/BAKERY-FIXTURES.md (20-15 + 20-16 provenance format);
    packages/app/scripts/parity-refresh.mjs (TARGETS — must EXCLUDE the new bakery-* fixture so upstream-drift tool does not false-flag);
    packages/app/tests/parity-corpus/bakery-runs/repro__LsnlgQ6osk.strudel (the verbatim Bakery code — vendored in Wave 0);
    `gh issue view 140` + `gh issue view 141` (provenance citations)
  </read_first>
  <files>
    packages/app/tests/parity-corpus/bakery-140-binding-transitive.strudel (new — distilled from repro__LsnlgQ6osk.strudel to the minimal transitive binding chain);
    packages/app/tests/parity-corpus/BAKERY-FIXTURES.md (append);
    packages/app/scripts/parity-refresh.mjs (extend the bakery-* exclusion to include bakery-140-*)
  </files>
  <action>
    1. **Distil ONE minimal fixture from `repro__LsnlgQ6osk.strudel`.** The canonical D-01-eligible transitive chain. 2-3 bindings + one final expr — the minimal shape proving G1+G2+G4+G3+fixpoint end-to-end. Keep it as close to the verbatim issue/repro as semantically minimal (the 20-15 V-2 lesson: a paraphrase silently substitutes a working form). It MUST exercise the SAME paths: chain-arg substitution via G1 (`sound(rp1)`), a transitive binding (`beat` references `rp1`), AND — to also cover G3 — at least one literal binding consumed in a chain-arg (`const numChords = 4` … `.slow(numChords)`).
    2. **Pre-20-17 verdict: bare Code** (segmenter passes post-20-16 Wave-0; the binding references in chain-arg + the literal RHS → without G1+G3+G4+fixpoint, bareCode). **Post-20-17 verdict: STRUCTURED.** The fixture's snapshot, regenerated under Wave E's branch tip, asserts STRUCTURED IR. The fixture IS the canonical regression wall.
    3. Append the fixture to BAKERY-FIXTURES.md with the issue ## #140 + #141, the `gh issue view 140`/`141` provenance, the named Bakery hash `--LsnlgQ6osk`, the closed gap classes (G1 chain-arg / G2 root-ident / G3 literal-union / G4 method-arg / fixpoint).
    4. Extend `parity-refresh.mjs` TARGETS so the upstream-drift tool does not report bakery-140-* "missing upstream" (mirror the 20-16 bakery-* exclusion).
    5. Regenerate the snapshot via `pnpm --filter @stave/app test -u tests/parity-corpus/parity.test.ts` for THIS new fixture ONLY. Assert STRUCTURED (`tag !== 'Code'` OR `via !== undefined`).
    6. Pre-existing 32 corpus snapshots must NOT move EXCEPT any files Wave C/D/E explicitly enumerated as legitimate bare-Code→structured (expected: zero — the 32 corpus has no binding-transitive shape).
    7. **Commit via COMMIT_TEMPLATE**, gitmoji `:white_check_mark:`.
  </action>
  <ownership>
    The fixture owned by the corpus dir; auto-discovered by parity.test.ts + loc-fidelity.test.ts. Provenance owned by BAKERY-FIXTURES.md (sourced from the vendored verbatim `repro__LsnlgQ6osk.strudel`). parity-refresh exclusion owned by parity-refresh.mjs.
  </ownership>
  <lifecycle>
    CI-gated post-fix (after E-1). The fixture becomes the permanent regression wall (PK17 step 5) for the D-01 mechanism — `--LsnlgQ6osk`-class regressions caught at CI.
  </lifecycle>
  <pre_mortem>
    Most likely error: a paraphrased distillation that substitutes a working form and silently does NOT exercise G1/G2/G3/G4 (the 20-15 V-2 `sound`-alias lesson). Mitigation: the distillation uses `sound(rp1)` (the exact G1-blocked shape) + a transitive binding + a literal binding `const numChords = 4` consumed in `.slow(numChords)`. Cross-check: with the fixture vendored, REVERT only Wave C (G1) → it MUST regress to bare Code; reapply → STRUCTURED. (Optional but recommended bisection.)
    Second error: the fixture's snapshot regenerated under an offset-drifted branch locks wrong loc. Mitigation: the per-file loc-fidelity STOP gate already passed for Wave E; the new fixture's loc values are inspected against the input by hand (the token text should slice cleanly).
  </pre_mortem>
  <verify>
    1. `pnpm --filter @stave/app test` — parity-corpus ≥ 33/33; the new fixture's snapshot STRUCTURED.
    2. `grep bakery-140 packages/app/scripts/parity-refresh.mjs` confirms the exclusion.
    3. `grep -c "#140\|#141\|--LsnlgQ6osk" packages/app/tests/parity-corpus/BAKERY-FIXTURES.md` ≥ 3.
    4. Per-file loc-fidelity correlation: every parity-UNCHANGED file's diff EMPTY (the new fixture is the ONLY parity-changed entry).
    5. Pre-existing 32 corpus snapshots byte-unchanged.
  </verify>
  <done>
    bakery-140-binding-transitive.strudel vendored + BAKERY-FIXTURES.md provenance + parity-refresh exclusion; structured snapshot; per-file loc-fidelity clean; corpus 32 → 33 (each spec); commit landed via COMMIT_TEMPLATE.
  </done>
</task>

<task id="V-3" type="auto" depends_on="A-1,C-1,C-2,D-1a,D-1b,D-1c,E-1,V-2">
  <name>Full-corpus per-file loc-fidelity STOP gate (cross-wave drift gate; the realized phase pre-mortem)</name>
  <read_first>
    packages/app/tests/parity-corpus/loc-fidelity.test.ts:97-116 (the actual mechanism — `src.slice` with `src === code`, no allow-list, no parity awareness; the Fix-4 finding);
    packages/app/tests/parity-corpus/parity.test.ts (loc-strip via normalize.ts — the BLINDNESS this gate compensates for);
    20-17-PLAN.md §"PRE-MORTEM" (the PER-FILE correlation rule, NOT the corpus aggregate; the loc-fidelity mechanism statement);
    E-1's pre_mortem Fifth-error finding (the definition-site mechanism is loc-safe → allow-list stays {Wave-0} ∪ {V-2 fixture}, NOT extended to spliced-subtree files)
  </read_first>
  <files>(verify-only — no source change)</files>
  <action>
    1. `pnpm --filter @stave/app test` — capture BOTH parity-corpus + loc-fidelity results.
    2. **THE STOP gate (PER-FILE correlation, not corpus aggregate):** for every corpus file, compare its parity classification vs the 20-16 main-branch baseline.
       - **Every file with UNCHANGED parity → its loc-fidelity snapshot diff MUST be EMPTY.** A diff = silent offset drift introduced by Wave A signature / Wave C G1/G2 / Wave D union / Wave E fixpoint splice → STOP, do NOT proceed to PR, bisect which wave re-based an offset, fix, re-run from that wave's regression oracle.
       - **Every file with CHANGED parity (bare-Code → structured) MUST be on the enumerated allow-list:** `{Wave-0 baseline} ∪ {bakery-140-binding-transitive.strudel}` ∪ {any pre-existing corpus file each wave flagged in its commit body}. Per E-1's Fix-4 finding the allow-list is NOT extended to "files containing G2/G4-spliced subtrees" — the definition-site mechanism (loc-fidelity.test.ts:82, `src.slice` from the single parsed string at the def-site offset) is loc-safe by construction, observed against the test source, not assumed. Any OTHER changed file = unexplained = drift = STOP.
    3. The aggregate "parity count unchanged AND loc non-empty" heuristic is explicitly NOT used (a legitimate bare-Code→structured improvement moves BOTH parity and loc on that file — the 20-16 lesson).
    4. `pnpm --filter @stave/editor test` ≥ 1564.
    5. **Built-dist freshness (P68):** for each wave's new minification-stable anchor — Wave A (`bindings` param, keepNames on), Wave C (`bindings.has(trimmed)` G2 string literal), Wave D (`classifyLiteralRhs`/`codeLiteral` named export), Wave E (`descs.length` / the loop) — `grep -c <anchor> packages/editor/dist/index.js` > 0 for at least one anchor per wave.
  </action>
  <ownership>
    This task owns the cross-wave offset-drift verdict via the PER-FILE parity↔loc-fidelity correlation against the enumerated allow-list. The loc-fidelity harness owns the drift signal parity is blind to.
  </ownership>
  <lifecycle>
    Final CI gate, after all waves + V-2 fixture. Cross-wave composition gate (verifies wave effects do not interact destructively on offsets).
  </lifecycle>
  <pre_mortem>
    Most likely error: a wave shifted offsets, parity stayed green (loc-blind), PR ships with broken click-to-source. OR a legitimate bare-Code→structured file is mis-flagged as drift and a real fix reverted. Mitigation: per-file correlation with the EXPLICIT allow-list ({Wave-0} ∪ {V-2 fixture} ∪ {per-wave enumerated, expected ∅}) is the precise pass criterion — not the parity count. The Fix-4 finding (E-1 pre_mortem 5) bounds the allow-list precisely: the splice is loc-safe, so a spliced-subtree file is NOT auto-added.
    Second error: dist freshness passes (grep > 0) but the symbol is from a DIFFERENT part of the file (false positive). Mitigation: the wave-specific anchor is unique (Wave D's `classifyLiteralRhs` is novel — pre-Wave-D grep returns 0, post > 0).
  </pre_mortem>
  <verify>
    Per-file correlation pasted verbatim: every parity-unchanged file's loc-fidelity diff EMPTY; the set of parity-changed files exactly = {Wave-0 baseline} ∪ {bakery-140-binding-transitive.strudel} ∪ {wave-enumerated, expected ∅}; parity-corpus ≥ 33/33; editor ≥ 1564; every wave's new symbol grep > 0 on `dist`.
  </verify>
  <done>
    The phase pre-mortem provably did NOT occur (per-file correlation: no parity-unchanged file drifted; every parity-changed file is on the precisely-bounded allow-list — the splice is loc-safe by the observed definition-site mechanism); all baseline gates hold; built dist fresh.
  </done>
</task>

<task id="V-4" type="auto" depends_on="V-1,V-2,V-3">
  <name>20-17-SUMMARY + catalogue updates (vyapti, krama PK16 PK17, conditional hetvabhasa) + backlog issues + single non-stacked PR + post-merge artifact verification</name>
  <read_first>
    .anvi/vyapti.md (PV49/PV50/PV51), .anvi/krama.md (PK16 stage-0.5, PK17), .anvi/hetvabhasa.md (P67/P68), .anvi/CONVENTIONS.md (Confirmed by / REF mandatory) — in .anvi/ (gitignored, git add -f);
    .planning/phases/20-musician-timeline/20-16-SUMMARY.md (SUMMARY format precedent);
    ~/.claude/projects/-Users-mrityunjaybhardwaj-Documents-projects-struCode/memory/feedback_stacked_pr_base_retarget.md (the 20-14 post-merge ancestry lesson — applies even for a single non-stacked PR);
    ~/.claude/projects/-Users-mrityunjaybhardwaj-Documents-projects-struCode/memory/feedback_commit_msg_heredoc.md (zsh `-m` + backticks/$() silent-strip trap — recurred 2× in 20-16; the COMMIT_TEMPLATE in EXECUTOR NOTES is the structural fix)
  </read_first>
  <files>
    .planning/phases/20-musician-timeline/20-17-SUMMARY.md (new);
    .anvi/vyapti.md (new entry — "binding substitution is pervasive optional-arg threading, never module-level mutable state" + a `Code.via` discriminated-union addendum);
    .anvi/krama.md (PK16 stage-0.5 amendment + PK17 this-cycle numbers);
    .anvi/hetvabhasa.md (conditional — see action 4)
  </files>
  <action>
    1. Write 20-17-SUMMARY.md: the V-1 D-03 dual-gate result (both criteria with audit-trail — `--LsnlgQ6osk` STRUCTURED + the dated/SHA'd FRESH parity %), the per-wave deltas (Wave 0 vendor + baseline 2/6; Wave A signature byte-unchanged + the folded byte-identical proto gate; Wave C G1+G2; Wave D D-1a union + D-1b regex + D-1c consumer audit; Wave E fixpoint + occurs-check), the kept γ-3 fences (UNCHANGED — fence predicate byte-identical, only repositioned), the D-02 CORRECTION applied (additive union arm; the original D-02 was unconstructible), the NEW fallback classes filed to backlog (NOT fixed — D-03; dominant → 20-18), the goal-backward check, the no-bar-lowering contract outcome.
    2. **vyapti (NEW entry + addendum)** — "Binding substitution is pervasive optional-arg threading (D-01 G4), NEVER module-level mutable state (the PV50 hazard). The bindings map flows on the call stack via applyChain + parseTransform + every internal parseExpression recursion as an optional 4th arg; `undefined` everywhere unintended. Combined with a bounded least-fixpoint inside buildBindingMap (≤ N iterations, monotone progress, occurs-check terminal = the kept opaque-RHS fence predicate, repositioned post-fixpoint), binding resolution is total + PTIME + order-independent without becoming an evaluator." Plus a `Code.via` addendum: "`Code.via` is a DISCRIMINATED UNION — the `wrapAsOpaque` arm `{method,args,callSiteRange,inner}` (opaque-fragment wrapper) and the additive `{literal:true;raw}` arm (G3 literal-RHS marker). Discriminate by `'literal' in via` / `via.inner === undefined`. The opaque fence test (`tag==='Code' && via===undefined`) is byte-identical across both arms — a literal sets `via !== undefined` so it never bails. Every `via.`-reader MUST guard the union (D-1c consumer audit)." Fields per CONVENTIONS.md (Claim / Why / Confirmed by [Phase 20-17 Waves A-E + the 4 proto synthetics + D-1c round-trip + fresh parity ≥ 85%] / Breaks when [a future change adds a discriminant to the wrapAsOpaque arm, or introduces module-level binding context] / REF [parseStrudel.ts buildBindingMap+applyChain+parseTransform+classifyLiteralRhs; PatternIR.ts Code.via union; 20-17-PLAN/SUMMARY]). Add a PV49 span addendum (the parseRoot G2 bound-ident-root arm joins the PV49-routed span).
    3. **krama PK16** — stage-0.5 amendment: `buildBindingMap` is a bounded fixpoint loop (≤ N iters) with a post-parse `classifyLiteralRhs` step; the opaque-RHS fence predicate is reached only POST-fixpoint (occurs-check terminal); descriptor-list + pending-set replaces the single forward pass. **krama PK17** — append this cycle's measured numbers (fresh % + N + stamp + SHA; #140/#141 closure; new classes → backlog; dominant → 20-18 if criterion 2 missed).
    4. **hetvabhasa (conditional)** — IF any wave's fix took > 1 attempt (e.g. a missed G4 recursion site, the literal regex over-matched, the fixpoint hung, or a missed D-1c consumer site produced a silent-wrong), add the error pattern (Pattern / Symptom / Detection / Wrong fix / Right fix / REF). If none > 1 attempt, record that in the SUMMARY, add NO entry (single occurrence → memory — promotion discipline).
    5. **File backlog GitHub issues** for each NEW V-1 fallback class (AnviDev: issue-before-fix; NOT fixed — D-03). #153 + #149 remain backlog if still applicable. If criterion 2 was missed, the dominant residual class is filed as the **20-18** seed.
    6. **Open a SINGLE non-stacked PR → main** with `closes #140` in the body (GitHub honours ONE keyword's auto-close per body — the 20-15/20-16 lesson). Reference #141 in body text; manually close #141 on merge. Include the dated/SHA'd parity claim (D-03 crit 2) + the proto verdict (crit 1) + the per-file loc-fidelity correlation + the kept-fence byte-identical audit + the D-1c consumer-audit checklist. Claude does NOT merge.
    7. **Post-merge (after the user merges) — verify the ARTIFACT not the badge:** HEAD == merge commit, 0/0 divergence; `grep` the new fixpoint/`classifyLiteralRhs`/G1-G2-arm symbols present on main's built `dist`; editor + parity-corpus counts hold (1564 + 33); AND `git merge-base --is-ancestor <PR-head-sha> main` MUST exit 0 (the EXACT 20-14 failure class — the GitHub MERGED badge is NOT proof; near-vacuous for a single non-stacked PR but kept explicit). Manually `gh issue close 141 --comment "Closed by PR #N (single closes-keyword GitHub limit; verified merged via git merge-base ancestry)"`.
  </action>
  <ownership>
    SUMMARY owns the reproducible parity claim + the proto verdict + the D-02-CORRECTION-applied outcome + the no-bar-lowering result. Catalogues own the durable invariant (pervasive-threading + bounded least-fixpoint vyapti + the `Code.via` discriminated-union addendum, the PK16 stage-0.5 amendment, the PK17 numbers). The PR owns issue closure (#140 auto; #141 manual). The post-merge `git merge-base --is-ancestor` check owns the "actually landed on main" verdict (badge ≠ truth).
  </ownership>
  <lifecycle>
    Phase close. After V-1 (D-03 dual gate), V-2 (fixture), V-3 (drift gate). Catalogue updates AFTER the fixes are verified (not speculative).
  </lifecycle>
  <pre_mortem>
    Most likely error: quoting a parity % without date/SHA/N → unreproducible (PK17 "Common violation"). Mitigation: SUMMARY + PK17 carry stamp + SHA + N; the bakery-140 fixture is the CI-reproducible floor independent of the live %.
    Second error: trusting the GitHub MERGED badge (20-14 lesson). Mitigation: step 7's `git merge-base --is-ancestor` is mandatory even for a single non-stacked PR.
    Third error: a hetvabhasa entry for a single-occurrence surprise (bloat). Mitigation: action 4 is explicitly conditional on > 1 attempt.
    Fourth error: forgetting to manually close #141 (the closes-keyword-per-body limit recurred 20-15/20-16). Mitigation: actions 6 + 7 explicitly note the manual close + the `gh issue close 141` command.
    Fifth error: a commit somewhere used `git commit -m "..."` with backticks (zsh substitution silently strips it; recurred 2× in 20-16). Mitigation: EVERY task in this phase commits via the named COMMIT_TEMPLATE (EXECUTOR NOTES) — structural, not memory-dependent.
  </pre_mortem>
  <verify>
    20-17-SUMMARY.md exists with the dated/SHA'd parity % + the proto verdict + the kept-fence byte-identical audit + the D-02-CORRECTION outcome + the no-bar-lowering result; `.anvi/vyapti.md` has the new pervasive-threading invariant + the `Code.via` discriminated-union addendum; `.anvi/krama.md` PK16 stage-0.5 + PK17 amended; `gh issue list` shows the new backlog issues + #140/#141 referenced; PR open (not merged by Claude). Post-merge: `git merge-base --is-ancestor <PR-head-sha> main` exit 0 recorded; #141 manually closed; built dist has all wave anchors.
  </verify>
  <done>
    Reproducible parity claim recorded (D-03 dual gate met with audit evidence, OR NOT-PASS declared with triage + 20-18 seed, no bar-lowering); catalogues reflect the pervasive-threading + bounded least-fixpoint invariant + the `Code.via` discriminated-union addendum + PK16/PK17 amendments; new fallback classes backlogged; single non-stacked PR open with `closes #140`, #141 manual-close planned; post-merge artifact-verification documented INCLUDING `git merge-base --is-ancestor`.
  </done>
</task>

---

## WAVE DEPENDENCY GRAPH (acyclic — post-B-collapse)

```
0-1 (vendor)
  └─> A-1 (signature refactor + folded byte-identical proto gate; collapses prior Wave B)
        └─> C-1 (G1 chain-arg)
              └─> C-2 (G2 root-ident arm)
                    └─> D-1a (Code.via additive union + classifyLiteralRhs helper)
                          └─> D-1b (STRICT literal regex contract + unit test)
                                └─> D-1c (MANDATORY consumer audit + round-trip acceptance)
                                      └─> E-1 (bounded fixpoint + occurs-check; consumes D-1a helper)
                                            ├─> V-1 (D-03 DUAL gate; no bar-lowering)
                                            ├─> V-2 (bakery-140 fixture)
                                            └─> V-3 (cross-wave per-file loc-fidelity STOP)
                                                  └─> V-4 (SUMMARY + catalogues + single non-stacked PR)
   (V-3 also deps A-1,C-1,C-2,D-1a,D-1b,D-1c,E-1,V-2 ; V-4 deps V-1,V-2,V-3)
```

Wave graph: `0 → A → C → D → E → V`. Strictly serial through E-1 by
shared-surface coupling (all parser changes touch `parseStrudel.ts`).
The prior decorative Wave B ("observe the wire", 2/6 unchanged, no
source change) is COLLAPSED into A-1 — A-1's verify now folds in the
post-refactor proto re-run as its byte-identical gate. V-1 and V-2 are
independent (both only need E-1). V-3 is the cross-wave composition
drift gate. V-4 closes the phase.

## GOAL-BACKWARD VERIFICATION CHAIN

- GOAL: real-world parity ≥ 85.0% (D-03 criterion 2) AND `--LsnlgQ6osk` grounds STRUCTURED in production (D-03 criterion 1). BOTH required; NO bar-lowering escape.
- ← V-4 records the dated/SHA'd FRESH real-world % (crit 2) + the proto verdict (crit 1) + catalogue updates (pervasive-threading vyapti + the `Code.via` discriminated-union addendum + PK16/PK17) + the post-merge `git merge-base --is-ancestor` check + the manual close of #141. If crit 2 missed → NOT-PASS declared, residual triaged, dominant → 20-18 (no bar-lowering).
- ← V-3 proves the phase pre-mortem (silent offset drift) did NOT occur (per-file parity↔loc-fidelity correlation; the allow-list precisely bounded by the Fix-4 finding — the splice is loc-safe by the observed definition-site mechanism, so spliced-subtree files are NOT auto-added).
- ← V-2 locks `--LsnlgQ6osk`-class regressions as permanent CI (bakery-140-binding-transitive.strudel; STRUCTURED snapshot; provenance cited; parity-refresh exclusion).
- ← V-1 is the D-03 DUAL gate: proto criterion 1 + FRESH PK17-step-6 criterion 2. Both pass or the phase does NOT pass — no "user lowers the gate" option; residual → named classes → 20-18.
- ← E-1 (Wave E) makes the matcher line DERIVED: total + PTIME + order-independent fall out of the bounded least-fixpoint; cycle/dup-key/opaque-RHS fall to graceful Code via KEPT fences (occurs-check terminal = the kept opaque-fence predicate, byte-unchanged, repositioned post-fixpoint). Consumes D-1a's named `classifyLiteralRhs`. Synthetics (b)/(c)/(d)/OQ1-5c all pass. The Fix-4 finding (loc-fidelity.test.ts:82 mechanism) confirms the N splices are loc-safe.
- ← D-1c (Wave D) closes the D-02 consumer-ripple — every `via.`-reader + `tag === 'Code'` assumer guarded; the named round-trip test proves `.slow(numChords)` → `.slow(4)` emits `via.raw` VERBATIM (code-invariance); the MusicalTimeline literal-projection test proves the HIGH-severity `MusicalTimeline.tsx:298-299` site does not silently drop a literal node from the timeline projection. The enumeration is a grep-reproduced FLOOR (not exhaustive-final). The phase's PRIMARY risk surface, given its own task + verify.
- ← D-1b (Wave D) locks + unit-tests the STRICT literal-recognition contract (number | "…" | '…' → literal; `4 + 1`/template/expression → null; `raw` byte-equal = term-splicing, never evaluation).
- ← D-1a (Wave D) widens `Code.via` with the ADDITIVE `{literal:true;raw}` arm (existing arm byte-unchanged; fence byte-identical) + the named `classifyLiteralRhs` helper. RESOLVES the original-D-02 BLOCKER (unconstructible: `via` was the `wrapAsOpaque` shape) per the LOCKED CONTEXT D-02 CORRECTION.
- ← C-2 (Wave C) adds the G2 bound-ident-root arm in parseRoot, BEFORE the strict regex arms; the spliced subtree carries DEFINITION-SITE offset (PV49 preserved).
- ← C-1 (Wave C) threads `, bindings` into the G1 loose-recursive arm at pS:1052 — one-line behaviour change; the wire is end-to-end.
- ← A-1 (Wave A) is the BYTE-IDENTICAL signature refactor — `bindings?` added to applyChain + parseTransform + every internal recursion site they reach; every existing caller omits it; "any byte diff = STOP" + the folded proto-2/6-unchanged gate (the prior decorative Wave B collapsed in). keepNames:true enabled so the new param is a minification-stable P68 anchor.
- ← 0-1 (Wave 0) vendors the regression oracle (proto + 6 repros + vitest config + script) — captures the baseline (2/6 on main behaviour) durable across reboots. Without this, Wave A-E has no oracle.
- Matcher-stays-a-matcher: D-01 is term-splicing (`.fast` is never run); cycle / dup-key / opaque-RHS / destructuring / arrow / `${}` all fall to graceful Code via KEPT fences (D-02 CORRECTION widens per-binding classification with an ADDITIVE union arm but does NOT remove or alter the fence predicate — byte-identical, only repositioned post-fixpoint by E-1); the fixpoint is bounded by N (total/PTIME guarantee-by-construction); no module-level state (PV50 hazard avoided by D-01 G4 = optional-arg threading). The matcher line is now DERIVED.

## DOES THIS PLAN ACHIEVE THE GOAL? (closing check — anchored on the D-03 DUAL gate)

Yes, on the D-03 dual gate (BOTH criteria required; NO bar-lowering escape):

1. **The 4 gaps (G1/G2/G3/G4) are closed in dedicated waves with named line citations** (re-verified 2026-05-19 against live source) — pS:1052 G1, pS:957-ish G2 arm, the `Code.via` additive union + `classifyLiteralRhs` + buildBindingMap fixpoint-consumption G3, pS:1213/1829 + every recursion site G4. Each wave's verify cites the proto's per-iter diagnostic for the targeted shape.

2. **The D-02 BLOCKER is RESOLVED** — the original "store as Code-with-via, no ripple" was unconstructible (`via` is the `wrapAsOpaque` `{method,args,callSiteRange,inner}` shape; a literal `4` has none). Per the LOCKED CONTEXT D-02 CORRECTION: an ADDITIVE `{literal:true;raw}` union arm (existing arm byte-unchanged), a named `classifyLiteralRhs` helper, the opaque-fence predicate byte-identical (a literal sets `via !== undefined`), and a MANDATORY consumer audit (D-1c — the phase's primary risk surface, its own task + verify + round-trip acceptance; the enumeration is a grep-reproduced FLOOR with `MusicalTimeline.tsx:298-299` as a HIGH-severity guarded site walked through its own timeline-projection acceptance test). G3 is IN by principle (Strudel IS JavaScript), not measured frequency.

3. **The Option-b A-wave split is preserved + the decorative Wave B collapsed** — A-1 is the byte-identical signature refactor whose verify folds in the post-refactor proto-2/6-unchanged byte-identical gate (no separate observe-only wave; the plan-checker MAJOR dim 3/4 fixed). C/D/E are the behaviour changes, each individually committable + observable.

4. **The bounded least-fixpoint + occurs-check terminal** makes the matcher line DERIVED (total + PTIME + order-independent by the bound + monotonicity); the kept γ-3 fences are PRESERVED exactly — the opaque-fence predicate is byte-identical (`git diff` proves it), only repositioned post-fixpoint.

5. **The per-file loc-fidelity STOP gate** (per-wave + cross-wave V-3) is the realized phase pre-mortem. The Fix-4 finding is OBSERVED, not assumed: loc-fidelity.test.ts:82 does `src.slice(start,end)` with `src === code` (the single parsed string, def-site offsets); γ-3's existing stack-arg splice passes today by this same mechanism → the fixpoint's N splices are loc-safe by the same definition-site mechanism → the V-3 allow-list stays `{Wave-0} ∪ {V-2 fixture}`, NOT extended to spliced-subtree files.

6. **The proto vendoring (Wave 0)** makes the regression oracle durable + CI-runnable — the named D-01-eligible hard case (`--LsnlgQ6osk`) gets a per-wave observable verdict.

7. **D-03 scope discipline + NO bar-lowering** — NEW fallback classes at V-1 → backlog (NOT fixed); if criterion 2 misses, the phase is declared NOT-PASS, residual triaged into named classes, the dominant becomes a 20-18 — there is NO "user lowers the gate" escape (the plan-checker MINOR dim 5/6 fixed). The 4×-cascade lesson internalized; #149 + #153 remain backlog. The OQ1 fence-relaxation is OUT (closes ZERO of the 6 measured).

8. **Operational discipline** — the named COMMIT_TEMPLATE (structural heredoc, EXECUTOR NOTES; the zsh-substitution trap recurred 2× in 20-16), `git add -f` for `.anvi/`+`.planning/`, keepNames:true so P68 grep targets a minification-stable anchor (NOT a param name unless keepNames is on, NOT a comment), manual-close of #141 (GitHub closes-keyword limit), post-merge `git merge-base --is-ancestor` ancestry check (even for a non-stacked PR).

Residual risk: D-03 criterion 2 (parity ≥ 85.0%) is not met by the
mechanism alone if the residual fallbacks are dominated by NEW classes
unrelated to G1-G4 + fixpoint. Mitigation: V-1's classification of NEW
fallbacks is explicit; if criterion 2 fails, the phase does NOT pass
even if criterion 1 (`--LsnlgQ6osk` STRUCTURED) does — the residual is
triaged into named classes, each filed as backlog, the dominant becomes
a 20-18. NO bar-lowering. STOP semantics explicit. (With G3 now IN by
principle, criterion 2 is more likely met — but the no-escape contract
stands regardless.)

## EXECUTOR NOTES

- AnviDev: issue → branch → fix → test → observe → PR → self-review → merge. Issues #140 + #141 already filed. Branch FIRST (never code on main): `git checkout -b feat/20-17-d01-pervasive` from `main` (HEAD `aaae98c`). Single non-stacked PR → main; Claude never merges.
- `.anvi/` + `.planning/` are gitignored — `git add -f` for catalogue + planning artifacts. No Co-Authored-By. Commits use gitmoji + Problem/Fix body via the COMMIT_TEMPLATE below.
- **COMMIT_TEMPLATE (structural — every task's commit step says "Commit via COMMIT_TEMPLATE"; do NOT reconstruct from memory; the `feedback_commit_msg_heredoc.md` zsh-substitution trap recurred 2× in 20-16, so the heredoc form is made structural here):**
  ```
  COMMIT_TEMPLATE:
  git -c commit.gpgsign=false commit -q -F - <<'MSG'
  <gitmoji> <type>(<scope>): <summary>

  Problem: <what/why>
  Fix: <what/how>
  MSG
  ```
  NEVER `git commit -m "…"` with backticks or `$()` — zsh executes the substitution and silently strips it. The single-quoted heredoc (`<<'MSG'`) + `-F -` is the only sanctioned form.
- **GitHub closes-keyword limit:** the PR body may include `closes #140` only (GitHub honours ONE keyword's worth of auto-close per body — the 20-15/20-16 lesson). #141 must be manually closed on merge via `gh issue close 141 --comment "Closed by PR #N (single-closes-keyword limit)"`. V-4 actions 6 + 7 cover this.
- **P68 build hygiene per editor-src commit:** one-shot `pnpm --filter @stave/editor build`, then `grep -c <minification-stable-anchor> packages/editor/dist/index.js` > 0 BEFORE `git commit`. **The anchor must NOT be: a comment (stripped from dist), NOR a parameter name (minifiers RENAME parameters — this is why Wave A enables `keepNames: true` in the editor tsup/esbuild config; with keepNames the `bindings` param IS a stable anchor and so are function names).** Prefer: a string/regex literal unique to the wave (Wave C `bindings.has(trimmed)`, Wave D `classifyLiteralRhs` + the regex literal), a named export (`classifyLiteralRhs`/`codeLiteral`), or `descs.length` (a property-access string survives). The known pre-existing `@strudel/mondo` TS7016 may fire on the DTS step — verify it is THAT error, not a new one.
- **keepNames decision (Wave A, documented):** the editor build is configured `keepNames: true` so the optional-arg refactor's `bindings` parameter and the function names survive minification, making the P68 grep anchor a SUCCESSFUL-refactor positive (without keepNames, grepping `bindings` after a correct refactor false-negatives — the plan-checker MAJOR dim 2/4). Rationale + the config file changed are stated in A-1's commit body.
- **Per-file loc-fidelity STOP gate:** every parity-UNCHANGED file's loc-fidelity snapshot diff MUST be empty. The aggregate "parity unchanged AND loc non-empty" form is DEFEATED whenever a legitimate bare-Code → structured improvement lands on an existing corpus file; only the per-file correlation (empty-iff-parity-unchanged, with the explicit allow-list `{Wave-0} ∪ {V-2 fixture}`) isolates true silent offset drift. **The allow-list is NOT extended to "files containing spliced subtrees"** — loc-fidelity.test.ts:82 slices from the single parsed string at the definition-site offset; γ-3's existing splice passes today by this mechanism, so the fixpoint's N splices are loc-safe by the same mechanism (the Fix-4 finding, observed against the test source, not assumed).
- **vite-node import path:** the proto + any local test importing `parseExpression`/`parseStrudel` must use the deep path `../../../editor/src/ir/parseStrudel` from `packages/app/tests/parity-corpus/` (the `@stave/editor` barrel crashes standalone node via `@strudel/draw → gifenc`). Mirror `_bakery-classify.spec.ts:11-18`.
- **The parser is a STRUCTURAL MATCHER.** D-01 is SUBSTITUTION (splice the parsed tree), never EVALUATION. The fixpoint never runs `.fast` / never evaluates `${}` / never invokes an arrow body. A literal RHS becomes the `{literal:true;raw}` Code-with-via node where `raw` is the SOURCE TEXT byte-verbatim (`4` stays `"4"`, never the number `4`). A graceful Code-fallback via a KEPT fence is the correct outcome for any case the fixpoint can't resolve via substitution.
- **Post-merge artifact verification (NOT the badge):** HEAD == merge commit, 0/0 divergence; code grep present on main's `dist`; editor + parity-corpus counts hold (1564 + 33); AND `git merge-base --is-ancestor <PR-head-sha> main` exit 0. Near-vacuous for a single non-stacked PR but the EXACT 20-14 failure class (`feedback_stacked_pr_base_retarget.md`) — keep explicit.
- **Reusable patterns from 20-16:** the Option-b signature-refactor / behaviour-change split (now folded into A-1 with the byte-identical proto gate; no separate observe wave), the per-file loc-fidelity STOP gate (per-wave + cross-wave V-3), the COMMIT_TEMPLATE heredoc discipline, the manual-close-on-merge, the P68 grep-after-build (now with the keepNames + no-param-name caveat), the `git merge-base --is-ancestor` post-merge check, the BAKERY-FIXTURES.md provenance format, the parity-refresh.mjs exclusion, the vite-node `_*.spec.ts` maintainer-only config.
- **What's genuinely new in 20-17:** the prototype vendored as a runnable CI-adjacent oracle (Wave 0); pervasive optional-arg threading across applyChain + parseTransform + every recursion (Wave A, keepNames:true for the P68 anchor); the parseRoot G2 bound-ident-root arm BEFORE the strict regex arms (Wave C); the **`Code.via` ADDITIVE discriminated-union widen** + the named `classifyLiteralRhs` helper + the MANDATORY consumer audit (a grep-reproduced FLOOR, not an exhaustive-final list; `MusicalTimeline.tsx:298-299` is a HIGH-severity guarded site with its own timeline-projection acceptance test) + the round-trip acceptance test (Wave D — the D-02 CORRECTION, resolving the unconstructible-original-D-02 BLOCKER); the descriptor-list + bounded fixpoint loop + occurs-check terminal consuming the D-1a helper (Wave E); the D-03 DUAL gate with NO bar-lowering escape (V-1); a NEW vyapti entry for "binding substitution is pervasive optional-arg threading, never module-level mutable state" + a `Code.via` discriminated-union addendum.
