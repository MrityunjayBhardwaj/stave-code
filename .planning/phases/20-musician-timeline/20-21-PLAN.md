---
phase: 20-21
title: Final-residual `-7LU6zgzViSM` — `//`-comment-aware chain-arg walkers (closes P70 occurrence-9; new issue supersedes #143) + STRETCH backlog audit sweep
created: 2026-05-21T20:30:00Z
context: .planning/phases/20-musician-timeline/20-21-CONTEXT.md (D-01..D-04 LOCKED — do NOT relitigate)
research_seed: .planning/phases/20-musician-timeline/20-21-RESEARCH.md (R-1 9-wave bisect V0..V14 + W1/W3 + A8 RUN-isolated apostrophe-in-//-comment class / R-2 mechanism at parseStrudel.ts:2598-2628 + :2664-2747; reference pattern at :414-417 / R-3 fix shape with pre/post code blocks / R-4 per-issue backlog audit probe shapes + decision trees / R-5 fixture spec / R-6 gate measurement plan / R-7 5 pre-mortems)
depends_on: 20-20 MERGED (PR #162, merge ≥ `fa09cfe`); main HEAD must contain `fa09cfe` in ancestry. Baseline editor 1627/1627, app 413/413 (parity-corpus 49 + loc-fidelity 49), Bakery 98.0% N=50 sha f73b3956 stamp `2026-05-21T12-51-24-407Z`.
upstream_pin_sha: f73b395648645aabe699f91ba0989f35a6fd8a3c
runtime_version: "@strudel/core@1.2.6 / @strudel/transpiler@1.2.6 / @strudel/tonal@1.2.6 / @strudel/webaudio@1.3.0 / superdough@1.3.0"
baseline_parity: "98.0% (49/50, N=50, 2026-05-21T12-51-24-407Z, sha f73b3956) — the must-not-regress floor"
target_parity: "100.0% (50/50; closing -7LU6zgzViSM moves residual from 1 → 0; dual gate with must-not-regress 98.0% floor)"
gate: dual (`-7LU6zgzViSM` STRUCTURED in production via Wave-B permanent CI fixture asserting `body.tag !== 'Code'` AND fresh `pnpm parity:bakery --n 50` = 50/50 (100.0%) AND must-not-regress 98.0% floor)
prior_baseline_on_main: "editor 1627/1627, app 413/413 (parity-corpus 49 + loc-fidelity 49), main HEAD fa09cfe (20-20 merged PR #162 2026-05-21T13:36Z)"
issues_gate_bearing: ["<NEW-ISSUE-N> — to be filed at V-4 (P70 occurrence-9 — the actual class: chain-arg walkers do NOT skip `//` line comments before string-quote test); #143 stays closed as 'superseded by NEW-ISSUE-N'"]
issues_backlog_audit: ["#156", "#153", "#149", "#147"]
waves: 6 (0 RE-CONFIRM bisect + RUN-classify backlog / A two-site walker fix / B-A vendor 2 fixtures / B-B apply backlog dispositions / V-1 dual gate / V-3 cross-wave loc-fidelity STOP / V-4 phase close)
branch: feat/20-21-comment-aware-walkers
---

# Phase 20-21 — `//`-comment-aware chain-arg walkers (P70 occurrence-9 close) + STRETCH backlog audit

## GOAL (goal-backward anchor — anchored on the LOCKED D-04)

Two char-by-char walkers in `parseStrudel.ts` — `findMatchingParen`
(pS:2598-2628) and `splitArgsWithOffsets` (pS:2664-2747) — currently
test for string delimiters (`'` / `"` / `` ` ``) BEFORE checking
whether they're inside a `//` line comment. When an odd-count
apostrophe lives in a `// comment` inside chain-method arg parsing
(the `-7LU6zgzViSM` exemplar's second `.layer()` arrow contains
`// This is a green piece ma'am, we offset everything here o_0`),
the walker enters unterminated-string state and either returns `-1`
(findMatchingParen) or mis-attributes commas (splitArgsWithOffsets) —
the chain-arg parser falls back to bareCode. The reference correct
pattern lives in `splitTopLevelStatements:414-417`, which DOES skip
`//`-to-EOL before testing for string-quote delimiters. The fix
copies that exact branch into both new walker sites, preserving
loc-fidelity (PV49-spirit byte-additive consumption — the comment
chars stay in `current` for `splitArgsWithOffsets`'s offset contract
at pS:2685-2693; only `i` advances for `findMatchingParen`). No new
mechanism class; no new IR shape; no new top-level PatternIR tag.
This is the SAME existing pattern at TWO new call sites.

**The phase PASSES iff the LOCKED D-04 dual gate holds on the SAME
merge commit verified on real main `dist/index.js`:**

1. **HARD GATE crit-1:** A Wave-B-style permanent CI fixture
   (`bakery-143-apostrophe-in-chain-arg-comment.strudel`) asserts
   whole-program STRUCTURED (`body.tag !== 'Code'`) — i.e. the
   `-7LU6zgzViSM` exemplar parses through the chain-arg walker class
   without bareCode'ing. Companion negative-control
   (`bakery-143-NEGATIVE-no-apostrophe-comment.strudel`) MUST ALSO
   STRUCTURE (pre- AND post-fix) — locks the proposition that the
   apostrophe-in-`//`-comment IS the gate, not the chain shape.
2. **HARD GATE crit-2:** Fresh `pnpm parity:bakery --n 50` (PK17
   step-6 cadence; NEW ISO stamp; SAME upstream pin SHA
   `f73b3956`) reports **= 50/50 (100.0%)** AND **must-not-regress**
   the **49/50 (98.0%)** baseline floor. Closing `-7LU6zgzViSM` alone
   is the arithmetic +1 (residual `{-7LU6zgzViSM}` → `{}`).
3. **No-bar-lowering / no-second-workaround / no-push-through.** If
   crit-2 falsifies the 98.0% floor, OR crit-1 falsifies altogether,
   OR any wave's per-file loc-fidelity STOP gate fires → PK18
   cascade discipline; STOP; re-pose LOCKED decision to user with
   EVIDENCE; SCOPE may be amended on evidence; never the FLOOR.

Every wave below chains to this GOAL. Wave 0 RE-CONFIRMS the
RESEARCH §R-1 bisect on the fresh branch AND runs the §R-4 RUN-classify
probes for `{#156, #153, #149, #147}`. Wave A lands the two-site
walker fix. Wave B-A vendors the permanent fixtures (V-2 cadence).
Wave B-B applies backlog dispositions via `gh issue` calls. Wave V
is the dual-gate measurement + cross-wave loc-fidelity STOP + phase
close (V-1 / V-3 / V-4).

> **PV54 FLOOR-grep obligation — EXPLICITLY NOT TRIGGERED THIS
> PHASE.** No new top-level PatternIR `tag` ships in 20-21. The fix
> is purely walker-tolerance (a `//`-skip branch) inside two existing
> helper functions; no new `kind` constant, no new arm in any
> tag-discriminating switch, no consumer surface change. PV53/PV54
> consumer-audit obligation is dormant. Confirm explicitly in V-4
> catalogue addendum.

## CONTEXT — what 20-20 established + what RESEARCH proved (Chesterton's existence check)

20-20 (PR #162, merge `fa09cfe`) shipped `splitRootAndChain`'s
PV49-extension on identifier-to-paren whitespace + 2 permanent CI
fixtures. Crit-2 hit **98.0% N=50** (+2pp). One residual remained:
`-7LU6zgzViSM` (#143 — originally framed in 20-15 V-1 as the
"guarded-boot" idiom; 20-16 V-2 shipped `GUARDED_BOOT_RE` and CLOSED
`#143`; yet the exemplar STILL bareCodes — a P70 occurrence-9
candidate confirmed by RESEARCH §R-1's 9-wave bisect).

### What RESEARCH proved (§§R-1..R-7 verbatim — do NOT re-derive)

- **§R-1 (9-wave bisect — RUN by researcher 2026-05-21):** the
  blocker is NOT the guarded-boot prelude, NOT the trailing
  `// @version 1.0`, NOT the URL comment after the closing `)`,
  NOT the `.cpm` chain (a Code-with-via red herring), NOT the first
  `.layer()` arrow body. It IS the SECOND `.layer()` arrow body's
  `// ma'am`-style line comment with an ODD-count apostrophe.
  Verbatim outcomes (§R-1 Wave 5):
  - `x=>n("b").set(x)` → STRUCTURED (Code-via{cpm})
  - `x=>n("b").set(x)\n        // ma'am\n        .mode("c4")` → **bareCode**
  - Same minus the `// ma'am` line → STRUCTURED
  - With TWO apostrophes (`// ma'am ma'am`, EVEN count) → STRUCTURED
- **§R-2 (mechanism — GREP + RUN):** `findMatchingParen`
  (pS:2598-2628) and `splitArgsWithOffsets` (pS:2664-2747) both walk
  char-by-char, testing `if (ch === '"' || ch === "'" || ch === '`')`
  BEFORE checking for `//`. The reference correct pattern lives in
  `splitTopLevelStatements` at **pS:414-417**:
  ```ts
  if (ch === '/' && body[i + 1] === '/') {
    while (i < body.length && body[i] !== '\n') i++
    continue
  }
  ```
- **§R-3 (fix shape):** copy the `//`-skip branch from pS:414-417 into
  both new sites, placed BEFORE the string-quote test, AFTER the
  `if (inString)` branch. INTERNAL class (D-02 outcome 1; D-02 grounding
  outcome from CONTEXT) — the fix is local to our parser; NO upstream
  RESEARCH needed (acorn handles `//` natively in upstream).
- **§R-4 (backlog audit probe shapes):** per-issue RUN-classify probes
  + decision trees for `{#156, #153, #149, #147}`. CLASSIFY-only;
  do NOT FIX (per CONTEXT D-03 STRETCH discipline).
- **§R-5 (fixtures):** positive `bakery-143-apostrophe-in-chain-arg-comment.strudel`
  + NEGATIVE-control `bakery-143-NEGATIVE-no-apostrophe-comment.strudel`.
- **§R-6 (gate measurement):** dual gate; arithmetic 49+1=50.
- **§R-7 (5 pre-mortems):** PM-1 second blocker hidden behind first;
  PM-2 encoding/line-endings mismatch; PM-3 P50 workaround cascade;
  PM-4 loc-fidelity drift from splitArgsWithOffsets `//` skip; PM-5
  V-1 fresh measurement below floor.

### Live precedent shapes on real main `fa09cfe` (re-grep before editing — line numbers may drift)

| Mechanism | Live line (re-grep at Wave 0) | Shape |
|---|---|---|
| `findMatchingParen` function head | **`packages/editor/src/ir/parseStrudel.ts:2598`** | `function findMatchingParen(str: string, startIdx: number): number` |
| `findMatchingParen` string-quote test | **`packages/editor/src/ir/parseStrudel.ts:2611`** | `if (ch === '"' || ch === "'" || ch === '`') {` — the surgery site (skip inserted IMMEDIATELY BEFORE this line) |
| `splitArgsWithOffsets` function head | **`packages/editor/src/ir/parseStrudel.ts:2664`** | the OFFSET CONTRACT comment at pS:2685-2693 is load-bearing |
| `splitArgsWithOffsets` string-quote test | **`packages/editor/src/ir/parseStrudel.ts:2710`** | `if (ch === '"' || ch === "'" || ch === '`') {` — the surgery site (skip inserted IMMEDIATELY BEFORE this line, after `if (inString)` at pS:2704-2708) |
| Reference `//`-skip in `splitTopLevelStatements` | **`packages/editor/src/ir/parseStrudel.ts:414-417`** | the exact branch shape to mirror |
| `skipWhitespaceAndLineComments` primitive | **`packages/editor/src/ir/parseStrudel.ts:1075`** (per 20-20 OBSERVATIONS) | PV49 substrate; informational |
| `extractParenContent` caller of findMatchingParen | **`packages/editor/src/ir/parseStrudel.ts:2638`** | unchanged |

**Every `pS:` line above is a snapshot derived from RESEARCH §R-2 (read against the post-20-20 dist baseline). Every task's first action RE-GREPs the live anchor before editing.**

### The matcher line (D-02 — PRESERVE exactly)

The fix is purely permissive — it widens the recogniser surface to
match upstream's whitespace-tolerance (acorn parses `// ma'am` as a
comment, not a string-literal). It does NOT change the source string;
the `code` flows through unchanged. It does NOT add new state. It
does NOT introduce a new IR tag; PV54 stays dormant. For
`splitArgsWithOffsets` the comment chars are appended to `current`
(preserving the OFFSET CONTRACT at pS:2685-2693: `consumed =
skipWhitespaceAndLineComments(current, 0)` is the SINGLE source of
truth for the trimmed value's start; pre-existing primitive consumes
leading `// comment\n` already, so the new code path makes the
comment a LEADING-prefix of the arg's `current` — which the primitive
THEN consumes during `pushCurrent`). For `findMatchingParen` only `i`
advances; no source string ownership. The matcher line holds.

### Composition with 20-18 / 20-19 / 20-20 (Chesterton — COMPOSE, do NOT SUBSUME)

- 20-18's `CHAIN_ROOT_RECOGNISER` (Signal/Builder roots) — NOT touched.
- 20-19's `stripSideEffectStatements` — NOT touched. Operates on
  `splitTopLevelStatements` output BEFORE `buildBindingMap`.
- 20-20's `splitRootAndChain` PV49-extension — NOT touched. The
  identifier-to-paren whitespace tolerance ships unchanged at
  pS:2521-2531.
- The two walker fixes operate INSIDE the chain-arg parsing path
  (called from `splitArgs` (pS:2647), `extractParenContent` (pS:2638),
  and `applyMethod` paths for `.layer` / `.pick` / `splitFirstArg`).
  Fully decoupled from the 20-18/20-19/20-20 surfaces.

## PRE-MORTEM (the most-likely wrong-but-green outcome — P70 is the spine)

**The dominant risk (RESEARCH §R-7 PM-2):** the fix works on the
bisect-isolated synthetic variant (W1) but doesn't flip the production
exemplar `-7LU6zgzViSM`. Most-likely cause: encoding / line-ending
mismatch (real CRLF? UTF-8 BOM? smart-quote U+2019 vs ASCII U+0027?),
OR a SECOND `// comment` with an apostrophe elsewhere in the exemplar
body that the bisect didn't isolate. Mitigation: Wave 0 RE-CONFIRMS by
running the §R-1 bisect on the EXACT exemplar source extracted from
`samples-2026-05-21T12-51-24-407Z.json` (NOT from a TS string literal).
Wave A's post-fix W3 probe is the production-exemplar gate (verbatim
source). If W3 still bareCodes post-fix → PK18 STOP → re-bisect on
post-fix source.

**Second most-likely (RESEARCH §R-7 PM-4):** loc-fidelity drift from
`splitArgsWithOffsets`'s `//` skip. The OFFSET CONTRACT at pS:2685-2693
keeps `consumed` as the single source of truth for the trimmed value's
start. The fix APPENDS comment chars to `current` (the per-arg byte
buffer) — preserving the contract because `consumed` then advances
past the comment when `pushCurrent` runs (the existing
`skipWhitespaceAndLineComments` primitive already consumes leading
`//` comments). Mitigation: Wave A action 6's loc-fidelity probe
explicitly tests `splitArgsWithOffsets("/* never */ x=>1, // comment\n y=>2")`
and confirms the second arg's offset lands on the `y`, not on the `/`.
V-3 cross-wave full-corpus per-file STOP gate is the observational
final guard.

**Third (RESEARCH §R-7 PM-3):** P50 workaround cascade. If Wave A's
probe shows the fix doesn't flip and the urge fires to add a THIRD
`//`-skip elsewhere or to extend the apostrophe-handling logic with
special cases — STOP. The framing is wrong; re-pose D-01 with the new
evidence. Single decision per cycle.

**Fourth:** the backlog audit (Wave B-B) surfaces an issue whose
RUN-classify outcome differs from RESEARCH §R-4's expectation. By
CONTEXT D-03 discipline, CLASSIFY ≠ FIX — record the actual outcome
verbatim, apply the corresponding disposition from the decision tree
(close-as-superseded / close-as-duplicate / close-as-not-a-bug /
refine / file-new-issue), never expand the code surface. If a backlog
item turns out to be the SAME class as the gate-bearing fix, BONUS-CLOSE.

## WAVE 0 — RE-CONFIRM the §R-1 bisect on the fresh branch + RUN-classify the backlog; FROZEN scope lock

<task id="0-1" type="auto" depends_on="">
  <name>Branch; baseline RE-OBSERVATION; RE-RUN the §R-1 4 load-bearing bisect variants on real main `fa09cfe`; RUN-classify the 4 backlog issues; lock scope; pre-allocate V-3 allow-list</name>
  <read_first>
    .planning/phases/20-musician-timeline/20-21-CONTEXT.md (D-01..D-04 LOCKED — strict scope discipline);
    .planning/phases/20-musician-timeline/20-21-RESEARCH.md §R-1 (verbatim bisect outcomes) + §R-4 (per-issue probe shapes + decision trees) + §R-7 (5 pre-mortems);
    .planning/phases/20-musician-timeline/20-20-PLAN.md Wave 0 (the cadence template) + 20-20-SUMMARY.md (the 98.0% measurement + the deferred-backlog records);
    packages/editor/src/ir/parseStrudel.ts lines 410-430 (the reference `//`-skip in `splitTopLevelStatements` — confirm shape) + 2595-2628 (`findMatchingParen` — confirm string-quote test live line) + 2660-2750 (`splitArgsWithOffsets` — confirm string-quote test live line + OFFSET CONTRACT at pS:2685-2693);
    packages/app/tests/parity-corpus/.bakery-runs/samples-2026-05-21T12-51-24-407Z.json (extract the verbatim `-7LU6zgzViSM` source for Wave-0 RE-CONFIRMATION);
    .anvi/hetvabhasa.md P70 (this phase is candidate occurrence-9 — RUN before scope) + P68 (build hygiene) + P69 (grounding — DISCHARGED by RESEARCH; INTERNAL class);
    .anvi/krama.md PK18 (HARD-GATE cascade discipline) + PK17 (friction-first cycle);
    .anvi/vyapti.md PV49 (the load-bearing precedent — the fix is a PV49-spirit extension at NEW call sites; the reference branch at pS:414-417 IS the same `//`-tolerance idiom) + PV52 (fence predicate UNCHANGED) + PV54 (NOT triggered — confirm explicitly)
  </read_first>
  <files>
    (branch only — NO production src edits this wave)
    .planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md (NEW — the verbatim Wave-0 bisect record + baseline re-observation + backlog audit RUN-classify outcomes + FROZEN scope lock + expected V-3 allow-list)
  </files>
  <action>
    1. **Branch FIRST (never code on main):** `git checkout main && git pull && git rev-parse HEAD` (record verbatim) and `git merge-base HEAD fa09cfe` — confirm merge-base equals `fa09cfe` (main contains the 20-20 merge). Planning-doc-only commits on main since `fa09cfe` (e.g. 20-21 CONTEXT + RESEARCH) ACCEPTABLE. ANY production-src commit on main since `fa09cfe` triggers action-2 re-grep. `git checkout -b feat/20-21-comment-aware-walkers`. Confirm `git rev-parse --abbrev-ref HEAD` == `feat/20-21-comment-aware-walkers`.
    2. **Re-grep the live anchors** (line numbers may drift). Record the LIVE lines of:
       - `function findMatchingParen` (CONTEXT-cited pS:2598)
       - the string-quote test inside `findMatchingParen` (CONTEXT-cited pS:2611) — this is the surgery SITE 1 anchor
       - `function splitArgsWithOffsets` (CONTEXT-cited pS:2664)
       - the string-quote test inside `splitArgsWithOffsets` (CONTEXT-cited pS:2710) — this is the surgery SITE 2 anchor
       - the OFFSET CONTRACT comment in `pushCurrent` (CONTEXT-cited pS:2685-2693)
       - the reference `//`-skip in `splitTopLevelStatements` (CONTEXT-cited pS:414-417)
       Write the verified table into `20-21-OBSERVATIONS.md` (the source of record for every later task's `read_first` line numbers).
    3. **Baseline observation (Lokayata, not inference).** Run all of: `pnpm --filter @stave/editor test` (capture 1627/1627), `pnpm --filter @stave/app test` (capture 413/413; parity-corpus 49 + loc-fidelity 49 + other 315). Capture VERBATIM stdout into OBSERVATIONS. If ANY count differs from baseline → PK18 STOP, re-pose.
    4. **Extract the verbatim `-7LU6zgzViSM` source** from the baseline samples JSON:
       ```
       node -e "const j=JSON.parse(require('fs').readFileSync('packages/app/tests/parity-corpus/.bakery-runs/samples-2026-05-21T12-51-24-407Z.json','utf8'));console.log(j.samples.find(s=>s.hash==='-7LU6zgzViSM').code)"
       ```
       Record the verbatim text in OBSERVATIONS under "Wave 0 — `-7LU6zgzViSM` verbatim source". Confirm it contains the substring `// This is a green piece ma'am` (the apostrophe-in-comment trigger). Count the apostrophes in the comment line via:
       ```
       node -e "const j=JSON.parse(require('fs').readFileSync('packages/app/tests/parity-corpus/.bakery-runs/samples-2026-05-21T12-51-24-407Z.json','utf8'));const c=j.samples.find(s=>s.hash==='-7LU6zgzViSM').code;const lines=c.split('\\n');for(const l of lines){if(l.includes('//')&&l.includes(\"'\")){console.log(JSON.stringify(l),'apostrophes=',(l.match(/'/g)||[]).length)}}"
       ```
       Expected: at least one `//` line containing exactly ONE apostrophe. Capture verbatim.
    5. **RE-RUN the §R-1 bisect variants** on the fresh branch. Write a vite-node spec `_wave0-classify-20-21.spec.ts` (under `packages/editor/src/`; gitignored — mirror the 20-20 disposition). The probe imports `parseStrudel` from the deep path `./ir/parseStrudel`, runs the 4 load-bearing variants from RESEARCH §R-1 Waves 1/2/5, and stdout-prints `body.tag` + `body.via?.method` + `isCodeFallback` per variant:
       - **V0 full exemplar verbatim** (from action 4) — EXPECTED bareCode (`body.tag === 'Code'`, `via === undefined`); pre-fix baseline.
       - **V0 arrow2-simple** (the §R-1 Wave 4 falsifier): the V0 exemplar with the SECOND `.layer()` arrow body replaced with `x=>n("b").set(x)` (no `// ma'am` comment) — EXPECTED STRUCTURED (Code-via{cpm}); confirms the second arrow's `// ma'am` is the trigger.
       - **W1 minimal repro** (the §R-1 Wave 5 minimal): `stack( stack(sound("bd")).bank("R"), stack(chord("<G#m>").dict("ireal").layer( x=>n("a").set(x), x=>n("b").set(x)\n        // ma'am\n        .mode("c4"), )) ).cpm(28)` — EXPECTED bareCode pre-fix; isolates the mechanism to the smallest synthetic.
       - **W3 V0 minus apostrophe-comment** (the §R-1 Wave 5 falsifier): the V0 exemplar with the `// This is a green piece ma'am, we offset everything here o_0` line REMOVED — EXPECTED STRUCTURED; confirms removing the apostrophe-in-comment flips it.
       - **A8 V0 with `ma'am ma'am`** (4 apostrophes, EVEN count): the V0 exemplar with the comment text replaced with `// ma'am ma'am` (or similar EVEN-apostrophe count) — EXPECTED STRUCTURED; confirms ODD-count is the trigger.
       Run: `pnpm --filter @stave/editor exec vitest run src/_wave0-classify-20-21.spec.ts`. Capture VERBATIM stdout into OBSERVATIONS under "Wave 0 — §R-1 bisect RE-CONFIRMATION on `feat/20-21-comment-aware-walkers` from `fa09cfe`".
    6. **Lock scope per the §R-1 decision rule.** Confirm the Wave-0 V-1 stdout matches the §R-1 matrix exactly: V0/W1 bareCode; V0-arrow2-simple/W3/A8 STRUCTURED. If matches → D-01 LOCKS Outcome 1 → scope locks to the apostrophe-in-`//`-comment chain-arg walker class; gate-bearing fix is the two-site walker `//`-skip. If stdout DIVERGES → PK18 HARD STOP; record verbatim; re-pose D-01 to user. DO NOT proceed to fix selection.
    7. **RUN-classify the 4 backlog issues** per RESEARCH §R-4 probe shapes. Write a vite-node spec `_wave0-backlog-audit-20-21.spec.ts` (under `packages/editor/src/`; gitignored). Each probe stdout-prints the per-issue verdict. Per RESEARCH §R-4:
       - **#153 (multi-top-level siblings):** probe `parseStrudel('sound("hh hh hh hh")\nsound("[bd bd][sd bd] bd sd")\n\n// @version 1.0')` on real main `fa09cfe` (post-20-20). Per 20-20 SUMMARY (LAST-WINS upstream verdict GROUNDED + bakery-159 negative-control STRUCTURED), expected STRUCTURED. Disposition: CLOSE-as-superseded if STRUCTURED; else REFINE.
       - **#156 (uncategorised Code-fallback from 20-17 V-1):** the original artifact is `samples-2026-05-19T13-24-45-538Z.json`. Iterate every row through the CURRENT parser; record per-row verdict; compare to the original 20-17 "uncategorised" hash. Probe:
         ```ts
         import fs from 'node:fs'
         import { parseStrudel } from './ir/parseStrudel'
         const j = JSON.parse(fs.readFileSync('packages/app/tests/parity-corpus/.bakery-runs/samples-2026-05-19T13-24-45-538Z.json', 'utf8'))
         const isFB = (ir: any) => { const b = ir?.tag === 'Track' ? ir.body : ir; return b?.tag === 'Code' && b.via === undefined }
         for (const s of j.samples) console.log(s.hash, isFB(parseStrudel(s.code)) ? 'CODE' : 'STRUCTURED')
         ```
         Disposition tree per §R-4: if uncategorised hash now STRUCTURES → CLOSE-as-superseded; if matches a known class → REFINE; if uncategorised + still bareCodes + not in current sample → CLOSE-as-not-reproducible-in-current-sample.
       - **#149 (template-literal root + .cpm(binding)):** the original `-1j62z5xjyCN` was bonus-closed in 20-19 per project memory. Probe BOTH `-1j62z5xjyCN` AND `-72eEl7NwK9e` against the current sample (likely neither in current N=50):
         ```ts
         for (const target of ['-72eEl7NwK9e', '-1j62z5xjyCN']) {
           const row = samples.samples.find((s: any) => s.hash === target)
           if (!row) { console.log(target, 'NOT IN current sample'); continue }
           const ir: any = parseStrudel(row.code)
           const b = ir?.body ?? ir
           console.log(target, 'body.tag=', b.tag, 'via=', b.via?.method)
         }
         ```
         Disposition per §R-4: stale-hash → CLOSE-as-stale; present + structured → CLOSE-as-superseded; present + bareCode same-class-as-gate-bearing → BONUS-CLOSE inside this phase; otherwise REFINE.
       - **#147 (samples() side-channel capture):** NO probe; this is a FEATURE-deferred placeholder per the 20-16 D-03 strip-only decision. Disposition per §R-4: REFINE-with-product-note ("still deferred; awaits autocomplete/alias consumer to define the capture shape; no parser change pending") OR CLOSE-as-feature-out-of-scope-for-parity-work if user explicitly wants the placeholder retired. DEFAULT: REFINE-with-product-note (do NOT close without user sign-off per §R-4).
       Run: `pnpm --filter @stave/editor exec vitest run src/_wave0-backlog-audit-20-21.spec.ts`. Capture VERBATIM stdout into OBSERVATIONS under "Wave 0 — backlog audit RUN-classify". For each issue, record the verbatim verdict + the chosen disposition (CLOSE-as-X / REFINE / BONUS-CLOSE).
    8. **Lock the backlog dispositions.** Record in OBSERVATIONS under "Wave 0 — locked backlog dispositions":
       - `#153` — disposition: <CLOSE-as-superseded|REFINE>; comment-body text drafted (cite 20-20 RESEARCH §4 LAST-WINS grounding + 20-20 SUMMARY)
       - `#156` — disposition: <CLOSE-as-superseded|CLOSE-as-not-reproducible|REFINE>; comment-body text drafted
       - `#149` — disposition: <CLOSE-as-stale|CLOSE-as-superseded|REFINE|BONUS-CLOSE>; comment-body text drafted
       - `#147` — disposition: REFINE-with-product-note (DEFAULT); comment-body text drafted
       These are applied via `gh issue` calls in Wave B-B.
    9. **The `_wave0-*.spec.ts` files are gitignored** — one-off instruments; not permanent regression specs. Verify `.gitignore` excludes `_wave0-*.spec.ts` (mirror 20-20 pattern) OR delete after capturing stdout. The OBSERVATIONS record is the source of truth.
    10. **Pre-allocate the V-3 allow-list.** Record in OBSERVATIONS the expected V-3 allow-list:
        - 2 Wave-B-A fixtures: `bakery-143-apostrophe-in-chain-arg-comment.strudel` + `bakery-143-NEGATIVE-no-apostrophe-comment.strudel`
        - their auto-captured snapshot entries in `parity.test.ts.snap` + `loc-fidelity.test.ts.snap`
        Expected allow-list size: EXACTLY 2 fixtures (+ 2 snapshot entries). The V-3 STOP gate in Wave V-3: parity-UNCHANGED corpus files have empty loc-fidelity diffs; parity-CHANGED set ⊆ allow-list; ANY other moved file = silent drift = STOP.
    11. **Capture pre-fix P68 dist anchor baselines** (for Wave A's INCREASE-by-N validation). Run `pnpm --filter @stave/editor build` against the Wave-0 commit (BEFORE any production-src edit). Capture:
        ```
        grep -c "findMatchingParen" packages/editor/dist/index.js      # expected: > 0 (≥ 9 per 20-20 baseline, but re-measure on fa09cfe)
        grep -c "splitArgsWithOffsets" packages/editor/dist/index.js    # expected: > 0
        grep -c "splitTopLevelStatements" packages/editor/dist/index.js # expected: > 0 (reference shape; defensive — must remain unchanged)
        ```
        Record the exact integer counts in OBSERVATIONS as the pre-fix baselines.
    12. **Commit via COMMIT_TEMPLATE** (see EXECUTOR NOTES), gitmoji `:mag:`, `git add -f .planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md`. Problem: "the Wave-0 §R-1 bisect outcome + apostrophe-in-`//`-comment class identification + backlog audit dispositions are unobserved on the fresh branch (P70 trap if inferred from RESEARCH §R-1 alone); the production exemplar's exact source has not been character-verified against the synthetic W1 variant"; Fix: "re-ran editor/app baselines + the §R-1 4 load-bearing bisect variants (V0/V0-arrow2-simple/W1/W3/A8) on real main `fa09cfe` from a fresh branch with VERBATIM `-7LU6zgzViSM` extracted from samples JSON; ran §R-4 RUN-classify probes for #156/#153/#149/#147; locked Outcome 1 (single-blocker apostrophe-in-`//`-comment chain-arg walker class) + the 4 backlog dispositions + the expected V-3 allow-list + the pre-fix P68 dist anchor baselines as the regression record".
  </action>
  <ownership>
    This wave owns the EMPIRICAL Wave-0 §R-1 bisect record (the empirical premise every later wave depends on), the SCOPE lock to the apostrophe-in-`//`-comment class only, the backlog audit RUN-classify outcomes + dispositions for #156/#153/#149/#147 (applied in Wave B-B), the expected V-3 allow-list, and the pre-fix P68 dist anchor baselines. `20-21-OBSERVATIONS.md` is created + owned here; every later wave appends, never rewrites, its verbatim findings. The branch is created here. NO production code is owned/changed this wave.
  </ownership>
  <lifecycle>
    PK17 step 1-2 (measure → classify) + PK18 step 8 (vendor the gate oracle early). Runs FIRST: no fix / fixture / gate work may begin until Outcome 1 is RE-CONFIRMED on the fresh branch and the scope + backlog dispositions are FROZEN.
  </lifecycle>
  <pre_mortem>
    Most likely error (RESEARCH §R-7 PM-2): the Wave-0 V-1 stdout DIVERGES from RESEARCH §R-1 — V0 unexpectedly structures (encoding mismatch — the JSON-extracted source differs from the TS-string-literal RESEARCH used; e.g. smart-quote U+2019 vs ASCII U+0027). Mitigation: action 4 extracts verbatim from JSON; action 5 runs against THAT exact source; action 6 STOPS on divergence. If smart-quote IS the difference, the fix shape still applies (smart-quote is also a non-string-delimiter; the `//`-skip skips them too) — but record the discovery as a new occurrence in OBSERVATIONS.
    Second error (RESEARCH §R-7 PM-1): the §R-1 bisect missed a SECOND blocker hidden behind the apostrophe-in-comment one. Mitigation: action 5 runs W1 (minimal repro) AND V0 (full exemplar verbatim). If W1 bareCodes but the V0 verbatim WHITHER expected (still bareCode pre-fix), the bisect is consistent; the question is post-Wave-A. Wave A action 6's W3 production-exemplar probe is the gate.
    Third error: a missed line-number drift between RESEARCH-cited `pS:2598/2664/414` and the executor's working tree (a hotfix landed on main since RESEARCH ran). Mitigation: action 2's re-grep is non-optional.
    Fourth error: a backlog audit probe outcome surfaces a regression (some pre-20-21 row that was structured now bareCodes — a pre-existing 20-20 regression). Mitigation: action 7's full-row iteration captures verdicts; cross-check against the baseline 49/50 (the 49 structured rows in `samples-2026-05-21T12-51-24-407Z.json` MUST still structure on the fresh branch). If any does NOT, that is a P70-class regression on main — STOP, file hotfix issue, re-pose phase scope.
  </pre_mortem>
  <verify>
    1. `git rev-parse --abbrev-ref HEAD` == `feat/20-21-comment-aware-walkers` and `git merge-base feat/20-21-comment-aware-walkers main` contains `fa09cfe`.
    2. `pnpm --filter @stave/editor test` == 1627/1627; `pnpm --filter @stave/app test` == 413/413. Counts VERBATIM in OBSERVATIONS.
    3. Wave-0 §R-1 bisect stdout matches RESEARCH §R-1 exactly: V0 bareCode; V0-arrow2-simple STRUCTURED; W1 bareCode; W3 STRUCTURED; A8 STRUCTURED. VERBATIM in OBSERVATIONS.
    4. The verbatim `-7LU6zgzViSM` source contains `// This is a green piece ma'am` (exactly 1 apostrophe in the `//` line).
    5. Backlog audit dispositions LOCKED for all 4 issues; comment-body text drafted for each in OBSERVATIONS.
    6. Scope LOCKED to apostrophe-in-`//`-comment chain-arg walker class; gate-bearing fix is two-site walker `//`-skip; V-3 allow-list pre-allocated (2 fixtures + 2 snapshot entries).
    7. Pre-fix P68 dist anchor baselines captured (findMatchingParen N1, splitArgsWithOffsets N2, splitTopLevelStatements N3).
    8. NO production src changed (`git diff --name-only origin/main` shows only `.planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md` and, if not gitignored, `packages/editor/src/_wave0-*.spec.ts` — preferred is gitignored).
    9. Commit landed via COMMIT_TEMPLATE.
  </verify>
  <done>
    Branch `feat/20-21-comment-aware-walkers` from `fa09cfe`; editor 1627 / app 413 baselines RE-OBSERVED; Wave-0 §R-1 bisect matches RESEARCH §R-1 verbatim; Outcome 1 RE-CONFIRMED (apostrophe-in-`//`-comment class); backlog audit RUN-classify outcomes + dispositions LOCKED for #156/#153/#149/#147 (drafts for Wave B-B); expected V-3 allow-list pre-allocated; pre-fix P68 dist anchor baselines captured; commit via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE A — Two-site walker `//`-skip (the surgical edit)

<task id="A-1" type="auto" depends_on="0-1">
  <name>Insert the `//`-skip branch from `splitTopLevelStatements:414-417` into BOTH `findMatchingParen` (Site 1, pS:2598-2628) AND `splitArgsWithOffsets` (Site 2, pS:2664-2747), placed BEFORE the string-quote test and AFTER the `if (inString)` branch; prove the production exemplar `-7LU6zgzViSM` flips STRUCTURED via Wave-A W3 probe; preserve loc-fidelity by appending comment chars to `current` in Site 2 (OFFSET CONTRACT preserved)</name>
  <read_first>
    20-21-OBSERVATIONS.md (Wave-0 verified live line numbers; Outcome 1 scope lock; pre-fix dist anchor baselines);
    packages/editor/src/ir/parseStrudel.ts (the Wave-0 verified anchors: `findMatchingParen` pS:2598-2628, `splitArgsWithOffsets` pS:2664-2747, OFFSET CONTRACT at pS:2685-2693, reference `//`-skip in `splitTopLevelStatements` at pS:414-417);
    .planning/phases/20-musician-timeline/20-21-RESEARCH.md §R-2 (mechanism diagnosis with verbatim pre-fix code blocks) + §R-3 (fix shape with pre/post code blocks for both sites — Fix 1 for findMatchingParen / Fix 2 for splitArgsWithOffsets with comment-into-current handling) + §R-7 PM-4 (loc-fidelity drift analytical mitigation);
    .anvi/vyapti.md PV49 (definition-site loc-additivity — the substrate the fix extends; reference branch at pS:414-417 IS the PV49 idiom) + PV50 (no module state) + PV52 (fence predicate UNCHANGED);
    .anvi/krama.md PK16 (parser pipeline — the fix lives at stage 2 inside chain-arg parsing helpers);
    ~/.claude/projects/-Users-mrityunjaybhardwaj-Documents-projects-struCode/memory/feedback_editor_watch_mode.md (P68 — start watch BEFORE editing, verify watch is alive)
  </read_first>
  <files>
    packages/editor/src/ir/parseStrudel.ts (ADD: the `//`-skip branch inside `findMatchingParen` at Wave-0 verified pS:2598-2628; ADD: the same `//`-skip branch inside `splitArgsWithOffsets` at Wave-0 verified pS:2664-2747 — with comment chars appended to `current` per OFFSET CONTRACT; touch NOTHING else)
  </files>
  <action>
    1. **RE-GREP the live anchors** (mandatory per Wave-0 OBSERVATIONS). Confirm pS:2598 (`findMatchingParen` head), pS:2611 (string-quote test in Site 1), pS:2664 (`splitArgsWithOffsets` head), pS:2710 (string-quote test in Site 2), pS:2685-2693 (OFFSET CONTRACT comment in `pushCurrent`), pS:414-417 (reference `//`-skip). If anything diverges from Wave-0 OBSERVATIONS, update OBSERVATIONS and proceed against LIVE source.
    2. **START the editor watch (P68 + memory `feedback_editor_watch_mode.md`):** `pnpm --filter @stave/editor dev` in a background terminal BEFORE editing. Verify the watch is alive — a tsup-DTS-build failure terminates the watch entirely (does NOT degrade to JS-only). The known `@strudel/mondo` TS7016 warning is benign; any new error blocks editing.
    3. **Apply the surgical edit at Site 1 (`findMatchingParen` pS:2598-2628).** Today's code:
       ```ts
       function findMatchingParen(str: string, startIdx: number): number {
         let depth = 0
         let inString = false
         let stringChar = ''

         for (let i = startIdx; i < str.length; i++) {
           const ch = str[i]

           if (inString) {
             if (ch === stringChar && str[i - 1] !== '\\') inString = false
             continue
           }

           if (ch === '"' || ch === "'" || ch === '`') {
             // 20-15 G3 (#136): backtick is a string delimiter too — without
             // this a `` ` `` inside args (e.g. `stack(s(`bd hh`))`) would not
             // suppress paren/bracket counting and the matcher mis-depths.
             inString = true
             stringChar = ch
             continue
           }

           if (ch === '(' || ch === '[' || ch === '{') depth++
           if (ch === ')' || ch === ']' || ch === '}') {
             depth--
             if (depth === 0) return i
           }
         }

         return -1
       }
       ```
       Becomes (INSERT a `//`-skip branch IMMEDIATELY AFTER the `if (inString)` block and BEFORE the string-quote test):
       ```ts
       function findMatchingParen(str: string, startIdx: number): number {
         let depth = 0
         let inString = false
         let stringChar = ''

         for (let i = startIdx; i < str.length; i++) {
           const ch = str[i]

           if (inString) {
             if (ch === stringChar && str[i - 1] !== '\\') inString = false
             continue
           }

           // 20-21 (P70 occurrence-9; supersedes #143 framing) — skip `//`
           // line comments BEFORE the string-quote test so an odd-count
           // apostrophe inside a `// comment` (e.g. `// ma'am`) does NOT
           // put the walker into unterminated-string state and mis-depth
           // the parens. Mirrors the reference branch at
           // splitTopLevelStatements:414-417 exactly. PV49-spirit: line-
           // comment tolerance at a NEW walker call site (chain-arg
           // depth-matcher) — not a new mechanism class.
           if (ch === '/' && str[i + 1] === '/') {
             while (i < str.length && str[i] !== '\n') i++
             continue
           }

           if (ch === '"' || ch === "'" || ch === '`') {
             // 20-15 G3 (#136): backtick is a string delimiter too — without
             // this a `` ` `` inside args (e.g. `stack(s(`bd hh`))`) would not
             // suppress paren/bracket counting and the matcher mis-depths.
             inString = true
             stringChar = ch
             continue
           }

           if (ch === '(' || ch === '[' || ch === '{') depth++
           if (ch === ')' || ch === ']' || ch === '}') {
             depth--
             if (depth === 0) return i
           }
         }

         return -1
       }
       ```
       Note: the `continue` after the `while` is sufficient — `i` lands on `\n` at the end of the inner `while`, the outer `for`'s `i++` then advances past `\n`. Mirrors pS:414-417's exact shape (the inner `while` does NOT consume `\n`; the outer `i++` does).
    4. **Apply the surgical edit at Site 2 (`splitArgsWithOffsets` pS:2664-2747).** Today's code at the string-quote test (pS:2710):
       ```ts
       if (ch === '"' || ch === "'" || ch === '`') {
         // 20-15 G3 (#136): backtick is a string delimiter — a `,` inside a
         // `` `…` `` arg must NOT split it (e.g. `stack(s(`bd, hh`))`), and
         // a backtick inside an arg must not let comma-splitting see commas
         // that belong to the template literal.
         inString = true
         stringChar = ch
         if (current.length === 0) currentStart = i
         current += ch
         continue
       }
       ```
       Insert IMMEDIATELY AFTER the `if (inString)` block (pS:2704-2708) and BEFORE the above string-quote test:
       ```ts
       // 20-21 (P70 occurrence-9; supersedes #143 framing) — skip `//`
       // line comments BEFORE the string-quote test so an odd-count
       // apostrophe inside a `// comment` (e.g. `// ma'am`) does NOT put
       // the walker into unterminated-string state and mis-attribute commas
       // as arg separators. Mirrors splitTopLevelStatements:414-417.
       //
       // OFFSET CONTRACT (PV49-spirit; mitigates RESEARCH §R-7 PM-4): the
       // comment chars are APPENDED to `current` (the per-arg byte buffer)
       // so the per-arg byte slice stays byte-identical to the source.
       // `pushCurrent` already consumes leading `// comment\n` via
       // `skipWhitespaceAndLineComments(current, 0)` (pS:2685-2693), so
       // the trimmed `value` + `offset` lockstep is preserved — the
       // comment is treated as leading-prefix the same way the existing
       // primitive handles leading whitespace.
       if (ch === '/' && argsStr[i + 1] === '/') {
         if (current.length === 0) currentStart = i
         while (i < argsStr.length && argsStr[i] !== '\n') {
           current += argsStr[i]
           i++
         }
         // Append the `\n` itself if we hit one (preserves byte-additive
         // semantics; the outer for-loop's i++ would otherwise skip it).
         if (i < argsStr.length) {
           current += argsStr[i]
         }
         continue
       }

       if (ch === '"' || ch === "'" || ch === '`') {
         // ... (existing 20-15 G3 block unchanged) ...
       }
       ```
       **CRITICAL invariants preserved:**
       - The `current` buffer stays byte-identical to `argsStr` (a perfect substring) — the OFFSET CONTRACT at pS:2685-2693 holds.
       - `currentStart` is set ONLY if `current` is empty when the comment starts — same idiom as the existing branches (depth-bracket / fall-through).
       - The `continue` lands on the outer `for`'s `i++`, advancing past whatever character `i` is on after the `\n` append (or EOL). The outer increment is fine because the inner `while`'s `i++` and the final `current += argsStr[i]` left `i` at the `\n`; the outer `i++` then moves past it.
    5. **Verify additive-only on the surgery sites.** `git diff packages/editor/src/ir/parseStrudel.ts` MUST show ONLY:
       (a) the new `//`-skip branch inside `findMatchingParen` (Site 1) — additive
       (b) the new `//`-skip branch inside `splitArgsWithOffsets` (Site 2) — additive
       ZERO edits to: the bare-string arm, the backtick arm, the existing `if (inString)` branches at both sites, the existing string-quote tests, the existing depth-bracket branches, the existing comma-split branch in Site 2, the `pushCurrent` body at pS:2674-2699, the OFFSET CONTRACT comment at pS:2685-2693, the final `pushCurrent()` + `return args` at pS:2745-2746, any other function in `parseStrudel.ts`. State this in the commit body.
    6. **P68 build hygiene with MINIFICATION-STABLE anchors.** One-shot `pnpm --filter @stave/editor build`. Grep counts (4 distinct):
       ```
       grep -c "findMatchingParen" packages/editor/dist/index.js
       grep -c "splitArgsWithOffsets" packages/editor/dist/index.js
       grep -c "splitTopLevelStatements" packages/editor/dist/index.js
       grep -c "skipWhitespaceAndLineComments" packages/editor/dist/index.js
       ```
       Per-anchor expectations:
       - `findMatchingParen` — > 0 AND ≥ pre-fix baseline N1 (the function name + call sites unchanged). Defensive: confirms the symbol stays.
       - `splitArgsWithOffsets` — > 0 AND ≥ pre-fix baseline N2 (defensive: symbol stays; called from `splitArgs` at pS:2647).
       - `splitTopLevelStatements` — UNCHANGED N3 (defensive: reference function NOT touched).
       - `skipWhitespaceAndLineComments` — UNCHANGED vs 20-20's count of 7 (defensive: this primitive is NOT a new call site; the fix mirrors `splitTopLevelStatements`'s INLINE `//`-skip branch rather than calling the primitive; this is intentional to match the reference shape exactly per RESEARCH §R-3).
       All 4 counts > 0; counts captured in commit body. The known `@strudel/mondo` TS7016 may fire on DTS — verify it is THAT error, not new.
    7. **Wave-A throwaway probe** (`_waveA-walker-fix-20-21.spec.ts`, gitignored under `packages/editor/src/`). The spec, via the production `parseStrudel` imported from `./ir/parseStrudel`, records (with stdout-print, no assertions — the OBSERVATIONS capture IS the gate):
       - **(i) W1 minimal post-fix** (the §R-1 Wave 5 minimal):
         `parseStrudel('stack( stack(sound("bd")).bank("R"), stack(chord("<G#m>").dict("ireal").layer( x=>n("a").set(x), x=>n("b").set(x)\n        // ma\\'am\n        .mode("c4"), )) ).cpm(28)')` — POST-FIX EXPECTED STRUCTURED (Code-via{cpm}); pre-fix was bareCode.
       - **(ii) W3 production-exemplar verbatim** (THE GATE-BEARING OBSERVATION):
         `parseStrudel(<verbatim -7LU6zgzViSM source from samples-2026-05-21T12-51-24-407Z.json, exactly as extracted in Wave 0 action 4>)` — POST-FIX EXPECTED STRUCTURED. If THIS bareCodes post-fix, the fix doesn't flip the production exemplar → PK18 STOP (§R-7 PM-1 or PM-2 confirmed).
       - **(iii) false-positive guards** (NEVER false-flip — preserve today's behaviour):
         - `parseStrudel('stack(sound("ma\\'am"))')` — actual JS string with apostrophe inside double-quotes (NOT inside a `//`); POST-FIX EXPECTED STRUCTURED (unchanged); the `//`-skip is correctly gated by `inString` check FIRST.
         - `parseStrudel('stack(sound("hh"))    // not a comment chained here')` — top-level `//` after a top-level call; STRUCTURED unchanged (the `//` outside chain-args is handled by `splitTopLevelStatements` already; this confirms no interaction).
         - `parseStrudel('stack(sound("a"), /* block */ sound("b"))')` — block comments NOT affected by this fix (block comment handling is at pS:418-426 in `splitTopLevelStatements`; chain-arg walkers currently don't handle block comments — that's a separate class and OUT of scope for 20-21). EXPECTED disposition: unchanged (if today it structures, still structures; if today it bareCodes, still bareCodes).
       - **(iv) loc-fidelity offset check** (RESEARCH §R-7 PM-4 mitigation):
         The probe calls `splitArgsWithOffsets("x => 1, // comment with ma'am\n y => 2")` directly (import via `(await import('./ir/parseStrudel')).__test__` if exported, else inline a test function in the spec that imports the internal helper via dynamic import; if not accessible, fall back to asserting the outer `parseStrudel` end-to-end produces correct `loc` ranges via a small synthetic with a `// ma'am` comment between args). Expected: arg 0 = `x => 1` (offset = 0); arg 1 = `y => 2` (offset = position of `y`, NOT position of `/`). If the offset of arg 1 lands on a `/` or a space character — the OFFSET CONTRACT broke; PK18 STOP.
       - **(v) NEGATIVE-control sanity** (will be vendored as a fixture in Wave B-A; pre-test here):
         `parseStrudel(<W1 with `ma\\'am` replaced with `maam`>)` — STRUCTURED both pre- and post-fix.
       Run: `pnpm --filter @stave/editor exec vitest run src/_waveA-walker-fix-20-21.spec.ts`. Capture VERBATIM stdout into OBSERVATIONS under "Wave A — strict-widen W1+W3 + false-positive + loc-fidelity probe". If ANY (i) does NOT flip, (ii) does NOT flip (THE gate), (iii) changes disposition, (iv) loc-fidelity drifts, or (v) NEGATIVE-control regresses → PK18 STOP.
    8. **Run the test gates (must hold — no expected breaks this wave).**
       - `pnpm --filter @stave/editor test` → MUST hold at 1627/1627. The `//`-skip is purely-additive; no editor test directly asserts on the chain-arg walker's apostrophe-in-comment behaviour today.
       - `pnpm --filter @stave/app test` → MUST hold at 413/413. Wave B-A fixtures not yet vendored. Pre-existing fixtures unaffected by construction (the fix is strictly permissive per §R-7 analytical mitigation).
       Record VERBATIM in OBSERVATIONS.
    9. **Per-file loc-fidelity STOP gate (per-wave).** Inside `pnpm --filter @stave/app test` (action 8), the loc-fidelity test asserts every IR `loc` slices into source byte-exact. PER-WAVE rule:
       - EVERY parity-UNCHANGED corpus file's loc-fidelity diff MUST be EMPTY;
       - the parity-CHANGED set ⊆ the expected V-3 allow-list (today: NONE in Wave A — fixtures land in Wave B-A);
       - ANY moved file = silent drift = STOP (RESEARCH §R-7 PM-4 / PV49 violation).
       The fix preserves the OFFSET CONTRACT by construction (comment chars stay in `current`; the existing primitive's leading-comment skip handles them at `pushCurrent` time). Loc-fidelity carries; verify per-wave. Record the actual moved set verbatim (expected: EMPTY).
    10. **Commit via COMMIT_TEMPLATE**, gitmoji `:sparkles:`. `git add -f packages/editor/src/ir/parseStrudel.ts .planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md`. Problem: "findMatchingParen + splitArgsWithOffsets do NOT skip `//` line comments before the string-quote test (pS:2611 + pS:2710 respectively); an odd-count apostrophe inside `// ma'am` (e.g. `-7LU6zgzViSM` second `.layer()` arrow body's `// This is a green piece ma'am...`) enters unterminated-string state — findMatchingParen returns -1 (root call-end not found) AND/OR splitArgsWithOffsets mis-attributes commas (sibling args mis-split); chain-arg parse falls back to bareCode (Wave-0 §R-1 RE-CONFIRMED on production exemplar; P70 occurrence-9 — 20-16 cascade classified #143 as guarded-boot prelude, but the actual blocker is in the chain-arg walkers)"; Fix: "copy the reference `//`-skip branch from splitTopLevelStatements:414-417 into BOTH walker sites at pS:2598-2628 + pS:2664-2747, placed BEFORE the string-quote test and AFTER the `if (inString)` branch; in splitArgsWithOffsets, append comment chars to `current` to preserve the OFFSET CONTRACT at pS:2685-2693 (PV49-spirit byte-additive consumption — the existing `skipWhitespaceAndLineComments` primitive at pushCurrent consumes leading `// comment\\n`s when the arg starts with one); editor 1627/1627; app 413/413; W1 + W3 (production exemplar) FLIP STRUCTURED; (iii) false-positives preserve disposition (apostrophe-in-double-quoted-string unaffected; gated by `inString` first); (iv) loc-fidelity offset preserved (arg 1 offset lands on `y`, not on `/`); (v) NEGATIVE-control unchanged; per-file loc-fidelity STOP gate clean".
  </action>
  <ownership>
    This wave owns the IMPLEMENTATION (the new `//`-skip branch at Site 1 + Site 2; the `current += argsStr[i]` comment-byte-append handling for Site 2's OFFSET CONTRACT). The reference `//`-skip pattern itself (the splitTopLevelStatements:414-417 branch) is owned by its prior phase. PV49-spirit loc-additivity is owned by the byte-additive comment-consumption (R-5 / §R-7 PM-4 analytically grounded). Site 1's depth-tracking + Site 2's comma-split logic remain owned by their prior phase — byte-unchanged.
  </ownership>
  <lifecycle>
    PK16 — the new walker tolerance inserts INSIDE `findMatchingParen` and `splitArgsWithOffsets`, both called from chain-arg paths (`splitRootAndChain`, `splitArgs`, `applyMethod`'s `.layer`/`.pick` arms). PK16 stage numbering unchanged: the fix lives at stage 2 (chain-arg processing) and does NOT cross any stage boundary. Runs after Wave 0 (Outcome 1 must be RE-CONFIRMED) and BEFORE Wave B-A (the permanent fixture is vendored only after the surgical edit + Wave-A probe both confirm the FLIP signal).
  </lifecycle>
  <pre_mortem>
    Most likely error (RESEARCH §R-7 PM-1, P70 occurrence-9 candidate): the W1 flips but the W3 production exemplar does NOT — meaning a SECOND blocker is hidden behind the apostrophe-in-comment one. Mitigation: action 7 (ii) is the gate-bearing observation; if W3 still bareCodes post-fix, STOP, re-bisect on post-fix source.
    Second error (RESEARCH §R-7 PM-4): loc-fidelity drift from the Site 2 `//`-skip — the comment chars appended to `current` corrupt the offset arithmetic. Mitigation: action 7 (iv) is the direct test; the OFFSET CONTRACT comment at pS:2685-2693 is byte-unchanged; `pushCurrent`'s `consumed = skipWhitespaceAndLineComments(current, 0)` IS the existing leading-`//` skip (which means the contract is preserved as long as `current` stays byte-identical to the original `argsStr` slice — which the action-4 code does).
    Third error (RESEARCH §R-7 PM-3): the fix doesn't flip and the urge fires to add a THIRD `//`-skip elsewhere or to extend the apostrophe logic. STOP. The framing is wrong; re-pose D-01.
    Fourth error: a false-positive — an existing corpus row that parses structured today now bareCodes because of an interaction the §R-7 analysis didn't anticipate. Mitigation: action 9's per-file loc-fidelity STOP gate runs on the full corpus; ANY non-allow-list movement = STOP.
    Fifth error: minifier elided the `//`-skip branch as dead code (analyzed away because the inner `while` looked like a no-op to the optimiser). Mitigation: action 6's grep counts confirm the symbols remain in the bundle; the W3 probe (a vite-node run against the source, NOT the bundle) would still catch a source-level bug. For the BUNDLE behaviour, the V-1 PK17 step-6 fresh measurement is the ultimate end-to-end check.
  </pre_mortem>
  <verify>
    1. `git diff packages/editor/src/ir/parseStrudel.ts` shows ONLY the two additive `//`-skip branches at Sites 1 + 2. ZERO edits to: all other functions, the OFFSET CONTRACT comment, the existing string-quote tests, the depth-bracket branches, the comma-split branch.
    2. `pnpm --filter @stave/editor build` succeeds (only known `@strudel/mondo` TS7016 may fire). 4 grep counts captured: `findMatchingParen` ≥ pre-fix N1; `splitArgsWithOffsets` ≥ pre-fix N2; `splitTopLevelStatements` == N3 (unchanged); `skipWhitespaceAndLineComments` == 7 (unchanged from 20-20). All recorded in commit body.
    3. The Wave-A probe (action 7) GREEN + recorded VERBATIM in OBSERVATIONS:
       - (i) W1 minimal flips bareCode → STRUCTURED (Code-via{cpm});
       - (ii) W3 production exemplar `-7LU6zgzViSM` flips bareCode → STRUCTURED (THE gate-bearing observation);
       - (iii) all 3 false-positive cases preserve disposition;
       - (iv) loc-fidelity offset for `x => 1, // ma'am\n y => 2` lands arg 1 on `y` (offset == position of `y`, NOT `/`);
       - (v) NEGATIVE-control (`// maam` no apostrophe) STRUCTURED.
    4. `pnpm --filter @stave/editor test` == 1627/1627.
    5. `pnpm --filter @stave/app test` == 413/413.
    6. Per-file loc-fidelity STOP gate: every parity-UNCHANGED corpus file's loc-fidelity diff EMPTY; the parity-CHANGED set is EMPTY (Wave A makes NO fixture changes); ANY moved file = STOP (none observed).
    7. Commit via COMMIT_TEMPLATE.
  </verify>
  <done>
    Two-site walker `//`-skip landed at `pS:2598-2628` (Site 1) + `pS:2664-2747` (Site 2); `git diff` additive-only; P68 build-hygiene grep counts captured (defensive symbols present; `splitTopLevelStatements` + `skipWhitespaceAndLineComments` counts UNCHANGED, confirming reference + primitive untouched); W1 + W3 (production exemplar) + false-positive + loc-fidelity + NEGATIVE-control probe all GREEN; editor 1627/1627; app 413/413; per-file loc-fidelity STOP gate clean; commit via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE B-A — Permanent CI fixture vendoring (V-2 cadence)

<task id="B-A" type="auto" depends_on="A-1">
  <name>Vendor 2 permanent CI fixtures under `packages/app/tests/parity-corpus/` — `bakery-143-apostrophe-in-chain-arg-comment.strudel` (canonical positive distillation of the apostrophe-in-`//`-comment chain-arg class) + `bakery-143-NEGATIVE-no-apostrophe-comment.strudel` (negative control proving the apostrophe is the gate); update BAKERY-FIXTURES.md with provenance citing P70 occurrence-9 + the `splitTopLevelStatements:414-417` reference pattern</name>
  <read_first>
    20-21-OBSERVATIONS.md (the Wave-0 expected V-3 allow-list + the Wave-A W3 flip evidence);
    packages/app/tests/parity-corpus/BAKERY-FIXTURES.md (the V-2 vendoring convention; auto-discovered by `parity.test.ts` + `loc-fidelity.test.ts`; structurally excluded from `parity-refresh.mjs` TARGETS via the `parity-refresh.mjs:68-75` `bakery-*` slug guard);
    packages/app/tests/parity-corpus/bakery-159-tokenizer-whitespace.strudel + bakery-159-NEGATIVE-no-whitespace.strudel (the 20-20 V-2 positive + negative-control templates);
    packages/app/tests/parity-corpus/bakery-143-guarded-boot.strudel (the existing 20-16 V-2 fixture — KEEP unchanged per RESEARCH §R-5; its provenance is the GUARDED_BOOT_RE recognition coverage; removing would lose 20-16's anti-drift);
    packages/app/tests/parity-corpus/.bakery-runs/samples-2026-05-21T12-51-24-407Z.json (the verbatim `-7LU6zgzViSM` row — already extracted in Wave 0 action 4);
    packages/app/scripts/parity-refresh.mjs lines 68-75 (the structural exclusion guard);
    .planning/phases/20-musician-timeline/20-21-RESEARCH.md §R-5 (fixture spec — canonical positive minimal distillation + NEGATIVE-control + the stretch shape closer to the actual exemplar) + §R-6 (gate measurement plan)
  </read_first>
  <files>
    packages/app/tests/parity-corpus/bakery-143-apostrophe-in-chain-arg-comment.strudel (NEW — the canonical positive: minimal distillation of the chain-arg + apostrophe-in-`//`-comment shape);
    packages/app/tests/parity-corpus/bakery-143-NEGATIVE-no-apostrophe-comment.strudel (NEW — the negative control: same shape minus the apostrophe in the `//`);
    packages/app/tests/parity-corpus/BAKERY-FIXTURES.md (UPDATE — add a 20-21 section documenting both fixtures + the P70 occurrence-9 framing + the reference-pattern note);
    packages/app/tests/parity-corpus/__snapshots__/parity.test.ts.snap (UPDATE — auto-captured on first run);
    packages/app/tests/parity-corpus/__snapshots__/loc-fidelity.test.ts.snap (UPDATE — auto-captured on first run)
  </files>
  <action>
    1. **Confirm Wave-0 disposition.** From OBSERVATIONS: Outcome 1 LOCKED; scope = apostrophe-in-`//`-comment chain-arg walker class only. The fixture set is {1 canonical positive + 1 negative-control} per RESEARCH §R-5.
    2. **Confirm `bakery-143-guarded-boot.strudel` STAYS** (per RESEARCH §R-5 final paragraph — its provenance is the 20-16 V-2 GUARDED_BOOT_RE ship; KEEP for anti-drift on that separate recognition surface). The new fixtures are ADDITIVE; the 143-prefix is now split across TWO classes (guarded-boot recognition + apostrophe-in-comment chain-arg walker), documented in BAKERY-FIXTURES.md.
    3. **Write the canonical positive fixture** `bakery-143-apostrophe-in-chain-arg-comment.strudel` per RESEARCH §R-5. Minimal distillation: just enough chain-arg + apostrophe-in-comment shape to reproduce the class. Content (per §R-5 minimal shape):
       ```strudel
       stack(
         stack(sound("bd")).bank("R"),
         stack(chord("<G#m>").dict("ireal").layer(
           x=>n("a").set(x),
           x=>n("b").set(x)
               // ma'am
               .mode("c4"),
         ))
       ).cpm(28)
       ```
       Exact content; LF newlines; final newline; no trailing whitespace. The fixture's parity snapshot will assert `body.tag !== 'Code'` OR `body.via !== undefined` (the `.cpm` `via`-wrap is acceptable per the structured-opaque shape — Code-with-via STRUCTURES per the parity oracle).
    4. **Write the negative-control fixture** `bakery-143-NEGATIVE-no-apostrophe-comment.strudel`. Same shape MINUS the apostrophe. Content:
       ```strudel
       stack(
         stack(sound("bd")).bank("R"),
         stack(chord("<G#m>").dict("ireal").layer(
           x=>n("a").set(x),
           x=>n("b").set(x)
               // maam
               .mode("c4"),
         ))
       ).cpm(28)
       ```
       Exact content; LF newlines; final newline. Pre-fix AND post-fix this fixture parses STRUCTURED (the apostrophe-in-comment is not engaged); it locks the proposition "the apostrophe-in-`//`-comment IS the gate, not the chain shape" — if positive bareCodes but NEGATIVE structures pre-fix, fix-shape is right; if both bareCode pre-fix, mechanism class is different (would have shown in Wave 0 §R-1 W3); if both structure pre-fix, the Wave-0 bisect was wrong (PK18 STOP).
    5. **Update `BAKERY-FIXTURES.md`** with a new section "Phase 20-21 fixtures (P70 occurrence-9 — apostrophe-in-`//`-comment chain-arg walker class; supersedes the original #143 guarded-boot framing)". Documentation:
       > Two char-by-char walkers in `parseStrudel.ts` (`findMatchingParen`
       > at pS:2598-2628 + `splitArgsWithOffsets` at pS:2664-2747) handle
       > string-quote detection but originally did NOT skip `//` line
       > comments before testing for string delimiters. An odd-count
       > apostrophe inside a `// line comment` inside chain-method arg
       > parsing put the walker into unterminated-string state →
       > `findMatchingParen` returns -1 (root call-end not found) OR
       > `splitArgsWithOffsets` mis-attributes commas → chain-arg parse
       > bareCodes. The reference correct pattern lives in
       > `splitTopLevelStatements:414-417` (which DOES skip `//`-to-EOL
       > before the string-quote test). The 20-21 fix copies that exact
       > branch into both walker sites.
       >
       > Provenance: P70 occurrence-9 — the original `#143` (filed in
       > 20-15 V-1, closed by 20-16 V-2's `GUARDED_BOOT_RE` recognition
       > extension) was the wrong class. The actual blocker for the
       > `-7LU6zgzViSM` exemplar is the chain-arg walker's `//`-comment
       > intolerance. A NEW issue tracks the actual class (filed in
       > 20-21 V-4); `#143` stays closed as superseded.
       Also note that `bakery-143-guarded-boot.strudel` (the original
       20-16 V-2 fixture) STAYS unchanged — it still validates the
       `GUARDED_BOOT_RE` recognition surface, which is a separate
       invariant.
    6. **Confirm `parity-refresh.mjs:68-75` exclusion guard.** The guard's grep is on `bakery-*` prefix; both new slugs start with `bakery-143-`, so they are excluded from upstream-drift checks. Run `node packages/app/scripts/parity-refresh.mjs --dry-run` (or the canonical invocation per the script's CLI by reading `parity-refresh.mjs:1-30` first); confirm the new slugs are NOT flagged as missing upstream. Record in OBSERVATIONS.
    7. **Snapshot-capture the new fixtures.** Run `pnpm --filter @stave/app test`. The new fixtures are auto-discovered by `parity.test.ts` and `loc-fidelity.test.ts`; their first run captures snapshots. NO `-u` flag. Expected:
       - parity-corpus count moves 49 → 51 (+2 new fixtures)
       - loc-fidelity count moves 49 → 51 (+2 new fixtures)
       - total app test count moves 413 → 417
       - EVERY pre-existing fixture's snapshot UNCHANGED (per-file loc-fidelity STOP gate per-wave)
       Both new fixtures' snapshots assert STRUCTURED (whole-program `body.tag !== 'Code'` OR `body.via !== undefined`). Record exact post-snapshot counts in OBSERVATIONS under "Wave B-A — V-2 fixtures captured".
    8. **Verify both fixtures' parity snapshots structure post-fix.** Run isolated: `pnpm --filter @stave/app test -- bakery-143-apostrophe` (or the canonical invocation; verify by reading the test runner config). Expected: both pass. The canonical positive's snapshot shows the `.cpm` `via`-wrap (Code with `via`); the negative-control's snapshot shows the same (or a Stack/Play depending on what the inherited recogniser arms produce). EITHER STRUCTURE — both pass the parity oracle's fence.
    9. **P68 build hygiene NOT NEEDED THIS WAVE** — no editor-src change. `git diff --name-only` shows only the new `.strudel` files, the BAKERY-FIXTURES.md update, and the new `__snapshots__/*.snap` entries.
    10. **Per-file loc-fidelity STOP gate (per-wave).** Same as Wave A action 9: every parity-UNCHANGED pre-existing corpus file's loc-fidelity snapshot byte-unchanged; the parity-CHANGED set = {2 new Wave-B-A fixtures} ⊆ V-3 allow-list. ANY other moved file = STOP. Record verbatim in OBSERVATIONS.
    11. **Commit via COMMIT_TEMPLATE**, gitmoji `:test_tube:`. `git add packages/app/tests/parity-corpus/bakery-143-apostrophe-in-chain-arg-comment.strudel packages/app/tests/parity-corpus/bakery-143-NEGATIVE-no-apostrophe-comment.strudel packages/app/tests/parity-corpus/BAKERY-FIXTURES.md packages/app/tests/parity-corpus/__snapshots__/*.snap`. Problem: "20-21's two-site walker `//`-skip has no permanent CI fixture; a future walker-tolerance regression (e.g. a refactor that drops the `//`-skip branch) would silently break the apostrophe-in-comment chain-arg class without per-class regression coverage; the P70 occurrence-9 framing — the actual blocker class — has no fixture even though `bakery-143-guarded-boot.strudel` exists for the original (wrong) class"; Fix: "vendor 2 permanent CI fixtures — `bakery-143-apostrophe-in-chain-arg-comment.strudel` (canonical positive: minimal distillation of the chain-arg + `// ma'am` shape; asserts whole-program STRUCTURED via the two-site `//`-skip) + `bakery-143-NEGATIVE-no-apostrophe-comment.strudel` (negative control: same shape with `// maam` no apostrophe; STRUCTURED both pre- and post-fix; proves the apostrophe is the gate); BAKERY-FIXTURES.md updated with P70 occurrence-9 provenance + reference-pattern note citing splitTopLevelStatements:414-417; the original bakery-143-guarded-boot.strudel STAYS for GUARDED_BOOT_RE anti-drift; per-file loc-fidelity STOP gate clean (no pre-existing fixture moved); structural exclusion from parity-refresh.mjs confirmed".
  </action>
  <ownership>
    This wave owns the per-class permanent CI fixtures (the anti-drift mechanism for the apostrophe-in-`//`-comment chain-arg walker class) + the negative-control + the BAKERY-FIXTURES.md provenance documentation. The auto-discovery + snapshot mechanism is owned by `parity.test.ts` / `loc-fidelity.test.ts` (unchanged). The structural exclusion from `parity-refresh.mjs` is owned by the existing `parity-refresh.mjs:68-75` guard (unchanged). The existing `bakery-143-guarded-boot.strudel` is owned by 20-16; UNTOUCHED.
  </ownership>
  <lifecycle>
    PK17 step 5 (vendor permanent regression coverage). Runs AFTER Wave A (the surgical edit must land; the W3 probe must confirm the FLIP; if either fails, no fixtures are vendored) and BEFORE Wave B-B (the backlog dispositions are independent of the fixture vendoring — they can happen in any order after Wave A — but B-A's snapshot capture must complete before V-1's fresh measurement runs against the suite that includes the new fixtures).
  </lifecycle>
  <pre_mortem>
    Most likely error (RESEARCH §R-7 PM-4): a new fixture's loc-fidelity snapshot accidentally shares state with a pre-existing fixture's. Mitigation: the loc-fidelity test file structure is `__snapshots__/<fixture>.strudel.snap` per-file; no cross-file state. Action 10's per-file STOP gate confirms each pre-existing snapshot is byte-unchanged.
    Second error: the negative-control fixture is malformed (e.g. line-ending or invisible-character difference makes it equivalent to the positive). Mitigation: action 4's content is byte-exact (LF newlines; ASCII only; final newline); record the file's byte count in OBSERVATIONS as a checksum-like anchor.
    Third error: the canonical positive's snapshot asserts `body.tag === 'Code'` (i.e. it's actually a Code-with-via — the `.cpm` chain). Mitigation: per RESEARCH §R-5, "Asserts `parseStrudel(src).body.tag !== 'Code'` OR `parseStrudel(src).body.via !== undefined`". Code-with-via IS STRUCTURED per the parity oracle's fence `isCodeFallback = body.tag === 'Code' && body.via === undefined`. Action 8 verifies this disposition explicitly.
    Fourth error: the `parity-refresh.mjs:68-75` exclusion guard does NOT cover `bakery-143-*` (e.g. the guard's slug-match is stricter than `bakery-*`). Mitigation: action 6's `--dry-run` recording. If the guard fails, file an issue + fix in a separate wave or in the existing 20-16 fixture's manner (was the original bakery-143-guarded-boot.strudel similarly excluded? confirm).
  </pre_mortem>
  <verify>
    1. 2 new fixture files present; canonical positive has the apostrophe inside `// ma'am`; negative-control has `// maam`; both have valid chain-arg structure with final newline.
    2. BAKERY-FIXTURES.md has a new "Phase 20-21 fixtures" section documenting both entries + the P70 occurrence-9 framing + the reference-pattern note (splitTopLevelStatements:414-417).
    3. The existing `bakery-143-guarded-boot.strudel` UNCHANGED.
    4. `pnpm --filter @stave/app test` GREEN — parity-corpus count 49 → 51; loc-fidelity 49 → 51; total app 413 → 417.
    5. Both new fixtures' snapshots assert STRUCTURED (either `body.tag !== 'Code'` directly or Code-with-`via`).
    6. EVERY pre-existing fixture's snapshot UNCHANGED; OBSERVATIONS records this.
    7. `pnpm --filter @stave/editor test` == 1627/1627 UNCHANGED.
    8. `node packages/app/scripts/parity-refresh.mjs --dry-run` (or canonical) — no missing-upstream for `bakery-143-apostrophe-in-chain-arg-comment` or `bakery-143-NEGATIVE-no-apostrophe-comment`.
    9. Commit via COMMIT_TEMPLATE.
  </verify>
  <done>
    2 permanent CI fixtures vendored; BAKERY-FIXTURES.md updated with P70 occurrence-9 provenance + reference-pattern note; existing 20-16 fixture untouched; auto-discovery picks up new files; parity-corpus 51 + loc-fidelity 51 GREEN; total app 417; per-file loc-fidelity STOP gate clean; structural exclusion confirmed; commit via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE B-B — Apply backlog dispositions via `gh issue` calls (CLASSIFY, not FIX)

<task id="B-B" type="auto" depends_on="A-1">
  <name>For each of `{#153, #156, #149, #147}`, apply the Wave-0-locked disposition via `gh issue comment` + `gh issue close` (where applicable); record the action in OBSERVATIONS; NO production-src changes</name>
  <read_first>
    20-21-OBSERVATIONS.md (the Wave-0 locked dispositions + drafted comment-body text for each issue);
    .planning/phases/20-musician-timeline/20-21-CONTEXT.md D-03 (the STRETCH discipline — CLASSIFY ≠ FIX; bonus-close only if SAME class as gate-bearing; otherwise close-as-superseded / close-as-duplicate / close-as-not-a-bug / REFINE / file-new);
    .planning/phases/20-musician-timeline/20-21-RESEARCH.md §R-4 (per-issue decision trees);
    .planning/phases/20-musician-timeline/20-20-SUMMARY.md (the LAST-WINS grounding for #153 — citations to inherit)
  </read_first>
  <files>
    .planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md (APPEND — the per-issue gh-call records + URLs of comments + close events)
  </files>
  <action>
    1. **Confirm Wave-0 dispositions are LOCKED in OBSERVATIONS.** Each of #153/#156/#149/#147 has:
       - the locked disposition (CLOSE-as-X / REFINE / BONUS-CLOSE / file-new)
       - the drafted comment-body text citing the Wave-0 verbatim observation + the supporting grounding (20-20 SUMMARY for #153; the §R-4 decision-tree branch chosen)
       - any cross-reference to gate-bearing class membership (if BONUS-CLOSE applies)
       Re-read these dispositions; do NOT re-decide; the Wave 0 RUN-classify outcome IS the decision. If any disposition seems wrong on re-read, that is itself a PK18 signal — re-pose to user before applying.
    2. **Apply `#153` disposition.** From RESEARCH §R-4 expected outcome: CLOSE-as-superseded if Wave 0 confirmed STRUCTURED (likely; 20-20 SUMMARY records LAST-WINS upstream verdict GROUNDED + bakery-159 NEGATIVE-control STRUCTURED → multi-top-level `sound("a")\nsound("b")` STRUCTURES on real main via the 20-20 substrate). Commands:
       ```
       gh issue comment 153 --body "$(cat <<'EOF'
       20-21 Wave 0 RUN-classify probe (real main fa09cfe):
       parseStrudel('sound("hh hh hh hh")\nsound("[bd bd][sd bd] bd sd")\n\n// @version 1.0')
       → body.tag = '<verbatim result>', via = '<verbatim>'

       Per 20-20 SUMMARY (LAST-WINS upstream verdict GROUNDED at:
       - @strudel/transpiler@1.2.6 transpiler.mjs:198-204 (addReturn rewrites only body[body.length-1])
       - @strudel/core@1.2.6 evaluate.mjs:37-38 (Function(body)() returns last)
       - @strudel/transpiler@1.2.6 transpiler.mjs:464-491 (labelToP only on labels)
       at Codeberg pin f73b3956), the multi-top-level case now structures on
       our parser via the 20-20 PV49-extension substrate. Our parser is
       FIRST-WINS on multi-top-level today; upstream is LAST-WINS. The
       semantic mismatch is NOT a parity blocker (the fence is body.tag
       !== 'Code'; FIRST-WINS still structures). A future phase that
       adopts LAST-WINS for full upstream parity inherits the §4 grounding
       without re-doing the Grounding Check.

       Closing as superseded by the 20-20 substrate work. Re-open if the
       LAST-WINS semantic adoption becomes a priority.
       EOF
       )"
       gh issue close 153 --comment "superseded by 20-20 substrate + bakery-159 NEGATIVE-control STRUCTURED on real main; LAST-WINS upstream verdict recorded for future inheritance"
       ```
       (If Wave 0 RUN-classify probe DIVERGED from §R-4 expectation — e.g. multi-top-level still bareCodes — apply REFINE instead of CLOSE; the comment-body text drafted in Wave 0 OBSERVATIONS reflects the actual outcome.)
       Capture the `gh issue comment` URL + the `gh issue close` confirmation in OBSERVATIONS under "Wave B-B — #153 disposition".
    3. **Apply `#156` disposition.** From RESEARCH §R-4 expected outcome: CLOSE-as-superseded (high prior — 20-17 had ~14% residual, current is 2%, so most 20-17 residuals closed by intervening phases) OR CLOSE-as-not-reproducible-in-current-sample (if the original "uncategorised" hash is not in the current sample at all). Wave 0 RUN-classify recorded the actual outcome. Apply the chosen disposition via `gh issue comment 156` + `gh issue close 156`. Comment body cites the Wave-0 per-row verdict from `samples-2026-05-19T13-24-45-538Z.json` re-run through the current parser. Capture URL + confirmation in OBSERVATIONS.
    4. **Apply `#149` disposition.** Wave 0 RUN-classify probed both `-1j62z5xjyCN` (bonus-closed in 20-19 per memory) and `-72eEl7NwK9e`. Per §R-4 decision tree:
       - If both hashes NOT in current sample → CLOSE-as-stale (rotated out of Bakery population);
       - If present + STRUCTURE → CLOSE-as-superseded;
       - If present + bareCode + SAME class as gate-bearing → BONUS-CLOSE in 20-21 (apply `gh issue close 149 --comment "bonus-closed by 20-21 P70 occurrence-9 fix; same class as gate-bearing #143-re-pose"`);
       - Else → REFINE with current observation.
       Apply per Wave-0 outcome. Capture URL + confirmation in OBSERVATIONS.
    5. **Apply `#147` disposition.** Per §R-4: DEFAULT is REFINE-with-product-note. Apply:
       ```
       gh issue comment 147 --body "$(cat <<'EOF'
       20-21 Wave 0 audit: still deferred per the 20-16 D-03 strip-only
       decision. samples() side-channel capture is a FEATURE waiting on an
       autocomplete/alias consumer to define the capture shape; no parser
       change pending; no parity-class blocker.

       Keeping open as a feature placeholder. If retiring the placeholder
       is desired, close as feature-out-of-scope-for-parity-work.
       EOF
       )"
       ```
       Do NOT close without user sign-off (per §R-4 — feature placeholders need product owner input). Capture URL in OBSERVATIONS.
    6. **Backlog audit summary entry.** Append to OBSERVATIONS under "Wave B-B — backlog audit summary":
       - Issues processed: 4
       - CLOSE-as-superseded: <count>
       - CLOSE-as-stale / CLOSE-as-not-reproducible: <count>
       - REFINE: <count>
       - BONUS-CLOSE (same class as gate-bearing): <count>
       - file-new (if any): <count> + new issue URLs
       Total dispositions = 4.
    7. **NO production-src changes this wave.** `git diff --name-only` shows only `.planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md`. The Wave-B-B commit captures the GH-call records as documentation; it does NOT touch the codebase.
    8. **Per-file loc-fidelity STOP gate.** Trivially satisfied (no fixture changes, no editor src changes). Confirm.
    9. **Commit via COMMIT_TEMPLATE**, gitmoji `:wastebasket:` (for closes) or `:books:` (for REFINE-only). Choose based on dispositions: if any CLOSE happened, `:wastebasket:`; if all REFINE, `:books:`. `git add -f .planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md`. Problem: "the open backlog has 4 issues filed across 20-16 / 20-17 framings; many predate the 20-18/20-19/20-20 substrate work and may be superseded; without RUN-classify the audit was inferential"; Fix: "RUN-classified #153/#156/#149/#147 via Wave-0 probes per RESEARCH §R-4 decision trees; applied per-issue dispositions via `gh issue comment` + `gh issue close` (per outcome — CLOSE-as-superseded for confirmed-structured-on-current-main; REFINE for still-relevant; CLOSE-as-stale for hashes rotated out of Bakery; BONUS-CLOSE if same class as gate-bearing #143-re-pose); #147 REFINE-with-product-note default (feature placeholder; no user sign-off to retire); audit summary recorded in OBSERVATIONS; NO production-src changes".
  </action>
  <ownership>
    This wave owns the BACKLOG DISPOSITIONS applied via GitHub. The Wave-0 outcome IS the decision; this wave executes. NO production code is owned/changed. The OBSERVATIONS record is the source of truth for which dispositions ran + the URLs of the GH events.
  </ownership>
  <lifecycle>
    PK17 step 2-and-a-half (classification activity; CLASSIFY ≠ FIX). Runs in parallel with B-A (both depend only on A-1; both feed V-1). Could collapse with V-4 but kept separate to keep V-4's PR-creation step uncluttered.
  </lifecycle>
  <pre_mortem>
    Most likely error: a `gh` call fails (auth lapsed; the issue was already closed by someone else; the comment body's heredoc gets mangled by zsh). Mitigation: each `gh` invocation is a separate command (no chained side effects); use `cat <<'EOF' … EOF` heredoc with single-quoted EOF (mirrors COMMIT_TEMPLATE discipline; prevents zsh substitution); if a call fails, capture the error verbatim in OBSERVATIONS, fix, retry. Do NOT silently move on.
    Second error: applying a CLOSE when the Wave-0 outcome actually required REFINE (mismatch between OBSERVATIONS record and chosen `gh` command). Mitigation: action 1 re-reads OBSERVATIONS; the disposition is keyed by issue number; never deviate from the locked disposition in the moment.
    Third error: BONUS-CLOSE applied to an issue that's actually a DIFFERENT class (false claim of incidental fix). Mitigation: BONUS-CLOSE requires the Wave-0 RUN-classify probe verdict matching the gate-bearing class — i.e. the issue's exemplar bareCodes pre-fix AND structures post-fix via the chain-arg walker `//`-skip; the Wave-A action 7 (i)/(ii) probe is the post-fix confirmation; only after Wave-A confirms can BONUS-CLOSE apply. Action 4 calls out this gate.
    Fourth error: closing #147 without user sign-off (the §R-4 explicit exception). Mitigation: action 5 has DEFAULT=REFINE; the close path is gated on user sign-off.
  </pre_mortem>
  <verify>
    1. 4 issues have a 20-21 Wave-0 disposition applied (comment or comment+close) on GitHub.
    2. The `gh issue comment` URLs are recorded in OBSERVATIONS for each issue.
    3. Any issue that was CLOSED shows `state: closed` on GitHub (`gh issue view <N>` confirms).
    4. The backlog audit summary entry in OBSERVATIONS lists all 4 issues + their dispositions.
    5. NO production-src changes (`git diff --name-only` shows only OBSERVATIONS).
    6. Per-file loc-fidelity STOP gate trivially satisfied.
    7. Commit via COMMIT_TEMPLATE.
  </verify>
  <done>
    4 backlog issues processed via RUN-classify + dispositions applied via `gh issue` calls (close-as-superseded / close-as-stale / close-as-not-reproducible / REFINE / bonus-close per Wave-0 outcome); audit summary recorded; NO production-src changes; commit via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE V — Dual gate (D-04) + cross-wave STOP gate + phase close (V-1 / V-3 / V-4 cadence)

### V-1 — Dual-gate measurement (crit-1 + crit-2 on the same merge-candidate commit)

<task id="V-1" type="auto" depends_on="B-A">
  <name>Fresh `pnpm parity:bakery --n 50` (PK17 step-6 cadence; new ISO stamp; same upstream pin); confirm crit-1 (Wave-B-A fixture asserts whole-program structured on `-7LU6zgzViSM` AND negative-control structured) + crit-2 (fresh = 50/50 = 100.0% AND ≥ 49/50 = 98.0% floor); record VERBATIM</name>
  <read_first>
    20-21-OBSERVATIONS.md (the 98.0% baseline + the FROZEN scope lock + the Wave-A W3 flip evidence + the Wave-B-A captured fixture counts);
    .planning/phases/20-musician-timeline/20-21-RESEARCH.md §R-6 (dual gate commands + crit-2 arithmetic + regression-classify procedure);
    .planning/phases/20-musician-timeline/20-20-SUMMARY.md "V-1" section (the 20-20 PK17 step-6 measurement template);
    packages/app/tests/parity-corpus/.bakery-runs/result-2026-05-21T12-51-24-407Z.json (the BASELINE 49/50 = 98.0% — the must-not-regress floor; the 1 residual row `-7LU6zgzViSM`);
    packages/app/tests/parity-corpus/_bakery-classify.spec.ts (line ~77 per 20-20 V-1 verification — the one-arg `parseStrudel(s.code)` invariant; confirm unchanged)
  </read_first>
  <files>
    .planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md (APPEND — V-1 measurement record);
    packages/app/tests/parity-corpus/.bakery-runs/samples-<NEW-ISO>.json (NEW — fresh stamp; gitignored);
    packages/app/tests/parity-corpus/.bakery-runs/result-<NEW-ISO>.json (NEW — fresh stamp; gitignored)
  </files>
  <action>
    1. **Confirm the one-arg oracle invariant** (the 20-18 Wave E hardened invariant). `grep -n 'parseStrudel(s.code)' packages/app/tests/parity-corpus/_bakery-classify.spec.ts` → the call site passes EXACTLY ONE arg (no opts). 20-21 ships no opts threading change. Record in OBSERVATIONS.
    2. **Confirm the parity:bakery script form.** Re-read `packages/app/package.json` and the workspace root `package.json` to determine the canonical invocation (`pnpm --filter @stave/app parity:bakery -- --n 50` or root-level `pnpm parity:bakery --n 50` — confirm per 20-20 V-1's captured form in OBSERVATIONS). Record canonical form in OBSERVATIONS.
    3. **Fresh PK17 step-6 measurement.** Run the canonical parity:bakery invocation with `--n 50`. Capture the NEW ISO stamp + the UPSTREAM_SHA + the structured count + the percentage. Expected: `samples-<NEW-ISO>.json` + `result-<NEW-ISO>.json` under `.bakery-runs/` (gitignored), SHA `f73b3956` unchanged, structured == 50 (the +1 from the fix), structured ≥ 50 = 100.0%, AND structured ≥ 49 (the must-not-regress 98.0% floor).
    4. **Classify the diff** vs `result-2026-05-21T12-51-24-407Z.json`. Specifically:
       - row `-7LU6zgzViSM` (#143-re-pose): baseline `code` → fresh expected `structured` (the +1 from the fix; THE gate-bearing flip);
       - every other row: baseline `structured` → fresh expected `structured` (no regression). ANY regression here is a PK18 STOP regardless of the % — re-run `pnpm parity:bakery --replay` if supported (else diff per-row classifications from the two `result-*.json` files via `node -e`); classify the regressing row's mechanism (most likely cause if it happens: RESEARCH §R-7 PM-4 — a loc-fidelity / OFFSET CONTRACT drift in `splitArgsWithOffsets` that the Wave-A action 7 (iv) probe didn't catch; OR an over-consuming `//`-skip that ate past a synthetic embedded `//` inside a string literal — but that's gated by `inString` first per the fix shape); route to backlog OR if traces back to the fix, re-pose D-02 fix-shape to user with EVIDENCE; NEVER amend the 98.0% floor downward.
    5. **Dual-gate check on the same head:**
       - **Crit-1 HARD:** the Wave-B-A `bakery-143-apostrophe-in-chain-arg-comment.strudel` fixture's parity snapshot asserts whole-program structured; re-confirm by `pnpm --filter @stave/app test -- bakery-143-apostrophe` isolated. Confirm both new fixtures (positive + negative-control) PASS. The original `bakery-143-guarded-boot.strudel` also still PASSES (defensive: confirms no regression to the 20-16 V-2 surface).
       - **Crit-2 HARD:** fresh structured = 50 (100.0%) AND ≥ 49 (must-not-regress 98.0% floor).
       - **Bar-lowering check:** no premise was bar-lowered; no scope-expansion mid-phase (Wave-0 backlog audit was CLASSIFY-only); no second-workaround; the floor is the 98.0% baseline unchanged.
       BOTH crit hold on the SAME head SHA. Record VERBATIM in OBSERVATIONS under "V-1 — D-04 dual-gate measurement", mirroring the 20-20 V-1 record block.
    6. **If crit-2 falsifies the floor (structured < 49):** PK18 HARD STOP (RESEARCH §R-7 PM-5). Capture the regressing row(s) verbatim from the result diff. Re-pose D-04 to user with EVIDENCE. Do NOT proceed to V-3/V-4. Do NOT amend the floor downward.
    7. **If crit-1 falsifies (the Wave-B-A canonical positive's parity snapshot does NOT assert structured):** PK18 HARD STOP. This means Wave A's surgical edit did not flip the canonical positive fixture in the shipped bundle (the Wave-A action 7 (i)/(ii) probe was source-level; the bundle behaviour diverges). Re-classify; re-pose D-02 to user.
    8. **If structured = 50 (the target) AND no regressions** → V-1 PASS. Capture the per-row diff verbatim:
       ```
       FLIPPED code->structured: [ '-7LU6zgzViSM' ]   ← the +1 from the fix
       REGRESSED structured->code: []                  ← zero regressions
       ```
       This is the GOAL — 100.0% on the current N=50 sample.
    9. **Commit via COMMIT_TEMPLATE**, gitmoji `:white_check_mark:`. `git add -f .planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md` (the .bakery-runs/*.json files are gitignored; record lives in OBSERVATIONS). Problem: "the 20-21 two-site walker `//`-skip needs a fresh PK17 step-6 measurement to confirm crit-2 = 100.0% + must-not-regress 98.0% (D-04 dual gate)"; Fix: "fresh `pnpm parity:bakery --n 50` recorded; SHA `f73b3956` unchanged; structured = <N>/50 = <X.X>% (= 100.0% target AND ≥ 98.0% floor); `-7LU6zgzViSM` flipped code → structured (the +1 from the fix); no regressions on the 49 baseline-structured rows; crit-1 (Wave-B-A positive + negative-control + original guarded-boot fixture all PASS) + crit-2 (100.0%) + bar-lowering check ALL clean".
  </action>
  <ownership>
    This wave owns the dual-gate verdict. Crit-1 was technically satisfied by Wave B-A (the fixture snapshot is structured); V-1 re-runs it on the merge-candidate HEAD to confirm it holds alongside the parity-corpus 51 + loc-fidelity 51 + editor 1627. Crit-2 is owned exclusively here — the fresh PK17 step-6 measurement is the gate. The must-not-regress floor enforcement is owned here.
  </ownership>
  <lifecycle>
    PK17 step 6 (measure on the merge candidate). Runs after Wave B-A (the fixtures are landed; the snapshot baseline is captured) and B-B (the backlog dispositions don't affect parity but should be applied before V-4's PR opens to avoid mid-PR backlog churn). Before V-3/V-4.
  </lifecycle>
  <pre_mortem>
    Most likely error (RESEARCH §R-7 PM-5): crit-1 PASSES but crit-2 < 98.0% — meaning the fix caused a regression on some OTHER row that 20-18/20-19/20-20 had structured. Mitigation: action 4 diffs per-row; ANY non-`-7LU6zgzViSM` row flipping structured → code is the regression; classify; never bar-lower.
    Second error: the new ISO stamp's UPSTREAM_SHA differs from `f73b3956` (upstream pin drift). Mitigation: action 3 captures the SHA; if differs, STOP (a baseline shift invalidates the comparison; re-pose to user).
    Third error (RESEARCH §R-7 PM-1): structured = 49 on the fresh measurement (i.e. `-7LU6zgzViSM` STILL bareCodes despite the fix) — meaning the Wave-A source-level probe (action 7 (ii)) didn't match the shipped bundle's runtime behaviour (e.g. minification stripped the `//`-skip branch). Mitigation: re-grep the bundle (`grep -A 5 'inString' packages/editor/dist/index.js | grep -i 'comment'`); if the comment-string anchors are missing, the minifier elided; re-pose D-02 to the user with EVIDENCE; consider switching to a `skipWhitespaceAndLineComments` call instead of inline (which would survive minification as the primitive is already in the bundle).
    Fourth error: the one-arg oracle invariant is broken. Mitigation: action 1's grep is non-optional.
  </pre_mortem>
  <verify>
    1. The one-arg oracle invariant in `_bakery-classify.spec.ts` `parseStrudel(s.code)` has EXACTLY one arg.
    2. Fresh `samples-<NEW-ISO>.json` + `result-<NEW-ISO>.json` under `.bakery-runs/`; SHA `f73b3956` unchanged; structured = 50/50 = 100.0% (AND ≥ 49 = ≥ 98.0% floor at minimum).
    3. Per-row diff: `-7LU6zgzViSM` flipped code → structured (THE gate-bearing flip); the 49 baseline-structured rows ALL still structured (zero regressions).
    4. Both Wave-B-A fixtures' parity snapshots assert structured; isolated runs PASS; the original `bakery-143-guarded-boot.strudel` also PASSES (defensive).
    5. Crit-1 + crit-2 + bar-lowering check ALL hold on the SAME HEAD SHA; recorded VERBATIM in OBSERVATIONS.
    6. Commit via COMMIT_TEMPLATE.
  </verify>
  <done>
    Fresh PK17 step-6 measurement recorded; SHA `f73b3956` unchanged; structured = 50/50 = 100.0% (≥ 98.0% must-not-regress floor); per-row diff verifies `-7LU6zgzViSM` flipped (the SOLE flip); no regressions; crit-1 + crit-2 + bar-lowering check pass; commit via COMMIT_TEMPLATE.
  </done>
</task>

### V-3 — Cross-wave per-file loc-fidelity STOP gate (full-corpus pass on the merge-candidate)

<task id="V-3" type="auto" depends_on="V-1">
  <name>Run the FULL `pnpm --filter @stave/app test` against the merge-candidate HEAD; confirm parity-corpus 51 + loc-fidelity 51 hold; confirm the parity-CHANGED set ⊆ the enumerated V-3 allow-list = {2 Wave-B-A fixtures + their snapshot entries}; any other moved file = STOP</name>
  <read_first>
    20-21-OBSERVATIONS.md (the Wave-0 expected V-3 allow-list + the per-wave loc-fidelity STOP gate records);
    .planning/phases/20-musician-timeline/20-20-SUMMARY.md "V-3" section (the 20-20 V-3 STOP gate precedent — 154/0 pure-addition diff)
  </read_first>
  <files>
    .planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md (APPEND — V-3 full-corpus record)
  </files>
  <action>
    1. **Run the full suite cleanly.** `pnpm --filter @stave/app test` against the V-1 head SHA. Expected: parity-corpus (49 + 2) = 51 GREEN + loc-fidelity (49 + 2) = 51 GREEN + the rest of the app tests = 315-base + 4 new = 413 + 4 = 417 GREEN total. NO `-u` flag — every snapshot is committed.
    2. **Enumerate the V-3 allow-list — the EXACT files that may have moved across the entire phase:**
       - 2 new Wave-B-A fixtures (`bakery-143-apostrophe-in-chain-arg-comment.strudel` + `bakery-143-NEGATIVE-no-apostrophe-comment.strudel`);
       - their auto-captured snapshot entries in `parity.test.ts.snap` + `loc-fidelity.test.ts.snap`;
       - any other parity-CHANGED file from the fresh PK17 step-6 (V-1 action 4 enumerates these — expected: NONE additional).
       Expected allow-list size: EXACTLY 2 fixtures + 2 snapshot-file additions = 4 file-level changes.
    3. **STOP gate.** Every parity-UNCHANGED corpus file's loc-fidelity diff MUST be EMPTY. The parity-CHANGED set ⊆ the enumerated allow-list. ANY other moved file = silent drift = STOP.
       - Run `git diff --name-only HEAD~5..HEAD packages/app/tests/parity-corpus/__snapshots__/` (5-commit window covers Wave 0 + Wave A + Wave B-A + Wave B-B + V-1; the only snapshot writes should be the 2 new fixtures' entries; Wave B-B added NO snapshot changes).
       - Diff against the allow-list. Each moved entry MUST justify by the allow-list.
       - Record verbatim in OBSERVATIONS under "V-3 — cross-wave full-corpus per-file loc-fidelity STOP gate".
    4. **If any file outside the allow-list moved:** PK18 STOP (RESEARCH §R-7 PM-4). The PV49-spirit byte-additive comment-consumption substrate proves no parity-UNCHANGED file's loc-spans should shift; an observed violation is silent drift. Classify the moved file's content shift; if traceable to a `splitArgsWithOffsets` false-positive, the fix is broken — re-pose D-02 to user. Do NOT extend the allow-list defensively (the 20-18/20-19/20-20 V-3 lesson).
    5. **Snapshot diff sanity check** — confirm the diff is pure-addition (no removals from pre-existing snapshots). Run `git diff --stat packages/app/tests/parity-corpus/__snapshots__/` and capture the insertions/deletions counts. Expected pattern (mirrors 20-20's 154/0 pure-addition): N insertions / 0 deletions across both snapshot files.
    6. **Commit via COMMIT_TEMPLATE**, gitmoji `:white_check_mark:`. `git add -f .planning/phases/20-musician-timeline/20-21-OBSERVATIONS.md`. Problem: "the cross-wave full-corpus per-file loc-fidelity STOP gate is the silent-drift guard — the PV49-spirit byte-additive comment-consumption substrate proves no movement by construction but the gate observes"; Fix: "ran `pnpm --filter @stave/app test` full-corpus pass against the V-1 head; (49+2) parity + (49+2) loc-fidelity GREEN; parity-CHANGED set = {2 new Wave-B-A fixtures} — exactly the enumerated allow-list; NO other moved file; snapshot diff is N/0 pure-addition; no silent drift".
  </action>
  <ownership>
    This wave owns the cross-wave PV49-spirit silent-drift guard's verdict. PV49 itself is owned by the existing primitive at pS:1075 + the inline `//`-skip references at pS:414-417 (the reference) + the Wave-A surgical edit at pS:2598-2628 + pS:2664-2747 (the new applications).
  </ownership>
  <lifecycle>
    PK17 step 6.5 (cross-wave validation on the merge candidate). Runs AFTER V-1 (the merge-candidate HEAD must be the V-1 SHA) and BEFORE V-4 (no SUMMARY / catalogue updates / PR draft until the silent-drift guard is CLEAN).
  </lifecycle>
  <pre_mortem>
    Most likely error: a moved file outside the allow-list (RESEARCH §R-7 PM-4). Mitigation: action 3's diff against the allow-list; action 4's NEVER-defensively-extend rule.
    Second error: a snapshot was over-written silently (someone ran `pnpm test -u` mid-phase). Mitigation: NO `-u` flag in action 1; the snapshot diff against `HEAD~5` enumerates every change across commits.
  </pre_mortem>
  <verify>
    1. `pnpm --filter @stave/app test` GREEN end-to-end on the V-1 head; counts (49+2) parity + (49+2) loc-fidelity + 315-base + 4 new = 417 total.
    2. The parity-CHANGED set across the entire phase ⊆ the enumerated allow-list = {2 Wave-B-A fixtures}; ZERO other moved files.
    3. Snapshot diff is N insertions / 0 deletions pure-addition.
    4. Record verbatim in OBSERVATIONS.
    5. Commit via COMMIT_TEMPLATE.
  </verify>
  <done>
    Cross-wave full-corpus per-file loc-fidelity STOP gate CLEAN; parity-CHANGED set ⊆ enumerated allow-list = {bakery-143-apostrophe-in-chain-arg-comment.strudel + bakery-143-NEGATIVE-no-apostrophe-comment.strudel}; snapshot diff pure-addition; ZERO silent drift; commit via COMMIT_TEMPLATE.
  </done>
</task>

### V-4 — Phase close: SUMMARY + catalogue updates + new issue (P70 occurrence-9 framing) + single non-stacked PR

<task id="V-4" type="auto" depends_on="V-3">
  <name>Write 20-21-SUMMARY.md; update .anvi catalogues (PK16/PK17/PK18 + P70 occurrence-9 row + PV49 new occurrence addendum; PV54 explicit NOT-triggered); file the NEW gate-bearing issue framing P70 occurrence-9 (since #143 was closed prematurely in 20-16); manually close #143 as superseded; open the single non-stacked PR with `closes #<NEW-ISSUE-N>`; record post-merge artifact verification recipe; Claude NEVER merges</name>
  <read_first>
    20-21-OBSERVATIONS.md (every wave's verbatim record);
    .planning/phases/20-musician-timeline/20-20-SUMMARY.md (the SUMMARY template + the catalogue-update section pattern);
    .anvi/krama.md PK16 (parser pipeline — extend with addendum for two new walker-`//`-skip occurrences at stage 2) + PK17 (friction-first cycle — add 20-21 measured 100.0% row + N PK18 re-poses) + PK18 (HARD-GATE cascade discipline — count any 20-21 PK18 re-poses);
    .anvi/hetvabhasa.md P70 (the eight-occurrence pattern — 20-21 is occurrence-9: the 20-16 cascade classified #143 as guarded-boot recognition gap; actual blocker for `-7LU6zgzViSM` is apostrophe-in-`//`-comment chain-arg walker; the lesson: when an exemplar STILL bareCodes after a recognition extension ships, RUN the bisect on the exemplar BEFORE accepting the close — don't trust shape-matching alone);
    .anvi/vyapti.md PV49 (the loc-additivity substrate — the 20-21 two-site walker extension is a new occurrence; mirror the 20-20 ORIGIN/WHY/HOW template) + PV54 (FLOOR-grep obligation — confirm explicit NOT-triggered note);
    ~/.anvideck/projects/struCode/ref/GROUND_TRUTH_SIGNAL_MJS.md (the 20-20 Ground Truth doc — extend with a note on chain-arg walker discipline IF the executor judges value; OPTIONAL);
    ~/.claude/projects/-Users-mrityunjaybhardwaj-Documents-projects-struCode/memory/feedback_commit_msg_heredoc.md (COMMIT_TEMPLATE heredoc enforcement);
    ~/.claude/projects/-Users-mrityunjaybhardwaj-Documents-projects-struCode/memory/feedback_stacked_pr_base_retarget.md (post-merge artifact verification recipe)
  </read_first>
  <files>
    .planning/phases/20-musician-timeline/20-21-SUMMARY.md (NEW — the phase close-out);
    .anvi/krama.md (UPDATE — PK16/PK17/PK18 addenda for 20-21);
    .anvi/hetvabhasa.md (UPDATE — P70 occurrence-9 row with full provenance);
    .anvi/vyapti.md (UPDATE — PV49 new occurrence row for the two-site walker `//`-skip with ORIGIN/WHY/HOW; PV54 explicit NOT-triggered note);
    ~/.anvideck/projects/struCode/ref/GROUND_TRUTH_SIGNAL_MJS.md (OPTIONAL UPDATE — only if the executor judges a note adds inheritable value for future phases)
  </files>
  <action>
    1. **File a NEW gate-bearing issue** framing the actual class (since #143 was prematurely closed in 20-16 — the close stood, but the close was framed around the wrong class). The new issue's title + body:
       ```
       gh issue create --title "parseStrudel: chain-arg walkers don't skip `//` line comments before string-quote test (P70 occurrence-9 — supersedes premature close of #143)" --body "$(cat <<'EOF'
       ## Class

       `findMatchingParen` (parseStrudel.ts:2598-2628) and
       `splitArgsWithOffsets` (parseStrudel.ts:2664-2747) walk char-by-char,
       testing `if (ch === '"' || ch === "'" || ch === '`')` BEFORE checking
       for `//`. An odd-count apostrophe inside a `// line comment` inside
       chain-method arg parsing puts the walker into unterminated-string
       state → findMatchingParen returns -1 OR splitArgsWithOffsets
       mis-attributes commas → chain-arg parse falls back to bareCode.

       ## Exemplar

       Bakery row `-7LU6zgzViSM` ("Doubly-Linked Liszt") contains
       `// This is a green piece ma'am, we offset everything here o_0`
       inside its second `.layer()` arrow body. Exactly one apostrophe
       (odd count) → walker enters unterminated-string state.

       ## Reference correct pattern

       `splitTopLevelStatements:414-417` DOES skip `//`-to-EOL BEFORE the
       string-quote test:

       ```ts
       if (ch === '/' && body[i + 1] === '/') {
         while (i < body.length && body[i] !== '\n') i++
         continue
       }
       ```

       The 20-21 fix copies this exact branch into both walker sites.

       ## P70 occurrence-9 context

       The original `#143` was filed in 20-15 V-1 against `-7LU6zgzViSM`
       and was framed as a "guarded-boot recognition gap." 20-16 V-2
       shipped `GUARDED_BOOT_RE` at `parseStrudel.ts:228-229` (which does
       correctly recognise the guarded-boot prelude pattern) and CLOSED
       `#143`. Yet `-7LU6zgzViSM` STILL bareCodes on real main `fa09cfe`.

       20-21 RESEARCH §R-1 ran a 9-wave bisect (V0..V14 + W1/W3 + A8
       variants) that ISOLATED the apostrophe-in-`//`-comment class —
       cleanly distinct from the guarded-boot prelude class. The 20-16
       cascade classification was wrong about WHY `-7LU6zgzViSM` bails;
       the actual blocker is in the chain-arg walkers, which `#143`
       never named.

       The original `#143` close stands (the `GUARDED_BOOT_RE` it shipped
       remains correct for the prelude-recognition surface). This NEW
       issue tracks the actual blocker class.

       ## Fix

       This issue closes via the 20-21 PR. The fix is two `//`-skip
       branches mirroring `splitTopLevelStatements:414-417`. Permanent CI
       fixtures `bakery-143-apostrophe-in-chain-arg-comment.strudel` +
       `bakery-143-NEGATIVE-no-apostrophe-comment.strudel` vendored for
       per-class regression coverage.
       EOF
       )"
       ```
       Capture the new issue number in OBSERVATIONS as `<NEW-ISSUE-N>`. This is the PR's `closes` target.
    2. **Manually re-close `#143` as superseded** (it's already closed by 20-16, but add a 20-21 comment marking the supersession explicitly):
       ```
       gh issue comment 143 --body "$(cat <<'EOF'
       20-21 RESEARCH §R-1 9-wave bisect (P70 occurrence-9) determined that
       the original `#143` framing (guarded-boot recognition gap) was the
       wrong class for `-7LU6zgzViSM`. The actual blocker is in
       `findMatchingParen` + `splitArgsWithOffsets` — chain-arg walkers
       that don't skip `//` line comments before the string-quote test.
       The `GUARDED_BOOT_RE` 20-16 shipped DOES correctly recognise the
       guarded-boot prelude (a real and load-bearing extension that other
       exemplars benefit from), so this issue stays CLOSED on that
       framing.

       Supersession: a NEW issue (#<NEW-ISSUE-N>) tracks the actual
       chain-arg walker `//`-skip class. The 20-21 PR closes that issue.

       Lesson catalogued as P70 occurrence-9: when an exemplar STILL
       bareCodes after a recognition extension ships, RUN the bisect on
       the exemplar BEFORE accepting the close — don't trust
       shape-matching alone.
       EOF
       )"
       ```
       Do NOT re-open + re-close (#143 has been closed since 20-16; the comment is sufficient annotation). Capture in OBSERVATIONS.
    3. **Write 20-21-SUMMARY.md** mirroring the 20-20-SUMMARY.md shape: status / verdict / branch / V-1 commit / baseline pre + post / bakery fresh stamp + percentage / closes-list / backlog dispositions applied / catalogue updates / fixtures-added / per-wave deltas / the FROZEN Outcome-1 scope shipped / the N PK18 re-poses (target: 0 — replicate 20-19+20-20) / V-3 allow-list / deferred backlog / operational-discipline-recap / Cognitive Discoveries section. Include the NEW-ISSUE-N reference + the 4 backlog dispositions applied in Wave B-B.
    4. **Update `.anvi/krama.md`** with PK16 + PK17 + PK18 addenda for 20-21:
       - PK16 addendum: two new `//`-skip occurrences at stage 2 (chain-arg walkers `findMatchingParen` + `splitArgsWithOffsets`); PK16 stage numbering unchanged.
       - PK17 addendum: 20-21 measured 100.0% (+2pp from 98.0% baseline); N PK18 re-poses (target: 0 — if achieved, the THIRD consecutive clean-run phase after 20-19 + 20-20).
       - PK18 addendum: any 20-21 PK18 re-poses (target: 0). If 0, document as the THIRD consecutive clean-run phase (the substrate-works hypothesis strengthens further).
    5. **Update `.anvi/hetvabhasa.md`** with P70 occurrence-9 — the FULL entry:
       - Symptom: bakery row `-7LU6zgzViSM` STILL bareCodes on real main after 20-16's `GUARDED_BOOT_RE` ship CLOSED `#143`.
       - The trap (wrong fix): assuming the 20-16 close meant the recognition gap was the actual blocker; the cascade classification didn't RUN the bisect on the exemplar post-fix.
       - The actual class: chain-arg walkers (`findMatchingParen` + `splitArgsWithOffsets`) don't skip `//` line comments before testing for string delimiters; odd-count apostrophes inside `// comments` inside chain-method args put the walkers into unterminated-string state.
       - The detection signal: the exemplar STILL bareCodes after a shape-matched recognition extension ships. Diagnostic: re-run the bisect on the post-ship exemplar.
       - The real fix: copy `splitTopLevelStatements:414-417`'s `//`-skip branch into both walker sites BEFORE the string-quote test.
       - REF: 20-21-RESEARCH.md §R-1 + §R-2 + 20-21-SUMMARY.md.
    6. **Update `.anvi/vyapti.md`**:
       - Add new PV49 occurrence row for 20-21:
         > **PV49 occurrence (20-21):** chain-arg walker `//`-skip applied at two new call sites (`findMatchingParen` + `splitArgsWithOffsets`).
         > - **ORIGIN:** 20-21 V-1 N=50 observation of bakery `-7LU6zgzViSM` STILL bareCode'ing on real main after 20-16's `GUARDED_BOOT_RE` close of `#143`; the 20-21 RESEARCH §R-1 9-wave bisect ISOLATED the apostrophe-in-`//`-comment class inside chain-arg walkers (P70 occurrence-9).
         > - **WHY:** without this skip, every chain-arg with a `// comment` containing odd-count apostrophes bareCodes — silently mis-classifying the chain. The class is open-ended (any user-authored comment with an apostrophe).
         > - **HOW:** both walkers add an inline `//`-skip branch IMMEDIATELY BEFORE the string-quote test, mirroring `splitTopLevelStatements:414-417` exactly. For `splitArgsWithOffsets`, comment chars are appended to `current` to preserve the OFFSET CONTRACT at pS:2685-2693 (the existing `skipWhitespaceAndLineComments` primitive at `pushCurrent` consumes leading `// comment\n`s — so the contract holds as long as `current` stays byte-identical to the source slice). Two-site change; no new IR shape; no new state. The substrate's domain grows: PV49 now serves 6 callers across 6 boundary classes (split-top-level-statements, extractTracks-label-scan, applyChain-inter-method, splitArgsWithOffsets pushCurrent leading-skip, splitRootAndChain identifier-to-paren whitespace, and the NEW chain-arg walker `//`-skips at findMatchingParen + splitArgsWithOffsets-walker body).
       - Add explicit PV54 note: "PV54 FLOOR-grep obligation NOT triggered this phase — 20-21 ships no new top-level PatternIR tag. The Signal/Builder tags from 20-18 + 20-20's identifier-to-paren tolerance are unchanged. The PV53/PV54 consumer-audit obligation is dormant for 20-21."
    7. **OPTIONAL Ground Truth doc update.** If the executor judges value, EXTEND `~/.anvideck/projects/struCode/ref/GROUND_TRUTH_SIGNAL_MJS.md` with a brief note on chain-arg walker discipline (acorn vs our hand-rolled walkers; the upstream's `//`-handling is via acorn's tokenizer which strips comments before AST construction — i.e. our hand-rolled walkers must explicitly mirror what acorn does implicitly). This is NOT mandatory (the 20-20 Ground Truth extension already covers the broader parser/evaluator pipeline). DEFAULT: SKIP unless the executor's V-3 OBSERVATIONS show a future-phase needs this section.
    8. **Self-review pass on the diff.** Per AnviDev step 5: audit every changed file for dead code, redundant logic, missing edge cases, parameter leaks, type safety holes. Specifically:
       - Confirm both walker `//`-skip branches mirror pS:414-417 exactly (same `if (ch === '/' && next === '/')`, same inner `while`, same `continue`).
       - Confirm the Site 2 `splitArgsWithOffsets` branch appends `current += argsStr[i]` (preserving OFFSET CONTRACT) — the Site 1 `findMatchingParen` does NOT need this (no `current` buffer; only `i` advances).
       - Confirm the comment block at each surgery site cites the P70 occurrence-9 framing + the reference pattern at pS:414-417 + a brief PV49-spirit note.
       - Confirm both fixtures' content is byte-exact (LF newlines, ASCII only) + final newline.
       - Confirm BAKERY-FIXTURES.md's new section is well-formed Markdown + cites the new issue number (added in action 1).
       Record self-review pass in OBSERVATIONS.
    9. **Open the PR (single non-stacked → main).**
       - `git push -u origin feat/20-21-comment-aware-walkers`.
       - `gh pr create --base main --head feat/20-21-comment-aware-walkers --title "feat(20-21): chain-arg walker `//`-skip — close P70 occurrence-9 (supersedes #143)"`. Body via HEREDOC:
         ```
         ## Summary
         - Two char-by-char walkers in `parseStrudel.ts` — `findMatchingParen` (pS:2598-2628) + `splitArgsWithOffsets` (pS:2664-2747) — did NOT skip `//` line comments before the string-quote test. An odd-count apostrophe inside a `// comment` inside chain-method arg parsing (e.g. `// ma'am`) put the walker into unterminated-string state → chain-arg parse bareCoded.
         - The fix copies the reference `//`-skip branch from `splitTopLevelStatements:414-417` into both walker sites IMMEDIATELY BEFORE the string-quote test and AFTER the `if (inString)` branch. For `splitArgsWithOffsets`, comment chars are APPENDED to `current` to preserve the OFFSET CONTRACT at pS:2685-2693.
         - This is P70 occurrence-9 — the original `#143` was prematurely closed in 20-16 around the wrong class (guarded-boot recognition gap; `GUARDED_BOOT_RE` ship is still correct for that surface but never closed `-7LU6zgzViSM`). The 20-21 RESEARCH 9-wave bisect ISOLATED the apostrophe-in-`//`-comment class.
         - `-7LU6zgzViSM` flips whole-program STRUCTURED via the inherited chain-arg recogniser arms.
         - 2 permanent CI fixtures (1 canonical positive + 1 negative-control) vendored as per-class regression coverage.
         - 4 backlog issues processed via RUN-classify + dispositions applied (#153 / #156 / #149 / #147 — per Wave-0 outcomes).

         ## Gate
         - **Crit-1 HARD:** `bakery-143-apostrophe-in-chain-arg-comment.strudel` parity snapshot asserts whole-program structured; negative-control also structured (proves the apostrophe is the gate, not the chain shape). Original `bakery-143-guarded-boot.strudel` still PASSES (defensive).
         - **Crit-2 HARD:** fresh `pnpm parity:bakery --n 50` = 50/50 = 100.0% (≥ 98.0% must-not-regress floor).
         - **No bar-lowering. No second-workaround. No scope-expansion.**

         ## Test plan
         - [ ] `pnpm --filter @stave/editor test` 1627/1627
         - [ ] `pnpm --filter @stave/app test` 417/417 (parity-corpus 51 + loc-fidelity 51 + 315 base)
         - [ ] `pnpm parity:bakery --n 50` structured = 50/50 (recorded in OBSERVATIONS)
         - [ ] Per-file loc-fidelity STOP gate clean (V-3 allow-list = {2 Wave-B-A fixtures})
         - [ ] Post-merge: `git merge-base --is-ancestor <PR-head> origin/main` exit 0; `dist/index.js` grep `findMatchingParen` + `splitArgsWithOffsets` + `splitTopLevelStatements` all > 0; `skipWhitespaceAndLineComments` count unchanged (the fix mirrors the reference inline, not a new primitive call)

         closes #<NEW-ISSUE-N>
         ```
       GitHub honors ONE auto-close keyword; `closes #<NEW-ISSUE-N>` only. The 4 backlog issues (#153/#156/#149/#147) were closed/refined via direct `gh issue` calls in Wave B-B (not via PR keyword). `#143` is already closed and stays closed; manually-annotated as superseded in action 2.
    10. **Record the post-merge artifact verification recipe** (for user to execute post-merge — Claude NEVER merges). In SUMMARY.md under "Operational discipline":
       ```bash
       git fetch origin
       git merge-base --is-ancestor <PR-HEAD-SHA> origin/main && echo "ancestor: OK"
       pnpm install
       pnpm --filter @stave/editor build
       grep -c "findMatchingParen"        packages/editor/dist/index.js     # > 0
       grep -c "splitArgsWithOffsets"     packages/editor/dist/index.js     # > 0
       grep -c "splitTopLevelStatements"  packages/editor/dist/index.js     # > 0 (defensive — reference pattern untouched)
       grep -c "skipWhitespaceAndLineComments" packages/editor/dist/index.js # ≥ 7 (unchanged from 20-20; the 20-21 fix mirrors the inline shape, not a new caller)
       pnpm --filter @stave/editor test                                       # 1627/1627
       pnpm --filter @stave/app test                                          # 417/417
       pnpm parity:bakery --n 50                                              # = 50/50 (100.0%), pin SHA f73b395648645aabe699f91ba0989f35a6fd8a3c
       ```
       If any grep returns 0, any test count regresses, or parity:bakery comes in below 98.0% on real main — that is a P70-class hetvabhasa entry. File hotfix issue immediately; do NOT bar-lower; do NOT silent-rollback.
    11. **Commit via COMMIT_TEMPLATE**, gitmoji `:memo:`. `git add -f .planning/phases/20-musician-timeline/20-21-SUMMARY.md .anvi/krama.md .anvi/hetvabhasa.md .anvi/vyapti.md`. Problem: "phase 20-21 needs a SUMMARY + catalogue provenance + new gate-bearing issue (P70 occurrence-9 framing) + PR opened for user-merge review (Claude never merges); #143 needs a supersession annotation comment"; Fix: "filed NEW gate-bearing issue framing P70 occurrence-9; manually annotated #143 as superseded; wrote 20-21-SUMMARY.md mirroring 20-20 close-out structure; updated .anvi/{krama,hetvabhasa,vyapti}.md with 20-21 addenda (PK16 two-site walker `//`-skip / PK17 100.0% +2pp / PK18 N re-poses / P70 occurrence-9 full row / PV49 two-site walker occurrence with ORIGIN/WHY/HOW / PV54 NOT-triggered); opened single non-stacked PR `feat/20-21-comment-aware-walkers` → main with `closes #<NEW-ISSUE-N>`; backlog dispositions for #153/#156/#149/#147 applied in Wave B-B via direct `gh issue` calls; recorded post-merge artifact verification recipe; Claude stops at PR-up".
    12. **STOP at PR-up.** Do NOT merge. Checkpoint to the user: "PR is up at #<N>. Review and merge when ready. Post-merge: execute the artifact verification recipe in SUMMARY.md."
  </action>
  <ownership>
    This wave owns the phase close-out: NEW issue filed (gate-bearing class), #143 supersession comment, SUMMARY, catalogue addenda, OPTIONAL Ground Truth note, PR draft, post-merge recipe. Claude NEVER merges. The new catalogue entries' provenance (ORIGIN/WHY/HOW) is owned here.
  </ownership>
  <lifecycle>
    PK17 step 7 (phase close + lessons compound forward) + PK18 step 9 (catalogue the discipline used). Runs after V-3 (silent-drift guard CLEAN) and B-B (backlog dispositions applied). Strictly the last wave.
  </lifecycle>
  <pre_mortem>
    Most likely error: a Co-Authored-By trailer accidentally added (AnviDev rule — "No Co-Authored-By"). Mitigation: COMMIT_TEMPLATE single-quoted heredoc never includes a Co-Authored-By line; pre-PR `git log --format='%(trailers:key=Co-Authored-By)' main..feat/20-21-comment-aware-walkers` is empty.
    Second error: Claude merges the PR (violation of AnviDev step 6). Mitigation: action 12 STOPS at PR-up.
    Third error: the PR's `closes` references `#143` (already closed) instead of `<NEW-ISSUE-N>` — GitHub rejects re-closing or the auto-close does nothing. Mitigation: action 9's `closes #<NEW-ISSUE-N>` is the documented target; #143 is handled via the manual supersession comment in action 2.
    Fourth error: post-merge, the artifact verification surfaces a regression (the merge race / squash mangled commits). Mitigation: action 10's recipe is verbose; if any grep returns 0 or any test count regresses on real main, that is a P70-class hetvabhasa entry — file a hotfix issue immediately.
    Fifth error: the NEW issue's body cites wrong line numbers (pS values drifted between Wave 0 and Wave V-4). Mitigation: action 1's body cites the pS values from the live Wave-0 OBSERVATIONS table (not from RESEARCH); if Wave 0 found drift, the body reflects it.
  </pre_mortem>
  <verify>
    1. NEW gate-bearing issue filed with P70 occurrence-9 framing; URL captured in OBSERVATIONS.
    2. `#143` annotated as superseded via a 20-21 comment citing the P70 occurrence-9 finding.
    3. `20-21-SUMMARY.md` exists with status / verdict / per-wave deltas / FROZEN Outcome-1 scope / V-3 allow-list / catalogue updates / fixtures-added / Ground-Truth-doc disposition (skip-or-extend) / backlog dispositions applied / Cognitive Discoveries / post-merge recipe.
    4. `.anvi/{krama,hetvabhasa,vyapti}.md` have 20-21 addenda with ORIGIN/WHY/HOW; P70 occurrence-9 full row added (with REF to RESEARCH + SUMMARY); PV54 explicit NOT-triggered note.
    5. `git log --format='%(trailers:key=Co-Authored-By)' main..feat/20-21-comment-aware-walkers` is empty.
    6. PR opened on GitHub; body has `closes #<NEW-ISSUE-N>` exactly once (NOT `#143`); counts + gate satisfied in description.
    7. Self-review pass recorded in OBSERVATIONS (the AnviDev step-5 critical analysis).
    8. Claude DID NOT merge; final message is the checkpoint to the user.
  </verify>
  <done>
    NEW gate-bearing issue filed (P70 occurrence-9 framing); #143 annotated as superseded; 20-21-SUMMARY.md written; `.anvi/{krama,hetvabhasa,vyapti}.md` updated with 20-21 addenda + provenance + P70 occurrence-9 row; OPTIONAL Ground Truth update applied or skipped per executor judgement; single non-stacked PR `feat/20-21-comment-aware-walkers` → main opened with `closes #<NEW-ISSUE-N>`; post-merge artifact verification recipe recorded; Claude stops at PR-up; commit via COMMIT_TEMPLATE.
  </done>
</task>

---

## WAVE DEPENDENCY GRAPH (acyclic)

```
0-1 (Wave 0 — RE-CONFIRM §R-1 bisect; RUN-classify backlog; FROZEN scope + dispositions)
  └─→ A-1 (Wave A — two-site walker `//`-skip; W3 production exemplar flips)
         ├─→ B-A (Wave B-A — 2 permanent CI fixtures vendored)
         │       └─→ V-1 (Wave V-1 — dual gate measurement)
         │                └─→ V-3 (Wave V-3 — cross-wave full-corpus STOP gate)
         │                          └─→ V-4 (Wave V-4 — NEW issue + #143 supersession + SUMMARY + catalogues + PR)
         └─→ B-B (Wave B-B — apply backlog dispositions via `gh issue` calls; parallel with B-A)
                  └─→ (feeds V-4 informationally — V-4's SUMMARY documents which dispositions applied)
```

B-A and B-B are independent post-A-1 (no production-src dependency); B-A's fixtures must land before V-1's fresh parity measurement runs against a suite that includes them; B-B's GH calls can happen any time after Wave A's W3 confirmation (since BONUS-CLOSE depends on the post-fix behaviour of the gate-bearing class). Both feed V-4's SUMMARY.

## GOAL-BACKWARD VERIFICATION CHAIN

1. **GOAL = LOCKED D-04 dual gate:** crit-1 (Wave-B-A canonical-positive fixture asserts whole-program structured on the apostrophe-in-`//`-comment class) AND crit-2 (fresh = 50/50 = 100.0% AND ≥ 49/50 = 98.0% floor) AND no-bar-lowering on the SAME merge commit.
   → **V-1** asserts both crit on the same head; **V-3** verifies the cross-wave silent-drift guard; **V-4** opens the PR for user-merge review.
2. **For crit-1 to pass,** the Wave-B-A fixture's parity snapshot must assert structured — which requires `-7LU6zgzViSM` actually flipping structured, which requires both walkers to tolerate the `// ma'am`-style line comments → **A-1** ships the two-site `//`-skip; **B-A** vendors the fixtures.
3. **For the canonical positive to flip,** the walker must consume past the `// ma'am` and continue the depth/string-state tracking correctly → **A-1**'s `//`-skip branch at Site 1 (findMatchingParen) + Site 2 (splitArgsWithOffsets with `current += argsStr[i]` to preserve OFFSET CONTRACT). The inherited chain-arg recogniser arms do the rest.
4. **For the upstream-grounded matcher line to hold,** the fix MIRRORS the reference branch at `splitTopLevelStatements:414-417` exactly → RESEARCH §R-2 grounded the reference pattern; **A-1** copies the branch verbatim (same `if`/`while`/`continue` shape).
5. **For no silent loc-fidelity drift,** the per-file STOP gate runs per-wave (Wave A, Wave B-A) + cross-wave (V-3) → the PV49-spirit byte-additive comment-consumption (comment chars appended to `current`; existing `skipWhitespaceAndLineComments` primitive at `pushCurrent` handles them) carries by construction (RESEARCH §R-3 + §R-7 PM-4 analytically grounded).
6. **For permanent regression coverage,** 1 canonical positive + 1 negative-control fixture (Wave B-A V-2 cadence) → 2 fixtures vendored, snapshot-captured, auto-discovered.
7. **For backlog audit completeness,** Wave 0 RUN-classifies + Wave B-B applies dispositions → 4 issues processed; CLASSIFY ≠ FIX discipline preserves SCOPE.
8. **For lessons to compound into 22+,** SUMMARY + catalogue provenance + P70 occurrence-9 row + NEW gate-bearing issue + single non-stacked PR + post-merge artifact verification → **V-4**. If 20-21 is a clean-run (0 PK18 re-poses), the THIRD consecutive clean-run phase — document explicitly.

## DOES THIS PLAN ACHIEVE THE GOAL? (closing check — anchored on D-04)

1. **Crit-1 (HARD) is reachable:** RESEARCH §R-1 9-wave bisect (RUN by researcher) + §R-2 mechanism diagnosis (grep + RUN) + §R-3 fix shape with verbatim pre/post code blocks prove the two-site `//`-skip flips the apostrophe-in-comment class; the §R-1 W1 minimal repro + W3 production exemplar are both gates. Wave A's probe re-observes (action 7 (i) + (ii)).
2. **Crit-2 (HARD) is reachable:** baseline 49/50 = 98.0%; `-7LU6zgzViSM`'s flip alone = 50/50 = 100.0%; any bonus from a coincidental exemplar flip only goes UP (target is exactly 50 — no further headroom available). The must-not-regress 98.0% floor holds by construction (the fix is strictly permissive on the `//`-comment-tolerance boundary; no existing structured-path outcome regresses per the §R-7 analytical mitigations + Wave-A action 7 (iii) + (iv) probes).
3. **D-01 (Wave-0 RE-CONFIRMATION) is RE-RUN on the fresh branch:** action 5 re-runs the §R-1 bisect; ANY divergence → STOP.
4. **D-02 (internal-class fix-shape) is HONEST:** the fix is local to our parser (no upstream RESEARCH needed); the §R-3 fix shape MIRRORS the existing `splitTopLevelStatements:414-417` reference (not a new mechanism class); Wave A action 3 implements verbatim.
5. **D-03 (STRETCH scope) is HONOURED:** the gate-bearing fix is the apostrophe-in-`//`-comment chain-arg walker class; the backlog audit RUN-classifies #156/#153/#149/#147 in Wave 0 + applies dispositions in Wave B-B; CLASSIFY ≠ FIX preserved.
6. **D-04 dual-gate is enforced:** V-1 measures both crit on the same head; the must-not-regress 98.0% floor is encoded; no bar-lowering.
7. **P70 is the spine + occurrence-9 is catalogued:** Wave 0 RE-OBSERVES the bisect; Wave V-4 files the NEW issue framing the actual class AND adds P70 occurrence-9 to hetvabhasa; the lesson ("when an exemplar STILL bareCodes after a recognition extension ships, RUN the bisect — don't trust shape-matching alone") compounds forward.
8. **PV49 carries by construction:** the byte-additive comment-consumption (comment chars stay in `current` for Site 2; only `i` advances for Site 1; existing primitive handles leading-`//` skip at `pushCurrent`) is the proof; the V-3 cross-wave STOP gate observes.
9. **PV54 does NOT trigger:** no new top-level PatternIR tag; the V-4 catalogue addendum records this explicitly.
10. **Operational discipline:** COMMIT_TEMPLATE single-quoted heredoc; P68 build hygiene with 4 distinct minification-stable greps (defensive symbols + reference symbols UNCHANGED); per-file loc-fidelity STOP gate per-wave + cross-wave; single non-stacked PR → main; `closes #<NEW-ISSUE-N>` only (NOT #143); manual #143 supersession comment; Claude never merges; post-merge artifact verification recipe recorded.

The plan achieves the D-04 GOAL: it widens the recogniser surface narrowly + via an EXISTING reference pattern applied at two new call sites, restores the structured projection for `-7LU6zgzViSM` via the inherited chain-arg recogniser arms, ships per-class permanent regression coverage, enforces the cross-wave silent-drift guard, measures fresh parity = 100.0% with the 98.0% must-not-regress floor, applies the STRETCH backlog audit dispositions via CLASSIFY-only discipline, files the gate-bearing issue framing P70 occurrence-9 (since #143 was prematurely closed in 20-16), and routes every falsified premise through PK18 — never a push-through.

## EXECUTOR NOTES

- AnviDev: issue → branch → fix → test → observe → PR → self-review → merge. NEW gate-bearing issue filed at V-4 action 1 (since #143 was prematurely closed). Branch FIRST (never code on main): `git checkout -b feat/20-21-comment-aware-walkers` from `main` (HEAD must contain `fa09cfe`). Single non-stacked PR → main; Claude NEVER merges (stop at PR-up; checkpoint to user).
- `.anvi/` + `.planning/` are gitignored — `git add -f` for catalogue + planning artifacts. NO Co-Authored-By. Commits use gitmoji + Problem/Fix body via the COMMIT_TEMPLATE below.
- **COMMIT_TEMPLATE (structural — every task's commit step says "Commit via COMMIT_TEMPLATE"; do NOT reconstruct from memory; `feedback_commit_msg_heredoc.md` zsh-substitution trap):**
  ```
  COMMIT_TEMPLATE:
  git -c commit.gpgsign=false commit -q -F - <<'MSG'
  <gitmoji> <type>(<scope>): <summary>

  Problem: <what/why>
  Fix: <what/how>
  MSG
  ```
  NEVER `git commit -m "…"` with backticks or `$()` — zsh executes the substitution and silently strips it. The single-quoted heredoc (`<<'MSG'`) + `-F -` is the only sanctioned form.
- **GitHub closes-keyword limit:** the PR body uses `closes #<NEW-ISSUE-N>` ONLY (GitHub honors ONE keyword's worth of auto-close). The 4 backlog issues (#153/#156/#149/#147) close/refine via direct `gh issue` calls in Wave B-B. `#143` stays closed (annotated as superseded via a comment in V-4 action 2).
- **P68 build hygiene per editor-src commit:** one-shot `pnpm --filter @stave/editor build`, then 4 distinct minification-stable literal-token greps on `dist/index.js` BEFORE `git commit`. **The anchors MUST be STRING/SYMBOL LITERALS (minification-stable) — NOT comments (stripped), NOT parameter names (renamed).** This phase's anchors: `findMatchingParen` + `splitArgsWithOffsets` (both surgery sites; defensive — symbols must remain) + `splitTopLevelStatements` (reference pattern; defensive — must NOT be touched) + `skipWhitespaceAndLineComments` (primitive; unchanged count vs 20-20). The fix mirrors the reference INLINE (not via primitive call), so `skipWhitespaceAndLineComments` count does NOT increase this phase (unlike 20-20). **Start `pnpm --filter @stave/editor dev` (tsup --watch) BEFORE editing `packages/editor/src/`** (memory `feedback_editor_watch_mode.md` / P66 / PV48 — workspace exports via dist/); verify the watch is ALIVE.
- **Per-file loc-fidelity STOP gate (per-wave + cross-wave V-3):** every parity-UNCHANGED corpus file's loc-fidelity snapshot diff MUST be EMPTY; the parity-CHANGED set ⊆ the enumerated allow-list = {2 Wave-B-A fixtures}. The fix is byte-additive by construction (Site 2 appends comment chars to `current`; the existing `skipWhitespaceAndLineComments` primitive consumes them at `pushCurrent`; Site 1 only advances `i`) — verify per-wave; ANY non-allow-list movement = STOP. The allow-list is NOT defensively extended.
- **vite-node import path for any local test/script importing `parseStrudel` directly:** use the deep path `./ir/parseStrudel` from `packages/editor/src/` (Wave-0 + Wave-A probes live there) or `../../../editor/src/ir/parseStrudel` from `packages/app/tests/parity-corpus/`. Mirror `_bakery-classify.spec.ts` / the 20-20 `_wave0-classify-20-20.spec.ts`.
- **The parser is a STRUCTURAL MATCHER (D-02 matcher line — PRESERVE exactly):** the new walker tolerance is purely permissive — it consumes `//` line comments inside chain-arg walking and either advances `i` (Site 1) or appends comment chars to the per-arg buffer (Site 2). It does NOT mutate the source string; does NOT add new state; does NOT introduce a new IR tag; does NOT widen the parser's accepted language beyond what upstream Strudel evaluates (acorn already tokenizes-out `//` before AST construction). The matcher line holds.
- **PK18 HARD-GATE cascade discipline (the P70 spine — the most likely trigger is the Wave-0 V-1 stdout diverging from RESEARCH §R-1, OR the Wave-A W3 production-exemplar probe NOT flipping, OR a backlog audit probe surfacing a non-superseded regression, OR crit-2 regressing below 98.0%):** a gate falsifying a premise → STOP, record VERBATIM in `20-21-OBSERVATIONS.md`, re-classify, re-pose the invalidated LOCKED decision (D-01 outcome / D-02 fix-shape / D-04 floor) to the user, reframe — NEVER push through, NEVER add a second workaround, NEVER lower the bar. New gap classes → backlog issues (issue-before-fix) → 20-22+, NOT fixed this phase.
- **Grounding Check (P69):** RESEARCH §R-2 grounded the reference pattern at `splitTopLevelStatements:414-417` (internal — INTERNAL class; D-02 outcome 1; no upstream RESEARCH needed since acorn handles `//` natively). 20-20's Ground Truth extension already covers the broader parser/evaluator pipeline; OPTIONAL 20-21 update only if executor judges value.
- **PV54 NOT triggered:** no new top-level PatternIR tag. The PV53/PV54 consumer-audit obligation is dormant. Confirm explicitly in V-4 catalogue addendum.
- **Post-merge artifact verification (NOT the badge):** `git merge-base --is-ancestor <PR-head-sha> origin/main` exit 0; `dist/index.js` greps `findMatchingParen` + `splitArgsWithOffsets` + `splitTopLevelStatements` all > 0; `skipWhitespaceAndLineComments` count unchanged (the fix is inline, not via primitive call); editor 1627 + app 417 hold; fresh parity:bakery N=50 = 50/50 (100.0%). V-4 records recipe; the USER executes post-merge.
- **String quoting** (memory `feedback_strudel_quote_style.md`): the Strudel transpiler converts double-quoted strings to mini Patterns; the fixture's `chord("<G#m>")` + `sound("bd")` uses double-quotes INSIDE arg strings (intentional — that IS the mini-pattern). The single-quote rule applies to `.p('name')` style id-args, NOT to mini-pattern bodies.
- **The apostrophe-in-comment issue is NOT a Strudel transpiler bug:** acorn's tokenizer strips `// comments` before AST construction (RESEARCH §R-2 implied; verified by upstream's permissive `// ma'am` handling in working programs). Our hand-rolled chain-arg walkers DIVERGE in the trivial way of "forgot to skip `//` before testing for string-quote delimiters." The fix mirrors what acorn does implicitly — no semantic widening.
- **What's genuinely new in 20-21:** the two-site `//`-skip at chain-arg walkers (`findMatchingParen` + `splitArgsWithOffsets`); the demonstration that PV49-spirit `//`-tolerance applies INLINE at walker sites (not just via the `skipWhitespaceAndLineComments` primitive call) — both are equivalent mechanism classes at different lexical scales; the catalogued P70 occurrence-9 (the 20-16 cascade misclassification of #143 — lesson: when an exemplar STILL bareCodes after a recognition extension ships, RUN the bisect on the exemplar BEFORE accepting the close); the THIRD consecutive clean-run phase IF 0 PK18 re-poses (continuing the 20-19 + 20-20 substrate-works cadence). 20-21 ships the FINAL residual of the post-20-17 parity-class cleanup; what's next is the pivot to bidirectional editing / Layer-3 / Closed Loop work per the project's broader roadmap.
